# Lane 5B: the upgrade path from v0.9.5 and from the 0.10 development layout

Design: dev/plans/evidence-workflow.md (read in full; A3 "Removed outright", A4, B1, B2 closely); design/readme.md "Content fundamentals" for every line the update prints
Rules: dev/plans/lanes/_rules.md (read in full)

## Sections (from the plan, verbatim)

## B2. Gate config

```json
{"gate":"recorded","ai_review_at":"high","min_strength":70,"mutation_engine":"auto",
 "sql_engine":null,"ci":"github"}
```
`resolve_gate(config) -> GateConfig`. Retired keys: `pre_push` beyond on/off,
`remote_verification`, `mutation_checks`, `quality_gate`, `platforms`, `audit_*`, `spec_dir`.

- **5B `scripts/init/update.py`** (reuses `migrate.py`'s detector and applier skeleton and
  `_back_up_copy`): detects v0.9.5 and 0.10-dev layouts; untracks and deletes committed
  proof files and receipts, untracks the blob, rewrites hooks, retires config keys, asks the
  gate question (default `recorded` when `pre_push` was `strict`), rewrites `@windows` and
  `@on` to `@env` only where confirmed, retires `figma://`; every write consent-gated and
  backed up; `sync_status` prints `→ Run: purlin:init --update` while pending.

Setup trace (every concept, from init to upgrade):

| Concept | `init` | `--gate` | `--update` | When missing later |
|---|---|---|---|---|
| gate | one question | rewritten | asked once | n/a |
| records folder and retention | created | unchanged | created; receipts removed | verify creates it |
| validation tags | in `--ci` output | unchanged | none | n/a |
| approvals | path documented | `approved` lists rules needing one | none | review list shows them |
| approver list | not needed | at `approved` | at `approved` | directive; gate exits 1 |
| risk, origin, criterion | optional; `approved` requires | `approved` lists untagged | default low and eng | `drift eng` lists |
| `designs/` | `recorded` and `approved` | if missing | if missing | spec creates on first design |
| pins | on demand | unchanged | old URLs kept, `figma://` retired | drift reports behind |
| engine | per language | unchanged | with consent | verify says unavailable, AI review one level lower |
| CI workflow (`purlin.yml`) | written under `recorded` and `approved`; under `tested` only with `--ci` | written if missing | offered | status says the gate cannot be met |
| dashboard artifact | with `--ci` | unchanged | with `--ci` | local page works |
| upstream check job | `--ci --upstream-check` | unchanged | offered when pins exist | drift covers it |
| OS matrix (`@env`) | rendered from tags | unchanged | re-rendered | status says "needs <os>"; `--update` re-renders |

Acceptance: `dev/test_init_scaffold.py`, `dev/test_init_update.py` (target 800 each) with
the two upgrade fixtures and each gate transition both ways.

## C3. Preloaded decisions (the orchestrator never asks; a lane that meets an unlisted choice picks the simplest option and records it in its DONE note)

| Question a lane might raise | Answer |
|---|---|
| Python floor | 3.9; every `open()` with `encoding='utf-8'` |
| Third-party deps in plugins or scripts | none; stdlib only (the Node plugins use no npm package beyond the test framework) |
| mutmut config location | `[tool.mutmut]` in `pyproject.toml` when it exists, else `setup.cfg` `[mutmut]`; written like the other wiring files |
| Record file name | `<YYYYMMDDTHHMMSSZ>-<commit7>-<runner>[-<os>].json`; runner slug `ci` or the developer's git email local part, lowercased, non-alphanumerics to `-` |
| Retention | newest three per feature per OS plus anything a `validated/*` tag names |
| Default branch | `git symbolic-ref refs/remotes/origin/HEAD`, else `main` |
| Approver slug in approval file names | email local part, lowercased, non-alphanumerics to `-` |
| Timestamp format everywhere | ISO 8601 UTC with `Z` |
| Test strength display | integer percent; `n/a` when no engine |
| Records from forked PRs | never committed; the job says so in the PR comment |
| xUnit detection | any `*.csproj` referencing the `xunit` package |
| Vitest compatibility | `onTestRunEnd` primary, `onFinished` kept for Vitest 1 and 2 |
| The one `@env(windows)` proof this repo keeps | the file-locking check in `static_checks.py` |
| `dev/manual/` CLI checks | hand-run only, never in `run_tests.sh` |
| Commit prefixes before phase 4B rewrites the conventions | the existing `references/commit_conventions.md` prefixes |
| Unknown tags in frozen specs | ignored with one warning until phase 9 |
| Emoji in any output | never |
| Colours in the dashboard | tokens only; a raw hex outside the inlined token block fails acceptance |
| Diagrams in docs | mermaid with the shared init block; no images except dashboard screenshots |
| A design decision not covered by `design/readme.md` | the plainer option; note it in the DONE note for the user |
| A lane finds a Part B claim false | record it under BLOCKED with evidence, skip, continue; never redesign inside the lane |
| Anything else | the simplest option that keeps the vocabulary in A1 and the deletions in C0 intact |


## Inputs

- `git show 539b9145:scripts/update/migrate.py` (524 lines; deleted in phase 0, read from
  history, never restore it): the detector and applier skeleton and `_back_up_copy` to
  copy into the new module.
- `dev/fixtures/upgrade-0.9.5/` and `dev/fixtures/upgrade-0.10-dev/` (each has a README;
  `_gitignore` is the project's `.gitignore`, renamed on copy). They are frozen: tests copy
  them to a temporary directory and `git init` there.
- `scripts/mcp/purlin/gate.py` (retired config keys), `specs.py` (tag parsing, the
  unknown-tag warning), `status.py` (where the `→ Run: purlin:init --update` line goes),
  `scripts/init/scaffold.py` (lane 5A rewrites it at the same time; its `--update` flag
  calls `update.main`), `scripts/proof/*` (the current plugins for the copies),
  `templates/gitignore.purlin`, `templates/config.json` (5A), `scripts/run/workflow.py` (2C).
- `dev/test_init_update.py` (1224) to rewrite.

## Outputs

- `scripts/init/update.py` (target 450): `update.py [--check] [--json] [--yes]
  [--project-root DIR]` and `main(argv)`; `pending(project_root) -> list` of the migrations
  a project still needs, each with an id, a one-line description and the files it touches.
  Detects the v0.9.5 layout (config `version` below 0.10, committed `.purlin/cache/`,
  proofs and verification JSON beside specs, the `windows-proofs` workflow, `spec_dir`) and
  the 0.10-dev layout (the runner registry in config, `remote_verification`,
  `mutation_checks`, the two hook shims, the committed dashboard data file, the two
  workflows, OS-scoped proof files, `@on(...)` tags, `figma://` sources, `> Visual-Reference:`).
  Applies, each consent-gated (`--yes` skips the questions) and backed up with
  `_back_up_copy`: untrack and delete the committed proof and verification JSON files
  beside specs; untrack the dashboard data file and add the two ignore lines; delete the
  pre-commit shim and its `.git/hooks` delegator, rewrite the pre-push shim to the new
  `pre-push.sh`; retire config keys and ask the gate question (default `recorded` when
  `pre_push` was `strict`, else `tested`), writing the B2 shape with the version from
  `VERSION`; rewrite `@windows` tier tags and `@on(<id>)` to `@env(<os>)` only where the
  mapping is confirmed by the person (linux, macos or windows read from the retired
  registry entry); retire `figma://` sources by rewriting `> Source:` to a
  `designs/<anchor>/` path and dropping `> Visual-Reference:` and `> Visual-Hash:`; delete
  the old workflows and offer `purlin.yml` through `render_workflow`; refresh the plugin
  copies; create `.purlin/records/` with its README. One commit at the end,
  `chore(update): migrate to <VERSION> (<ids>)`. `--check` prints what is pending and
  exits 1 when anything is; `sync_status` shows `→ Run: purlin:init --update` while
  `pending()` is non-empty (add the smallest lazy call in `scripts/mcp/purlin/status.py`).
- `dev/test_init_update.py` (target 800): each fixture copied to a temp git repo:
  `--check` exits 1 and names every migration; `--yes` applies them all; `--check` then
  exits 0; no proof or verification JSON remains tracked; the config is in the B2 shape
  with the right gate default; the `@env` rewrite happens only when confirmed (drive the
  prompt with a scripted answer) and `@on` is gone after; `figma://` is gone; the backups
  exist; the commit message carries the ids; running `--yes` twice is idempotent; the
  status line appears before and not after.
- `dev/test_vocabulary.py`: `PENDING_REWRITE` lists `scripts/init/` and `dev/test_init_update.py`; lane 5A
  runs at the same time and edits the same tuple for its own files, so change only the
  `dev/test_init_update.py` entry (delete it) and add the `# retired` line exclusion for
  `scripts/init/update.py` described under Acceptance; leave `scripts/init/` to 5A.

## Acceptance

```bash
cd /Users/richlabarca/LocalCode/purlin-wt/5B
export PATH=/Users/richlabarca/LocalCode/purlin/.venv/bin:$PATH
python3 -m pytest dev/test_init_update.py dev/test_vocabulary.py -q -p no:cacheprovider 2>&1 | tail -3
T=$(mktemp -d) && cp -R dev/fixtures/upgrade-0.9.5/. "$T" && mv "$T/_gitignore" "$T/.gitignore" && git -C "$T" init -q && git -C "$T" add -A && git -C "$T" -c user.name=t -c user.email=t@t commit -qm init && python3 scripts/init/update.py --check --project-root "$T"; echo "check exit $?"
test "$(wc -l < scripts/init/update.py | tr -d ' ')" -le 550 || echo "UPDATE TOO LONG: $(wc -l < scripts/init/update.py)"
grep -rn -i -w 'audit\|gauge\|HOLLOW\|PROVABLE\|receipt\|platform\|mode\|forge\|Pages' scripts/init/update.py dev/test_init_update.py | grep -v 'sys\.platform' | sed 's/^/RETIRED WORD: /'
git clean -f specs/ >/dev/null; git status --short
```

The pytest line must show 0 failed; the check on the v0.9.5 fixture prints the pending
migrations and `check exit 1`; no `TOO LONG` or `RETIRED WORD` line. The strings the
detector matches (old tag names, old file suffixes, old config keys) may appear in
`update.py` only inside the migration table, each on a line ending with the comment
`# retired`, and `dev/test_vocabulary.py` excludes lines carrying that comment in that
one file; add that exclusion.

## Rules that bind this lane

- Worktree `/Users/richlabarca/LocalCode/purlin-wt/5B`, branch `lane/5B` from `evidence-workflow`.
- `specs/` untouched; the two fixtures untouched; never run the update against this
  checkout (phase 9 does that).
- Never recreate `scripts/update/migrate.py`, `references/legacy_features_migration.md`,
  `dev/test_e2e_spec_migration.py`.
- Do not edit `scripts/init/scaffold.py` (lane 5A, at the same time) beyond nothing: the
  `--update` hand-off is 5A's; `update.py` is importable and runnable on its own.
- No retired vocabulary outside the marked migration table (`sys.platform` excepted); no
  emoji; stdlib only; Python 3.9.
- Commit prefixes `feat(update):`, `test(update):`; the two attribution lines;
  `PURLIN_SKIP_DIGEST=1`.
- End with the DONE note.
