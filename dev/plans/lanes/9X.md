# Lane 9X: phase 9 cleanup, one home for the plugin rules and one collection fix

Design: dev/plans/evidence-workflow.md (read in full)
Rules: dev/plans/lanes/_rules.md (read in full); the phase 9 rules in dev/plans/lanes/9B.md "Rules every phase 9 lane follows"

## 1. The plugin contract lives in the anchor only

`specs/run/run_script.md` RULE-23 to RULE-35 restate what `specs/_anchors/proof_common.md`
RULE-1 to RULE-14 already claim (runtime location, root walk, `test_file` shape, no-marker
run, merge key, sort order, skip handling, loud exit, retired keyword refused, temp-file
write, standard-library-only, marker shapes, tier placement), and every affected test carries
both markers. CLAUDE.md forbids two homes for one concept. Delete run_script RULE-23 to
RULE-35 and their proofs (leave the numbers vacant; the format allows it; do not renumber
RULE-36 to RULE-38), add `> Requires: proof_common` to `specs/run/run_script.md`, and remove
the now-orphaned `run_script` markers from the tests (`dev/test_multilang_proof_plugins.py`,
`dev/test_proof_plugins_missing.py`, `dev/test_proof_stress.py`, `dev/test_plugin_contract.py`,
the shell suites under `dev/`): a test keeps its `proof_common` marker and loses only the
`run_script` one that named a deleted proof. Check RULE-38 against proof_common RULE-8 and
RULE-9: keep it only if it claims something the anchor does not.

## 2. One collection fix, not two

Two lanes fixed the same pytest collection problem: `conftest.py` at the root
(`collect_ignore = ['dev/fixtures']`, which framework detection also reads pytest off) and
`dev/conftest.py` (`collect_ignore_glob = ['fixtures/*']`). Keep the root file; drop the
`collect_ignore_glob` line and its comment from `dev/conftest.py`; confirm a bare `pytest -q`
from the root still collects everything under `dev/` and nothing under `dev/fixtures/`.

## Acceptance

```bash
cd /Users/richlabarca/LocalCode/purlin-wt/9X
export PATH=/Users/richlabarca/LocalCode/purlin/.venv/bin:/opt/homebrew/opt/dotnet@8/bin:$PATH
for f in run_script proof_common; do python3 scripts/run/purlin_run.py --feature $f --quick --project-root . 2>&1 | grep -E "^$f|Tested|Proof ready|missing" | head -4; done
grep -c 'run_script' dev/test_multilang_proof_plugins.py dev/test_proof_plugins_missing.py dev/test_proof_stress.py
grep -E '^- (RULE|PROOF)-(2[3-9]|3[0-5])\b' specs/run/run_script.md | sed 's/^/STILL PRESENT: /'
grep -q '^> Requires: proof_common' specs/run/run_script.md && echo "requires ok"
grep -c 'collect_ignore' conftest.py dev/conftest.py
python3 -m pytest -q -p no:cacheprovider 2>&1 | tail -1
python3 -m pytest dev/test_vocabulary.py dev/test_schema_spec_format.py -q -p no:cacheprovider 2>&1 | tail -1
git status --short
```

run_script shows every remaining rule Tested; proof_common 14 Tested; no `STILL PRESENT`
line; `requires ok`; `conftest.py:1` and `dev/conftest.py:0`; the whole-tree pytest and the
two checks show 0 failed; tree clean.

## Rules that bind this lane

- Worktree `/Users/richlabarca/LocalCode/purlin-wt/9X`, branch `lane/9X` from `evidence-workflow`.
- Touch only `specs/run/run_script.md`, the test files named, `dev/conftest.py`. Lane 9F runs
  at the same time on `specs/skills/`, `specs/instructions/`, `specs/tools/`, `dev/test_skills.py`,
  `dev/run_tests.sh`, `dev/test_e2e_build_changeset.sh`: do not touch those.
- No retired vocabulary; commits `spec(run_script):`, `test(run_script):`, `chore:` with
  `--author="Claude Opus 5 <noreply@anthropic.com>"`; the two attribution lines;
  `PURLIN_SKIP_DIGEST=1`.
- End with the DONE note.
