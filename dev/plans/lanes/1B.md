# Lane 1B: static_checks.py slimmed to the five languages' test-body checks

Design: dev/plans/evidence-workflow.md (read in full)
Rules: dev/plans/lanes/_rules.md (read in full)

## Sections (from the plan, verbatim)

- **1B `scripts/audit/static_checks.py`** slimmed to the Python, shell, JS, C#, SQL checks,
  `_run_body_checks`, `analyze_test_file`, `deterministic_sweep`, `_extract_test_code`;
  findings vocabulary; delete design pass, cache engine, C and PHP; target under 1,200
  lines. `dev/test_static_checks.py` pruned.

## B7. Brief `scripts/review/brief.py` (~250 lines)

Layers, cheapest first, stop when enough for the risk: free checks on proof text (no
expected value, happy path only, vague verb, missing trigger, tier mismatch, implementation
coupling), free checks on the test body (`static_checks.py` findings: `no_assertion`,
`tautology`, `mock_of_target`, `logic_mirroring`), test strength from the latest record, then
the AI review when `needs_ai_review`, prompt built from `references/review_criteria.md`
verbatim; design rules add mock beside screenshot with an AI pre-compare. Output: brief JSON beside the approval it informs plus a text rendering; CI writes briefs for the review list.

## C3. Preloaded decisions (the orchestrator never asks; a lane that meets an unlisted choice picks the simplest option and records it in its DONE note)

| Question a lane might raise | Answer |
|---|---|
| Python floor | 3.9; every `open()` with `encoding='utf-8'` |
| Third-party deps in plugins or scripts | none; stdlib only (the Node plugins use no npm package beyond the test framework) |
| mutmut config location | `[tool.mutmut]` in `pyproject.toml` when it exists, else `setup.cfg` `[mutmut]`; written like the other wiring files |
| Record file name | `<YYYYMMDDTHHMMSSZ>-<commit7>-<runner>[-<os>].json`; runner slug `ci` or the developer's git email local part, lowercased, non-alphanumerics to `-` |
| Retention | newest three per feature per OS plus anything a `validated/*` tag names |
| Default branch | `git symbolic-ref refs/remotes/origin/HEAD`, else `main` |
| Approver slug in approval file names | email local part, lowercased, non-alphanumerics to `-` |
| Timestamp format everywhere | ISO 8601 UTC with `Z` |
| Test strength display | integer percent; `n/a` when no engine |
| Records from forked PRs | never committed; the job says so in the PR comment |
| xUnit detection | any `*.csproj` referencing the `xunit` package |
| Vitest compatibility | `onTestRunEnd` primary, `onFinished` kept for Vitest 1 and 2 |
| The one `@env(windows)` proof this repo keeps | the file-locking check in `static_checks.py` |
| `dev/manual/` CLI checks | hand-run only, never in `run_tests.sh` |
| Commit prefixes before phase 4B rewrites the conventions | the existing `references/commit_conventions.md` prefixes |
| Unknown tags in frozen specs | ignored with one warning until phase 9 |
| Emoji in any output | never |
| Colours in the dashboard | tokens only; a raw hex outside the inlined token block fails acceptance |
| Diagrams in docs | mermaid with the shared init block; no images except dashboard screenshots |
| A design decision not covered by `design/readme.md` | the plainer option; note it in the DONE note for the user |
| A lane finds a Part B claim false | record it under BLOCKED with evidence, skip, continue; never redesign inside the lane |
| Anything else | the simplest option that keeps the vocabulary in A1 and the deletions in C0 intact |


## Inputs

- `scripts/audit/static_checks.py` (3443 lines). Function map (line: name): 86-165 Python
  parsing (`_split_source_lines`, `_segment`, `_python_proof_functions`, `_python_parse`);
  167-362 Python checks (`_has_assertion`, `_is_always_true`, `_check_assert_true`,
  `_check_bare_except`, `_collect_call_names`, `_check_logic_mirroring`,
  `_check_mock_target_match`); 363 `check_python`; 409 `check_shell`; 510-705 JS
  (`_read_js_string` ... `check_js`); 726-913 C# (`_skip_c_like_noncode` ... `check_csharp`);
  914 `_is_constant_expression`; 973-1092 PHP (delete); 1093-1197 SQL (`_sql_executable`
  ... `check_sql`); 1198-1343 C (delete); 1344-1467 per-language tautology and assertion
  helpers (keep JS, C#, SQL; delete PHP and C); 1468 `_run_body_checks`; 1500
  `resolve_test_file_from_name`; 1578 `analyze_test_file`; 1599 `_read_rule_descriptions`;
  1633 `_proof_file_parts`; 1649 `_split_proof_tags`; 1720 `_read_proof_descriptions`;
  1807-1904 the design pass (`_design_finding`, `_read_proof_tags`, `check_proof_design`:
  delete; lane 1A lifts the heuristics into the package); 1905 `audit_scope` (delete);
  2034-2234 `_sweep_unmeasurable_reason`, `_sweep_file_verdicts`, `deterministic_sweep`
  (keep); 2235 `check_spec_coverage`; 2256 `check_proof_file`; 2315 `compute_proof_hash`,
  2327 `compute_design_hash` (delete; the triple hash is phase 3's `approve.py`);
  2338-2649 proof-input resolution (`ProofInputsError`, `_read_project_config`,
  `_find_spec_path`, `_RunCache`, `_file_text`, `run_scope`, `_spec_inputs`,
  `_proof_backings`, `_proof_records`, `_find_proof_record`, `_python_proof_sources`,
  `_test_bodies`, `_extract_test_code`, `resolve_proof_inputs`: keep what
  `_extract_test_code` and `deterministic_sweep` need); 2650-2755 cache keys and stamps
  (delete); 2756-2839 file locking (`_lock_exclusive`, `try_lock_exclusive`, `_unlock`:
  keep, see below); 2840-3150 cache validation, write, criteria, clear, prune (delete);
  3151 `_force_utf8_stdio`, 3163 `main` (keep, slimmed).
- `dev/test_static_checks.py` (3650 lines plus the cheat-matrix rows lane 0A appended at
  the end). Classes to keep: the Python, shell, JS, C#, SQL check classes, `TestJsonOutput`,
  `TestExitCodes`, `TestSpecCoverage`, `TestProofIdCollision`, `TestProofRuleOrphan`,
  `TestCrossPlatformPortability` (rename it: the word is retired), `TestDispatchAllExtensions`
  (minus PHP and C), `TestDeterministicSweep`, `TestWriteCacheLocking` reduced to the
  locking helper itself, `TestRunScope`, `TestSingleReadPerFile`, `TestOneBodyCheckDriver`,
  `TestCliSelfDocumentation`, the lifted cheat-matrix rows. Delete every class about the
  cache, criteria, cache keys, stamps, the design pass, PHP and C.
- `references/formats/proofs_format.md` for the proof entry shape (lane 2A rewrites it).

## Outputs

- `git mv scripts/audit/static_checks.py scripts/review/static_checks.py` and delete the
  empty `scripts/audit/` directory: the old directory name is retired vocabulary and would
  otherwise sit in every import line. Lane 3A's `brief.py` and `approve.py` land beside it.
- `scripts/review/static_checks.py` under 1,200 lines: the Python, shell, JS, C# and SQL
  body checks; `_run_body_checks`; `analyze_test_file`; `deterministic_sweep`;
  `_extract_test_code` (with what it needs); the findings vocabulary as the only output
  names: `no_assertion`, `tautology`, `mock_of_target`, `logic_mirroring`, `bare_except`,
  `assert_true_literal`, `fixture_only`, `happy_path_only` (keep every existing check that
  maps onto one of these; rename identifiers that carry a retired word). Proof records are
  read from `.purlin/runtime/proofs/<feature>.<tier>.json` (one file per feature and tier,
  entries in today's shape minus the runner and OS fields, no run marker); a missing
  directory is an empty result. The file-locking helpers stay as a small public pair
  `lock_exclusive(path)` / `unlock(handle)` with the Windows branch (`msvcrt`) and the POSIX
  branch (`fcntl`): this is the one place the repository keeps a Windows-specific proof, and
  `sys.platform` is allowed there. A slim `main()` keeps the CLI: file, feature,
  `--project-root`, `--json`, `--sweep`, exit codes documented in `--help`.
- `dev/test_static_checks.py` pruned to the classes listed above, green, no retired
  vocabulary anywhere in the file; the cheat-matrix rows that run the old plugins
  (`_run_python`, `_run_typescript`, `_run_sql`) keep working because the old plugins still
  exist until phase 2.
- No other file changes except import paths that named `scripts/audit` in kept files
  under `dev/` (grep for `scripts/audit` and `scripts', 'audit'`); leave `scripts/ci/verify_gate.py`
  and `scripts/hooks/refresh_digest.py` alone (lanes 3A and 1A repoint them).

## Acceptance

```bash
cd /Users/richlabarca/LocalCode/purlin-wt/1B
test "$(wc -l < scripts/review/static_checks.py | tr -d ' ')" -lt 1200 || echo "TOO LONG: $(wc -l < scripts/review/static_checks.py)"
test ! -e scripts/audit || echo "OLD DIRECTORY REMAINS"
python3 -m pytest dev/test_static_checks.py -q -p no:cacheprovider 2>&1 | tail -3
python3 scripts/review/static_checks.py --help >/dev/null && echo "cli ok"
grep -n -i -w 'audit\|gauge\|HOLLOW\|PROVABLE\|receipt\|platform\|mode\|php\|criteria' scripts/review/static_checks.py dev/test_static_checks.py | grep -v 'sys\.platform' | sed 's/^/RETIRED WORD: /'
git clean -f specs/ >/dev/null; git status --short
```

The pytest line must show 0 failed; `cli ok` must print; no `TOO LONG`, `OLD DIRECTORY
REMAINS` or `RETIRED WORD` line.

## Rules that bind this lane

- Worktree `/Users/richlabarca/LocalCode/purlin-wt/1B`, branch `lane/1B` from `evidence-workflow`.
- `specs/` untouched.
- Never recreate: `scripts/audit/` after the move, `scripts/proof/c_purlin.h`,
  `scripts/proof/c_purlin_emit.py`, `scripts/proof/phpunit_purlin.php`,
  `references/audit_criteria.md`, `dev/test_cheat_matrix.py`.
- No retired vocabulary in any file you write (`sys.platform` excepted); no emoji.
- Commit prefixes `feat(static_checks):`, `test(static_checks):`; the two attribution
  lines; `PURLIN_SKIP_DIGEST=1`.
- Lane 1A runs at the same time on `scripts/mcp/` and the tests that import the server:
  do not touch `scripts/mcp/`, `scripts/hooks/`, `scripts/ci/` or `dev/test_mcp_server.py`.
- End with the DONE note.
