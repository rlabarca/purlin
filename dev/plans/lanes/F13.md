# Lane F13: the board on the brand navy, as the design system's own kit draws it

Design: dev/plans/evidence-workflow.md (A11); design/readme.md in full
Rules: dev/plans/lanes/_rules.md (read in full); the phase 9 rules in dev/plans/lanes/9B.md (rule tags, the author flag)

The user compared the built dashboard with the design system's own dashboard kit and the
colours do not match: the kit draws the board on the brand navy (`--purlin-navy-800`
ground, `--purlin-navy-900` bars and tiles, cream ink, blush secondary text, copper
eyebrows, the shared state hues), while our page sets `data-surface="product"` and so
takes the cool slate ramp. The kit is the reference. Its sources are at
`/private/tmp/claude-501/-Users-richlabarca-LocalCode-purlin/bdea5a4d-f9c8-4410-904f-32e75781c94b/scratchpad/ds/ui_kits/dashboard/`
(`App.jsx`, `OverviewScreen.jsx`, `RuleScreen.jsx`, `ReviewScreen.jsx`, `index.html`,
`light.html`, `README.md`): read them for which token each surface uses (stat tiles, group
headers, table rows, pills, coverage bars, the top bar) and match the page to them.

## Change

- Drop `data-surface="product"` from the page so it inherits the default warm theme from
  `design/tokens/theme-dark.css` and, under `data-theme="light"`, the paper ramp (which the
  user already tuned to a yellowish white; keep it).
- Walk `scripts/report/src/styles.css` and the three screens against the kit: every
  surface, border, text and state colour comes from the semantic aliases the kit uses for
  that element. Stat tiles and panels sit one tint step above the ground with a hairline,
  as the readme's "Cards" rule says. Nothing else changes: no new component, no new
  colour, no raw hex outside the token block.
- Rebuild, run `dev/test_purlin_report.py` (adjust any test that asserted the product
  surface; add one that asserts the page carries no `data-surface` attribute and that
  the body's computed background in the dark theme equals `--purlin-navy-800`), regenerate
  all five screenshots, and refresh the root `purlin-report.html`.
- `specs/dashboard/purlin_report.md`: edit the rule that names the product surface, if
  one does (keep its number), and add the new proof.

## Acceptance

```bash
cd /Users/richlabarca/LocalCode/purlin-wt/F13
export PATH=/Users/richlabarca/LocalCode/purlin/.venv/bin:$PATH
python3 dev/build_report.py && python3 -m pytest dev/test_purlin_report.py -q -p no:cacheprovider 2>&1 | tail -1
grep -c 'data-surface' scripts/report/purlin-report.html
python3 - <<'PY'
import re
t = open('scripts/report/purlin-report.html', encoding='utf-8').read()
i = t.index('id="purlin-tokens"'); j = t.index('</style>', i)
outside = t[:i] + t[j:]
print('raw hex outside tokens:', [m for m in re.findall(r'#[0-9a-fA-F]{3,8}\b', outside) if not m.lower().startswith('#purlin')][:3] or 'none')
PY
python3 dev/capture_doc_screenshots.py >/dev/null && echo "screenshots ok"
git status --short
```

The pytest line shows 0 failed; the grep prints 0; no raw hex; screenshots ok; tree clean.
Put the dark and the light board renders side by side in your DONE note as paths under
your scratch directory so the orchestrator can look.

## Rules that bind this lane

- Worktree `/Users/richlabarca/LocalCode/purlin-wt/F13`, branch `lane/F13` from `evidence-workflow`.
- Touch only `scripts/report/src/`, the built page, `dev/test_purlin_report.py`,
  `specs/dashboard/purlin_report.md`, `docs/images/*.png`, `docs/dashboard.md` if a sentence
  is now wrong. Do not edit `design/`.
- No retired vocabulary; commits `feat(purlin_report):`, `spec(purlin_report):` with
  `--author="Claude Opus 5 <noreply@anthropic.com>"`; the two attribution lines;
  `PURLIN_SKIP_DIGEST=1`.
- End with the DONE note.
