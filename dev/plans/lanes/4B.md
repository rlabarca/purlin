# Lane 4B: the skills test, verify, review, approve, status, drift, find, rename; the references; the vocabulary check

Design: dev/plans/evidence-workflow.md (read in full; A1, A3, A4, A5, A6, A10 are what these skills say); design/readme.md "Content fundamentals" for the voice
Rules: dev/plans/lanes/_rules.md (read in full)

## Sections (from the plan, verbatim)

## B3. Run script `scripts/run/purlin_run.py` (~450 lines)

```
purlin_run.py (--feature NAME ... | --all) (--quick | --record [--commit] [--ci] [--tag NAME] | --remote) [--tier unit|all] [--project-root DIR]
```
`--quick` (from `purlin:test`): plugins run the tagged tests into `.purlin/runtime/proofs/`,
states printed. `--record` (from `purlin:verify`): tests, breaks, a record written;
`--commit` commits it under the developer's identity (the `tested` default); `--ci` commits through the git host API, auto-approves, writes briefs, posts the PR comment, publishes the artifact. `--remote` (from `purlin:verify --remote`): push the current branch, watch the workflow (`gh run watch` on GitHub; ADO prints the pipeline URL and returns), pull, print the table. Proofs tagged `@env` for another OS are skipped locally and listed as "needs <os>". Exit 0 ok, 1 test failed or evidence missing, 2 bad invocation.

### Phase 4: skills and agent (Opus 4.8, 2 lanes; Fable reviews)

Targets: `init` 250, `spec` 240, `spec-from-code` 150, `anchor` 140, `build` 130, `test`
70, `verify` 80, `review` 130, `approve` 70, `status` 60, `drift` 120, `find` and `rename`
60 each. **4A**: `agents/purlin.md` (one page: core loop, five NEVERs, routing table for
PM, designer, engineer, QA including plain-language routes to every subcommand), `init`,
`spec`, `spec-from-code`, `anchor`, `build`. **4B**: `test`, `verify`, `review`,
`approve`, `status`, `drift`, `find`, `rename`; `references/purlin_commands.md`,
`commit_conventions.md`, `drift_criteria.md`, `hard_gates.md` (the gate, defined once),
`glossary.md` (A1 plus a retired-terms table). Acceptance: lengths met; `dev/test_vocabulary.py`
(20 lines) greps every shipped file for retired terms, glossary excepted.

## C3. Preloaded decisions (the orchestrator never asks; a lane that meets an unlisted choice picks the simplest option and records it in its DONE note)

| Question a lane might raise | Answer |
|---|---|
| Python floor | 3.9; every `open()` with `encoding='utf-8'` |
| Third-party deps in plugins or scripts | none; stdlib only (the Node plugins use no npm package beyond the test framework) |
| mutmut config location | `[tool.mutmut]` in `pyproject.toml` when it exists, else `setup.cfg` `[mutmut]`; written like the other wiring files |
| Record file name | `<YYYYMMDDTHHMMSSZ>-<commit7>-<runner>[-<os>].json`; runner slug `ci` or the developer's git email local part, lowercased, non-alphanumerics to `-` |
| Retention | newest three per feature per OS plus anything a `validated/*` tag names |
| Default branch | `git symbolic-ref refs/remotes/origin/HEAD`, else `main` |
| Approver slug in approval file names | email local part, lowercased, non-alphanumerics to `-` |
| Timestamp format everywhere | ISO 8601 UTC with `Z` |
| Test strength display | integer percent; `n/a` when no engine |
| Records from forked PRs | never committed; the job says so in the PR comment |
| xUnit detection | any `*.csproj` referencing the `xunit` package |
| Vitest compatibility | `onTestRunEnd` primary, `onFinished` kept for Vitest 1 and 2 |
| The one `@env(windows)` proof this repo keeps | the file-locking check in `static_checks.py` |
| `dev/manual/` CLI checks | hand-run only, never in `run_tests.sh` |
| Commit prefixes before phase 4B rewrites the conventions | the existing `references/commit_conventions.md` prefixes |
| Unknown tags in frozen specs | ignored with one warning until phase 9 |
| Emoji in any output | never |
| Colours in the dashboard | tokens only; a raw hex outside the inlined token block fails acceptance |
| Diagrams in docs | mermaid with the shared init block; no images except dashboard screenshots |
| A design decision not covered by `design/readme.md` | the plainer option; note it in the DONE note for the user |
| A lane finds a Part B claim false | record it under BLOCKED with evidence, skip, continue; never redesign inside the lane |
| Anything else | the simplest option that keeps the vocabulary in A1 and the deletions in C0 intact |


## Inputs

- The current files, for structure and frontmatter only: `skills/test/SKILL.md` (218),
  `skills/verify/SKILL.md` (285), `skills/status/SKILL.md` (142), `skills/drift/SKILL.md`
  (279), `skills/find/SKILL.md` (72), `skills/rename/SKILL.md` (136);
  `references/purlin_commands.md` (111), `commit_conventions.md` (117), `drift_criteria.md`
  (203), `hard_gates.md` (95), `glossary.md` (75).
- The scripts a skill invokes, always through `${CLAUDE_PLUGIN_ROOT}`:
  `scripts/run/purlin_run.py` (B3 grammar above: `--quick` for test, `--record [--commit]`
  and `--remote` and `--tag` for verify), `scripts/review/brief.py` and `approve.py`
  (phase 3: `brief.py --feature <f> [--rule RULE-N]` writes the brief JSON and text;
  `approve.py <feature> [RULE-N ...] [--batch]` writes approval files and makes the signed
  commit; the review list is `payload.review_list`), `scripts/ci/verify_gate.py --check`,
  the MCP tools `sync_status` and `drift` with the role argument, `scripts/mcp/purlin/ids.py`
  for rename.
- `references/review_criteria.md` (lane 3B, at the same time; cite by path).

## Outputs (target line counts from the plan; frontmatter included)

- `skills/test/SKILL.md` (70), `verify/SKILL.md` (80: local, `--remote`, `--tag`, what CI
  does with `--ci`, the record's label and what counts under each gate), `review/SKILL.md`
  (130, new: no arguments needed; finds every rule that needs a look, shows the review
  list by risk, walks it one brief at a time with approve, add a case, or skip; a feature,
  rule or filter only narrows it; adding a case in plain language drafts the proof and test
  for the next build), `approve/SKILL.md` (70, new: one rule, a feature or a batch, as a
  signed commit by someone on the approver list; under `approved` it reaches main by PR),
  `status/SKILL.md` (60), `drift/SKILL.md` (120: the four role views and when to run
  each), `find/SKILL.md` (60), `rename/SKILL.md` (60: across specs, tests, approvals,
  records).
- `references/purlin_commands.md`: the A3 table as the one home of the skill one-liners,
  every command's canonical syntax and the plain-language phrases that reach it.
- `references/commit_conventions.md`: prefixes for 0.10.0: `spec(<name>):`, `feat(<name>):`
  with the changeset body, `fix(<name>):`, `test(<name>):`, `purlin: record for <commit7>`
  (the record commit, developer or CI), `approve(<name>): RULE-N ...` (signed),
  `anchor(<name>): create | sync (<sha>) | propose`, `chore(update): migrate to <VERSION>
  (<ids>)`, `chore:`, `docs:`. The verification commit of earlier versions is gone.
- `references/drift_criteria.md`: file classification, config field ownership, and the
  drift detection per role from A3 and A6.
- `references/hard_gates.md`: the gate defined once: the three levels, what CI requires
  before merge under each, the three GitHub rulesets and the ADO branch security, the
  approver list and the signature check, what the pre-push hook does (optional,
  `--quick` only), and the statement that nothing in a Claude Code hook gates anything.
- `references/glossary.md`: A1 verbatim as the definitions, then one retired-terms table
  (old term → replacement or "removed"), the one place the retired words may appear.
- `dev/test_vocabulary.py` (about 40 lines): greps every tracked file for the retired
  terms as whole words, case-insensitive (`audit`, `gauge`, `HOLLOW`, `PROVABLE`, `receipt`,
  `platform`, `@on(`, `mutation score`, `caught score`, `records branch`, `forge`, `queue`,
  `CODEOWNERS`, `approver rule`; `Pages` capitalised and whole-word; `mode` whole-word in
  `.md` files only) and fails naming file and line. Exclusions, each with its reason in a
  comment: `references/glossary.md` (the retired table), `design/` (an external design
  system copied verbatim), `dev/fixtures/upgrade-*/` (old layouts on purpose),
  `dev/plans/` (the plan names what it retires), `dev/test_vocabulary.py` itself, the
  literal `sys.platform`, and `specs/` and `.purlin/` until phase 9 (frozen). Plus a
  `_PENDING_REWRITE` set of paths that later phases still rewrite, each with the phase that
  clears it: `scripts/ci/verify_gate.py` (3A), `tools/PM/`, `tools/QA/` (7),
  `scripts/init/scaffold.py`, `scripts/hooks/pre-push.sh`, `.purlin/hooks/`,
  `templates/config.json` (5A), `scripts/init/update.py` (5B), `scripts/report/purlin-report.html`
  (6, if it lands after you), `docs/`, `README.md`, `CLAUDE.md`, `RELEASE_NOTES.md`,
  `assets/purlin-logo.svg` (8), and every `skills/*/SKILL.md` and `agents/purlin.md` that
  lane 4A rewrites at the same time (4A). Later lanes delete their entries as they land.
  Add it to the pytest pool in `dev/run_tests.sh`.

## Acceptance

```bash
cd /Users/richlabarca/LocalCode/purlin-wt/4B
for f in skills/test/SKILL.md:80 skills/verify/SKILL.md:90 skills/review/SKILL.md:140 skills/approve/SKILL.md:80 skills/status/SKILL.md:70 skills/drift/SKILL.md:130 skills/find/SKILL.md:70 skills/rename/SKILL.md:70; do p=${f%%:*}; n=${f##*:}; test "$(wc -l < $p | tr -d ' ')" -le "$n" || echo "TOO LONG: $p $(wc -l < $p)"; done
for f in skills/review/SKILL.md skills/approve/SKILL.md; do head -1 $f | grep -q '^---$' || echo "NO FRONTMATTER: $f"; done
python3 -m pytest dev/test_vocabulary.py -q -p no:cacheprovider 2>&1 | tail -3
grep -n 'dev/\|specs/mcp\|specs/skills\|specs/instructions' skills/*/SKILL.md references/purlin_commands.md references/commit_conventions.md references/drift_criteria.md references/hard_gates.md references/glossary.md | sed 's/^/DEV PATH IN SHIPPED FILE: /'
grep -q 'test_vocabulary.py' dev/run_tests.sh && echo "sweep ok"
git status --short
```

The pytest line must show 0 failed (with the pending set in place); `sweep ok`; nothing else.

## Rules that bind this lane

- Worktree `/Users/richlabarca/LocalCode/purlin-wt/4B`, branch `lane/4B` from `evidence-workflow`.
- `specs/` untouched. Do not touch `agents/purlin.md`, `skills/init`, `spec`,
  `spec-from-code`, `anchor`, `build`, or `references/spec_quality_guide.md` and
  `review_criteria.md` (lanes 4A and 3B).
- Never recreate `skills/audit/`, `references/audit_criteria.md`,
  `references/remote_verification.md`, `references/formats/receipt_format.md`.
- Every skill is prose against Part A and B3; scripts through `${CLAUDE_PLUGIN_ROOT}`
  only; no `dev/` path and no path into this repository's `specs/` in a shipped file. Every
  skill ends by naming the next step computed from the state.
- No retired vocabulary outside the glossary's retired table; no emoji.
- Commit prefixes `docs:` for skills and references, `test(vocabulary):` for the check;
  the two attribution lines; `PURLIN_SKIP_DIGEST=1`.
- End with the DONE note.
