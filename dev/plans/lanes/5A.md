# Lane 5A: init rebuilt around the one question

Design: dev/plans/evidence-workflow.md (read in full; A2, A4, A5, A10 closely); design/readme.md "Content fundamentals" for every line init prints
Rules: dev/plans/lanes/_rules.md (read in full)

## Sections (from the plan, verbatim)

## B2. Gate config

```json
{"gate":"recorded","ai_review_at":"high","min_strength":70,"mutation_engine":"auto",
 "sql_engine":null,"ci":"github"}
```
`resolve_gate(config) -> GateConfig`. Retired keys: `pre_push` beyond on/off,
`remote_verification`, `mutation_checks`, `quality_gate`, `platforms`, `audit_*`, `spec_dir`.

### Phase 5: init and upgrade (Opus 5, 2 lanes)

- **5A `scripts/init/scaffold.py`** rebuilt (target 600): the gate question and nothing else on a project with code; language question only on an empty tree; approvers question only under `approved`; git host read from the remote URL; plugin and engine install; `[tool.mutmut]` written like the other wiring files and listed in the summary; config; hooks (optional `pre-push --quick` only; the pre-commit hook is removed since it only committed the blob; the Claude Code refresh hook that regenerates local dashboard data stays); `.gitignore` block; dashboard copy; `designs/` and `.purlin/records/`
  with READMEs; the CI workflow written automatically under `recorded` and `approved` (host from the remote; `--ci github|ado` overrides; the OS matrix rendered from `@env` tags and re-rendered by `--update` when tags change) with verify, API record commit, auto-approve, briefs, PR comment and artifact steps, and the three rulesets printed;
  `--ci --upstream-check`; `--gate <level>` (raising `approved` asks for approver emails, writes `approvers`, prints the signing setup, lists untagged rules); under `tested` no CI is written unless `--ci` is passed; `--add <language>`; `--dry-run`.
- **5B `scripts/init/update.py`** (reuses `migrate.py`'s detector and applier skeleton and
  `_back_up_copy`): detects v0.9.5 and 0.10-dev layouts; untracks and deletes committed
  proof files and receipts, untracks the blob, rewrites hooks, retires config keys, asks the
  gate question (default `recorded` when `pre_push` was `strict`), rewrites `@windows` and
  `@on` to `@env` only where confirmed, retires `figma://`; every write consent-gated and
  backed up; `sync_status` prints `→ Run: purlin:init --update` while pending.

Setup trace (every concept, from init to upgrade):

| Concept | `init` | `--gate` | `--update` | When missing later |
|---|---|---|---|---|
| gate | one question | rewritten | asked once | n/a |
| records folder and retention | created | unchanged | created; receipts removed | verify creates it |
| validation tags | in `--ci` output | unchanged | none | n/a |
| approvals | path documented | `approved` lists rules needing one | none | review list shows them |
| approver list | not needed | at `approved` | at `approved` | directive; gate exits 1 |
| risk, origin, criterion | optional; `approved` requires | `approved` lists untagged | default low and eng | `drift eng` lists |
| `designs/` | `recorded` and `approved` | if missing | if missing | spec creates on first design |
| pins | on demand | unchanged | old URLs kept, `figma://` retired | drift reports behind |
| engine | per language | unchanged | with consent | verify says unavailable, AI review one level lower |
| CI workflow (`purlin.yml`) | written under `recorded` and `approved`; under `tested` only with `--ci` | written if missing | offered | status says the gate cannot be met |
| dashboard artifact | with `--ci` | unchanged | with `--ci` | local page works |
| upstream check job | `--ci --upstream-check` | unchanged | offered when pins exist | drift covers it |
| OS matrix (`@env`) | rendered from tags | unchanged | re-rendered | status says "needs <os>"; `--update` re-renders |

Acceptance: `dev/test_init_scaffold.py`, `dev/test_init_update.py` (target 800 each) with
the two upgrade fixtures and each gate transition both ways.

## C3. Preloaded decisions (the orchestrator never asks; a lane that meets an unlisted choice picks the simplest option and records it in its DONE note)

| Question a lane might raise | Answer |
|---|---|
| Python floor | 3.9; every `open()` with `encoding='utf-8'` |
| Third-party deps in plugins or scripts | none; stdlib only (the Node plugins use no npm package beyond the test framework) |
| mutmut config location | `[tool.mutmut]` in `pyproject.toml` when it exists, else `setup.cfg` `[mutmut]`; written like the other wiring files |
| Record file name | `<YYYYMMDDTHHMMSSZ>-<commit7>-<runner>[-<os>].json`; runner slug `ci` or the developer's git email local part, lowercased, non-alphanumerics to `-` |
| Retention | newest three per feature per OS plus anything a `validated/*` tag names |
| Default branch | `git symbolic-ref refs/remotes/origin/HEAD`, else `main` |
| Approver slug in approval file names | email local part, lowercased, non-alphanumerics to `-` |
| Timestamp format everywhere | ISO 8601 UTC with `Z` |
| Test strength display | integer percent; `n/a` when no engine |
| Records from forked PRs | never committed; the job says so in the PR comment |
| xUnit detection | any `*.csproj` referencing the `xunit` package |
| Vitest compatibility | `onTestRunEnd` primary, `onFinished` kept for Vitest 1 and 2 |
| The one `@env(windows)` proof this repo keeps | the file-locking check in `static_checks.py` |
| `dev/manual/` CLI checks | hand-run only, never in `run_tests.sh` |
| Commit prefixes before phase 4B rewrites the conventions | the existing `references/commit_conventions.md` prefixes |
| Unknown tags in frozen specs | ignored with one warning until phase 9 |
| Emoji in any output | never |
| Colours in the dashboard | tokens only; a raw hex outside the inlined token block fails acceptance |
| Diagrams in docs | mermaid with the shared init block; no images except dashboard screenshots |
| A design decision not covered by `design/readme.md` | the plainer option; note it in the DONE note for the user |
| A lane finds a Part B claim false | record it under BLOCKED with evidence, skip, continue; never redesign inside the lane |
| Anything else | the simplest option that keeps the vocabulary in A1 and the deletions in C0 intact |


## Inputs

- `scripts/init/scaffold.py` (1127): the file to rebuild; keep its interpreter resolution
  and the way it edits conftest and jest config (the pattern the plan cites), drop the rest.
- `scripts/mcp/purlin/frameworks.py` (detection), `specs.py` (`@env` tags for the matrix),
  `gate.py` (`resolve_gate`, the derived defaults), `status.py` (the next-step line).
- `scripts/run/workflow.py` (lane 2C): `render_workflow(host, env_tags, purlin_ref,
  upstream_check=False)`; `scripts/run/mutation/mutmut.py` (lane 2B):
  `mutmut_config_block(source_paths, test_args, style='toml'|'cfg')`; `templates/purlin.yml` and
  `templates/purlin.azure-pipelines.yml`.
- `scripts/proof/*` (lane 2A) for the plugin copies into `.purlin/plugins/`;
  `scripts/report/purlin-report.html` (lane 6) for the dashboard copy;
  `templates/gitignore.purlin`; `hooks/hooks.json` (the refresh hook stays as it is).
- `scripts/hooks/pre-push.sh` (149) to rewrite; `git show 539b9145:scripts/hooks/pre-push.sh`
  is the same file. `.purlin/hooks/pre-push` in this repository is the tracked shim that
  hands to it (kept until phase 9).
- `templates/config.json` (9) to rewrite in the B2 shape.
- `dev/test_init_scaffold.py` (1471), `dev/test_init_e2e.sh` (1234),
  `dev/test_pre_push_hook.py` (1494): to rewrite. `dev/fixtures/consumer-ci/` (lane 2C).

## Outputs

- `scripts/init/scaffold.py` rebuilt (target 600): `init [--gate tested|recorded|approved]
  [--ci github|ado] [--ci --upstream-check] [--add <language>] [--update] [--dry-run]
  [--project-root DIR] [--yes]`. On a project with code, the one question (what must be
  true before CI lets a change merge) and nothing else; `--gate <level>` answers it
  without asking; an empty tree is asked the language; `approved` is asked the approver
  emails. Then, in order and each named in the summary: language detected, git host read
  from the remote URL, `.purlin/config.json` in the B2 shape (version from the `VERSION`
  file), plugin copies into `.purlin/plugins/` with `.purlin/plugin-root` written (the
  installed plugin's path: `${CLAUDE_PLUGIN_ROOT}` or this checkout), the test-runner
  wiring (conftest, jest or vitest config, the xUnit logger reference, edited the way they
  already are), the engine wiring (`[tool.mutmut]` in `pyproject.toml` or `[mutmut]` in
  `setup.cfg` through `mutmut_config_block`, the Stryker note), the `.gitignore` block from
  the template, `designs/` and `.purlin/records/` with three-line READMEs, the dashboard
  copy `purlin-report.html`, the optional pre-push hook (`.purlin/hooks/pre-push` shim plus
  `.git/hooks/pre-push` delegator, running `purlin_run.py --all --quick`; no pre-commit
  hook), and under `recorded` and `approved` the CI workflow `purlin.yml` (host from the
  remote; `--ci` overrides; the OS matrix from the `@env` tags; the Purlin ref pinned to
  `v<version>`) plus the three GitHub rulesets or the ADO branch security printed as text
  the person applies. Under `tested` no CI is written unless `--ci` is passed.
  `--gate <level>` later raises (adds what is missing, asks before each write) or lowers
  (writes the setting, deletes nothing). `--update` delegates to `scripts/init/update.py`
  (lane 5B; when the module is absent print that it arrives with the upgrade lane).
  `--dry-run` prints every write it would make. Every write consent-gated unless `--yes`.
  Ends with the next step computed from the state.
- `scripts/hooks/pre-push.sh` rewritten (~60): runs `purlin_run.py --all --quick`, never
  blocks unless the config's `pre_push` is `on` and a test failed, prints one line either way.
- `templates/config.json` in the B2 shape with `version` stamped from `VERSION` at init
  time (the template carries the current literal; `dev/bump_version.sh` keeps it).
- `dev/test_init_scaffold.py` (target 800): the one question path for each answer on a
  temp project per language this machine can run (pytest, jest or vitest with a stub
  `package.json`, xunit with a stub `.csproj`, shell, sql), the empty-tree language
  question, the approver question, each gate transition both ways, `--ci` under `tested`,
  the matrix from `@env` tags, `--add`, `--dry-run`, the marketplace path (`CLAUDE_PLUGIN_ROOT`
  pointing at a copied plugin directory) and the `--plugin-dir` path (this checkout), the
  plugin copies byte-equal to `scripts/proof/`, the summary naming every write.
- `dev/test_init_e2e.sh` rewritten (target 400): the C4 item 2 walk on a Python and a
  TypeScript fixture (xUnit too with the dotnet@8 PATH when `dotnet` resolves): init at
  each gate → a hand-written spec and tagged test → `purlin_run.py --quick` →
  `--record --commit` → `scripts/review/approve.py <feature> RULE-N` (read its `--help`; the signed commit needs a throwaway ssh signing key in the temp repo's git config, the way `dev/test_approvals.py` does it) → `scripts/ci/verify_gate.py --check` exits 0 under `tested`;
  exits 1 under `recorded` until a ci-labelled record exists (the git host API mocked by
  committing the record with `github-actions[bot]` as committer through a fake
  `commit_records`); exits 1 under `approved` until a high-risk approval exists whose
  commit is signed by an email on the approver list and the list is present.
- `dev/test_pre_push_hook.py` rewritten (target 300).
- Remove the phase 5 entries you rewrote (`scripts/init/` stays while `update.py` is lane 5B's
  unless yours is the only file left there that hits; `scripts/hooks/`, `scripts/purlin_python.sh`,
  `templates/config.json`, `dev/test_init_scaffold.py`, `dev/test_init_e2e.sh`,
  `dev/test_pre_push_hook.py`) from `PENDING_REWRITE` in `dev/test_vocabulary.py`, keeping an
  entry only if the test names a real hit.
- Add `dev/test_brief.py` and `dev/test_approvals.py` to `PYTEST_FILES` in `dev/run_tests.sh`
  (lane 3A wrote them; nothing runs them yet).

## Acceptance

```bash
cd /Users/richlabarca/LocalCode/purlin-wt/5A
export PATH=/Users/richlabarca/LocalCode/purlin/.venv/bin:/opt/homebrew/opt/dotnet@8/bin:$PATH
python3 -m pytest dev/test_init_scaffold.py dev/test_pre_push_hook.py dev/test_vocabulary.py -q -p no:cacheprovider 2>&1 | tail -3
bash dev/test_init_e2e.sh >/dev/null 2>&1 && echo "init e2e ok" || echo "init e2e FAILED"
test "$(wc -l < scripts/init/scaffold.py | tr -d ' ')" -le 700 || echo "SCAFFOLD TOO LONG: $(wc -l < scripts/init/scaffold.py)"
python3 scripts/init/scaffold.py --dry-run --gate recorded --yes --project-root "$(mktemp -d)" 2>&1 | tail -12
grep -rn -i -w 'audit\|gauge\|receipt\|platform\|mode\|@on\|CODEOWNERS\|approver rule\|forge\|Pages' scripts/init/scaffold.py scripts/hooks/pre-push.sh templates/config.json dev/test_init_scaffold.py dev/test_init_e2e.sh dev/test_pre_push_hook.py | grep -v 'sys\.platform' | sed 's/^/RETIRED WORD: /'
git clean -f specs/ >/dev/null; git status --short
```

The pytest line must show 0 failed; `init e2e ok`; the dry run on an empty directory
prints the language question's default and every write it would make, then the next step;
no `TOO LONG` or `RETIRED WORD` line.

## Rules that bind this lane

- Worktree `/Users/richlabarca/LocalCode/purlin-wt/5A`, branch `lane/5A` from `evidence-workflow`.
- `specs/` untouched; this repository's own `.purlin/config.json` and hooks stay as they are
  until phase 9 (never run init against this checkout outside `--dry-run`).
- Never recreate `scripts/hooks/pre-commit.sh`, `scripts/hooks/pre_push_gate.py`,
  `scripts/update/migrate.py`, `.github/workflows/verify-gate.yml`.
- Do not create `scripts/init/update.py` (lane 5B, at the same time); do not edit
  `dev/test_init_update.py`.
- No retired vocabulary (`sys.platform` excepted); no emoji; stdlib only; Python 3.9;
  everything init writes into a project must work whether the plugin is this checkout or
  the marketplace copy, and must never point a project at `dev/`.
- Commit prefixes `feat(init):`, `feat(pre_push_hook):`, `test(...)`, `chore:`; the two
  attribution lines; `PURLIN_SKIP_DIGEST=1`.
- End with the DONE note.
