# Feature: skill_spec_from_code

> Scope: skills/spec-from-code/SKILL.md
> Stack: markdown (skill definition)
> Description: The `purlin:spec-from-code` skill scans codebases and migrates existing specs in any format to the current compliant 3-section format.

## Rules

- RULE-1: Skill file has YAML frontmatter with `name` and `description` fields
- RULE-2: Skill file contains a `## Usage` section documenting command syntax
- RULE-3: The `name` field in frontmatter is `spec-from-code`, matching the directory name
- RULE-4: Skill includes mandatory tier tag review for proof descriptions
- RULE-5: `skills/spec-from-code/SKILL.md` Phase 1 step 3 instructs the agent to look for migration candidates in two locations: a legacy `features/` directory, read recursively with the `.impl.md` and `.discoveries.md` companions excluded, and non-compliant specs reached by globbing `specs/**/*.md`; candidates are saved to `.purlin/cache/sfc_existing.md` and migrated to `specs/<category>/<name>.md`
- RULE-6: `skills/spec-from-code/SKILL.md` Phase 1 step 3b lists the non-compliance criteria for a spec already in `specs/`: missing `## Rules` section, rules not numbered in `RULE-N:` format, missing `## Proof` section, missing `> Description:` metadata, or an outdated Given/When/Then format; Phase 3 step 3 orders each of them fixed
- RULE-7: `skills/spec-from-code/SKILL.md` Phase 1 step 3b states that compliant specs (numbered rules, proofs, proper sections) are left untouched and are not migration candidates, so they enter neither the candidate ledger nor the migration pass
- RULE-8: `skills/spec-from-code/SKILL.md` Phase 3 step 3 instructs the agent to treat the old spec as the primary input, preserving the author's intent, rules and descriptions with minimal loss, keeping existing `> Scope:`, `> Stack:` and `> Requires:` metadata, and flagging any divergence from the current code
- RULE-9: `skills/spec-from-code/SKILL.md` Phase 4 step 2 instructs the agent to ask before removing `features/` and states that non-compliant specs in `specs/` are overwritten in place, needing no cleanup
- RULE-10: `skills/spec-from-code/SKILL.md` Phase 3 step 6's feature-spec template numbers rules sequentially from `- RULE-1:`, so a spec generated from a plain description carries RULE-N lines starting at RULE-1 with no gaps
- RULE-11: `skills/spec-from-code/SKILL.md` Phase 3 step 6's feature-spec template pairs every rule with a proof (`- PROOF-1 (RULE-1):`, `- PROOF-2 (RULE-2):`), and step 11 requires at least one `PROOF-N (RULE-N):` line in every generated spec
- RULE-12: `skills/spec-from-code/SKILL.md` Guidelines forbid the `(assumed)` tag on generated rules, because rules extracted from code are observed behavior rather than assumptions, so a spec generated from explicit values carries no `(assumed)` tag
- RULE-13: A spec generated from a PRD extracts every testable constraint as a RULE-N line — at least 5 rules for a multi-requirement PRD
- RULE-14: A spec generated from a PRD contains valid metadata: `> Description:`, `> Scope:`, and `> Stack:` fields are present
- RULE-15: A spec generated from a PRD includes `> Requires:` referencing anchors whose scope overlaps with the feature's scope
- RULE-16: A spec generated from a vague description adds `(assumed — <context>)` tags to rules where the agent inferred specific values
- RULE-17: Rules with (assumed) tags still follow RULE-N format and are parseable by sync_status
- RULE-18: A spec generated from customer feedback translates complaints into testable RULE-N constraints with specific thresholds or behaviors
- RULE-19: Every proof line across all four scenarios ends with an appropriate tier tag (@integration, @e2e, @manual, or no tag for unit)
- RULE-20: sync_status successfully parses specs from all four scenarios without errors, reporting correct rule counts and UNTESTED status
- RULE-21: The `## Rules` and `## Proof` sections are both present in specs from all four scenarios
- RULE-22: When a vague-input spec's (assumed) rule is updated with an explicit value, the (assumed) tag is removed and the rule remains valid
- RULE-23: Skill contains data contract extraction mandatory for ALL features (not just UI) with five categories: inbound contracts, outbound contracts, transformation rules, state transitions, and access contracts
- RULE-24: Skill contains a mandatory draft-and-evaluate step that applies rebuild/behavior/overlap tests to every candidate rule, and a rebuild-risk filter that verifies contract coverage across all five categories before presenting specs
- RULE-25: `skills/spec-from-code/SKILL.md` Phase 3 step 3 instructs the agent to read the `.impl.md` companion in full and turn each PM-ACCEPTED row of its Active Deviations table into a rule reflecting the implementation's actual behavior
- RULE-26: `skills/spec-from-code/SKILL.md` Phase 3 step 3 instructs the agent to read the `.discoveries.md` companion in full, turning resolved `[BUG]` entries into regression rules and open bugs into rules tagged `(deferred)`
- RULE-27: `skills/spec-from-code/SKILL.md` Phase 3 step 3 instructs the agent to preserve `.discoveries.md` Figma and design references as `> Visual-Reference:` metadata or `@manual` proof references
- RULE-28: Quality guide references coverage dimensions instead of a fixed rule count target
- RULE-29: The taxonomy phase never produces a category folder containing a single spec — single-feature categories are merged into a related category during taxonomy review, or the spec is placed directly at `specs/<name>.md` when no category fits
- RULE-30: The tier review pass (Phase 3 step 7) includes an inverse check: every `@e2e` proof description must read as an observable flow (arrange → act → observe through the real running app) and must not name a source file or internal function; mis-tagged proofs are rewritten as boundary observations or retagged, per `references/spec_quality_guide.md` ("E2E proof descriptions")
- RULE-31: Spec validation (Phase 3 step 11) includes a proof implementation-coupling check that rejects proof descriptions naming source files or internal symbols as the asserted target
- RULE-32: When a category's generated proofs include `@e2e` and no e2e-capable test runner was detected in Phase 1, the skill surfaces a warning in the category review block and in the Phase 4 summary (tool-agnostic — Playwright, Cypress, MCP-driven browser, etc.)

## Proof

- PROOF-1 (RULE-1): Grep `skills/spec-from-code/SKILL.md` for YAML frontmatter delimiters (`---`); verify `name:` and `description:` fields exist
- PROOF-2 (RULE-2): Grep `skills/spec-from-code/SKILL.md` for `## Usage`; verify the section exists
- PROOF-3 (RULE-3): Extract `name:` from frontmatter; verify it equals `spec-from-code`
- PROOF-4 (RULE-4): Grep `skills/spec-from-code/SKILL.md` for tier review instructions and tier tag references (`@integration`/`@e2e`/unit tier); verify present
- PROOF-5 (RULE-5): Grep `skills/spec-from-code/SKILL.md` Phase 1 step 3 for the two candidate locations it names: the heading "a) Legacy `features/` directory:" and the glob "Glob `specs/**/*.md` and read each file"
- PROOF-6 (RULE-6): Grep `skills/spec-from-code/SKILL.md` Phase 1 step 3b for the five non-compliance criteria verbatim: "Missing `## Rules` section", "Rules are not numbered (`RULE-N:` format)", "Missing `## Proof` section", "Missing `> Description:` metadata" and "Uses an outdated format"
- PROOF-7 (RULE-7): Grep `skills/spec-from-code/SKILL.md` Phase 1 step 3b for the exclusion sentence: "(with numbered rules, proofs, and proper sections) are left untouched" and "they are not migration candidates"
- PROOF-8 (RULE-8): Grep `skills/spec-from-code/SKILL.md` Phase 3 step 3 for the fidelity clause: "primary input" and "preserve the author's intent, rules, and descriptions with minimal loss"
- PROOF-9 (RULE-9): Grep `skills/spec-from-code/SKILL.md` Phase 4 step 2 for both cleanup paths: the question "Remove old features/ directory?" and the sentence "are overwritten in place"
- PROOF-10 (RULE-5): Grep `skills/spec-from-code/SKILL.md` Phase 1 step 3a for the legacy read it orders: "Read all `.md` files recursively (excluding `.impl.md` and `.discoveries.md`" and the extraction item "scenarios (Given/When/Then blocks)"
- PROOF-11 (RULE-6): Grep `skills/spec-from-code/SKILL.md` Phase 1 step 3b for the outdated-format criterion "Uses an outdated format (e.g., Given/When/Then scenarios instead of Rules/Proof)" and for what is salvaged from such a spec: "existing rules (even if unnumbered), existing proofs, description"
- PROOF-12 (RULE-6): Grep `skills/spec-from-code/SKILL.md` Phase 3 step 3 for the remedy of the missing-metadata criterion: "Fix compliance issues: add missing `> Description:`"
- PROOF-13 (RULE-6): Grep `skills/spec-from-code/SKILL.md` Phase 3 step 3 for the remedy of the other three criteria: "number unnumbered rules, add missing `## Proof` section, convert any Given/When/Then scenarios to Rules/Proof format"
- PROOF-14 (RULE-7): Grep `skills/spec-from-code/SKILL.md` Phase 1 step 3 for the candidate ledger a compliant spec stays out of: "Save all migration candidates to `.purlin/cache/sfc_existing.md`" and the summary line "Found N specs to migrate: X from features/, Y non-compliant in specs/."
- PROOF-15 (RULE-7): Grep `skills/spec-from-code/SKILL.md` Phase 3 step 3 for the gate that reads that ledger before a spec is rewritten: "check if this feature has a migration candidate in `.purlin/cache/sfc_existing.md`" and "If no migration candidate exists, generate from code alone"
- PROOF-16 (RULE-8): Grep `skills/spec-from-code/SKILL.md` Phase 3 step 3 for the metadata-preservation clause: "Preserve all content that is already correct: existing rules (renumber if needed), existing proofs, existing metadata (`> Scope:`, `> Stack:`, `> Requires:`)"
- PROOF-17 (RULE-5): Grep `skills/spec-from-code/SKILL.md` for the two path literals of the migration mapping: Phase 3 step 3's "Read the original `features/<category>/<name>.md` file in full" and the destination path "specs/<category>/<name>.md" in Phase 3 step 6
- PROOF-18 (RULE-8): Grep `skills/spec-from-code/SKILL.md` Phase 3 step 3 for the two clauses that keep a migration reviewable: "If the code has diverged, flag the discrepancy for the user in the review step" and the header marker "<!-- Migrated by purlin:spec-from-code. Review and refine. -->"
- PROOF-19 (RULE-10): Grep `skills/spec-from-code/SKILL.md` Phase 3 step 6 for the feature-spec template's rule numbering: the adjacent lines "- RULE-1: <Behavioral constraint extracted from code>" and "- RULE-2: <Another constraint>", in that order
- PROOF-20 (RULE-11): Grep `skills/spec-from-code/SKILL.md` Phase 3 step 6 for the template's rule-to-proof pairing, the adjacent lines "- PROOF-1 (RULE-1): <Observable assertion>" and "- PROOF-2 (RULE-2): <Observable assertion>", plus step 11's readback requirement "`## Proof` contains at least one `PROOF-N (RULE-N):` line"
- PROOF-21 (RULE-12): Grep `skills/spec-from-code/SKILL.md` Guidelines for the prohibition and its reason: "**Do not use the `(assumed)` tag.**" and "Rules extracted from code are observed behavior, not assumptions"
- PROOF-22 (RULE-13): e2e: Run sync_status over the PRD scenario spec, written from a PRD naming 6 separate requirements: the own-rule lines it prints are exactly RULE-1 to RULE-6, clearing RULE-13's floor of 5 rules for a multi-requirement PRD @e2e
- PROOF-23 (RULE-14): e2e: Run sync_status over the PRD scenario spec and read the dashboard payload: `description` and `stack` come back verbatim from the spec ("python/flask, stripe, redis" for the `checkout` scenario, "python/django, stripe, postgres" for `checkout_flow`); with the `> Description:` line deleted `description` is null, with `> Stack:` deleted `stack` is null, and the `> Scope:` line is load-bearing too, the feature's directive reading "→ Run: purlin:test" when a scope file is on disk and "→ Run: purlin:build" when none is @e2e
- PROOF-24 (RULE-15): e2e: Run sync_status over the PRD scenario spec beside the anchor its `> Requires:` names: the feature reads "0/8 rules proved" and prints one "NO PROOF (required)" line per anchor rule (`api_conventions/RULE-1` and `api_conventions/RULE-2`, or `api_rest_conventions/RULE-1` and `/RULE-2`); with the `> Requires:` line deleted the same spec reads "0/6 rules proved" and prints no anchor line @e2e
- PROOF-25 (RULE-16): e2e: The vague-input scenario spec carries 2 rules tagged `(assumed — user said "<wording>")`, and sync_status prints "⚠ 2 rules have (assumed) values — PM should confirm" for it; with both tags rewritten as a bare "(assumed)" with no context, the same run prints no "(assumed) values" line, so only the context-carrying form is counted @e2e
- PROOF-26 (RULE-17): e2e: Run sync_status over the vague-input scenario spec, 2 of whose rules carry an `(assumed — user said "...")` tag: every rule id is still printed and counted ("0/5 rules proved" with RULE-1 to RULE-5 for the `search` scenario, "0/4" with RULE-1 to RULE-4 for `password_reset_vague`), each with its own planned-proof line, and the block appends "⚠ 2 rules have (assumed) values — PM should confirm" @e2e
- PROOF-27 (RULE-18): e2e: Pair the scenario's quoted customer complaints with the generated spec and run sync_status: it reports 4 rules for the feature, the slowness complaint is answered by RULE-1 with a numeric latency bound ("500ms" in the search scenario, "2 seconds" in the dashboard scenario), and each remaining complaint maps to exactly one rule carrying its own literal ("fuzzy matching", "25 items per page"), so no complaint is left as prose @e2e
- PROOF-28 (RULE-19): e2e: Run sync_status over all four scenario specs: every planned-proof line it prints ends with @integration, @e2e, @manual or no tag at all, and the run emits no proof tag WARNING; insert "@e2e" before the trailing "@integration" on PROOF-1 of the plain scenario and re-run: it prints "WARNING: PROOF-1: a second tier tag @e2e precedes @integration"; SKILL.md step 7 routes a proof needing "a browser or full app stack" to `@e2e` and one that shells out to git or an external service to `@integration` @e2e
- PROOF-29 (RULE-20): e2e: Run sync_status over one project holding all four scenario specs: no ERROR is printed, every feature's Status reads UNTESTED, and each rule count matches its spec (the plain-description and customer-feedback scenarios "0/4", the vague scenario "0/5" or "0/4", the PRD scenario "0/6", or "0/8" where the anchor it requires adds 2 rules) @e2e
- PROOF-30 (RULE-21): e2e: Run sync_status over the four scenario specs: each prints its rule count and one planned-proof line per proof; the same plain-description spec with its `## Rules` section deleted instead reads "no rules defined" under "WARNING: No ## Rules section found.", and with its `## Proof` section deleted still counts its 4 rules but prints no planned-proof line, falling back to the placeholder directive `"PROOF-N", "RULE-1"` @e2e
- PROOF-31 (RULE-22): e2e: Rewrite the vague-input scenario spec's tagged rule as the explicit "- RULE-4: Search returns in under 200ms" and run sync_status: the rule count is unchanged, RULE-4 is still printed with its planned proof, and the assumed advisory falls from "⚠ 2 rules have (assumed) values" to "⚠ 1 rule has (assumed) values" when one tag goes, and disappears when both do @e2e
- PROOF-32 (RULE-23): Grep SKILL.md for "Inbound contracts", "Outbound contracts", "Transformation rules", "State transitions", "Access contracts" as subsections of step 4; verify all five exist and step is mandatory for ALL features
- PROOF-33 (RULE-23): e2e: Create a simulated component with API field consumption, filter/sort transformations, conditional gates by user segment, and missing-data fallbacks; verify the extraction identifies inbound fields, transformations, access gates, and failure modes @e2e
- PROOF-34 (RULE-24): Grep SKILL.md for "Draft and evaluate" AND "rebuild test" in Phase 3; verify the step applies rebuild, behavior, and overlap tests. Grep for "Verify contract coverage" in the rebuild-risk filter; verify it checks all five contract categories
- PROOF-35 (RULE-25): Grep SKILL.md for ".impl.md" AND "Active Deviations" AND "PM-ACCEPTED"; verify the skill reads deviations and converts PM-accepted deviations to rules reflecting actual behavior
- PROOF-36 (RULE-25): e2e: Create a features/ directory with a spec and .impl.md containing a PM-ACCEPTED deviation; verify the deviation's actual behavior becomes a RULE-N in the migrated spec @e2e
- PROOF-37 (RULE-26): Grep SKILL.md for ".discoveries.md" AND "Resolved bugs" AND "(deferred)"; verify resolved bugs become rules and open bugs become deferred rules
- PROOF-38 (RULE-26): e2e: Create features/ with .discoveries.md containing one RESOLVED bug and one OPEN bug; verify the resolved bug becomes a RULE-N and the open bug becomes a RULE-N with (deferred) tag @e2e
- PROOF-39 (RULE-27): Grep SKILL.md for "Visual-Reference" AND "Figma" in the .discoveries.md migration section; verify design references are preserved as metadata or manual proof references
- PROOF-40 (RULE-28): Grep spec_quality_guide.md for "Coverage dimensions"; verify section exists. Grep for "5–10 rules per feature"; verify the fixed target no longer exists
- PROOF-41 (RULE-29): Grep `skills/spec-from-code/SKILL.md` for the Phase 2 single-feature category check; verify it instructs merging single-feature categories into a related category or placing the spec at `specs/<name>.md` without a folder
- PROOF-42 (RULE-30): Grep `skills/spec-from-code/SKILL.md` step 7 for the inverse check: "observable flow", arrange → act → observe, and "must not name a source file or internal function"; verify the pointer to `spec_quality_guide.md` "E2E proof descriptions" is present
- PROOF-43 (RULE-31): Grep `skills/spec-from-code/SKILL.md` step 11 for the proof implementation-coupling check; verify it rejects proof descriptions naming source files or internal symbols as the asserted target
- PROOF-44 (RULE-32): Grep `skills/spec-from-code/SKILL.md` for the e2e-runner warning in BOTH the step 12 review block and the Phase 4 summary; verify both locations mention `@e2e` proofs with no e2e runner detected
- PROOF-45 (RULE-30): Grep `references/spec_quality_guide.md` for the "E2E proof descriptions" section; verify the canonical guidance the skill points to exists
