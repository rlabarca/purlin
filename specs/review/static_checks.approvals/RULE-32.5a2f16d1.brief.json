{
  "ai_review": null,
  "design": null,
  "design_hash": null,
  "feature": "static_checks",
  "generated_at": "2026-09-15T19:39:21Z",
  "layers": [
    "proof text",
    "test body"
  ],
  "min_strength": 80,
  "origin": "eng",
  "proof_hash": "af0a94a405b40d6363ce853c6ca57248859762f6f654e2027c5285538aeefa56",
  "proofs": [
    {
      "env": null,
      "findings": [
        "happy_path_only"
      ],
      "id": "PROOF-55",
      "text": "Parse the command's own source; verify every `--flag` it compares against the argument list appears in the usage text, that the usage advertises no other flag, and that the docstring cites the exit convention",
      "tier": "unit"
    }
  ],
  "reasons": [
    "PROOF-55 tautology"
  ],
  "record": ".purlin/records/static_checks/20260915T193752Z-04c92e2-ci-windows.json",
  "risk": "low",
  "rule": "RULE-32",
  "rule_hash": "5c6d294cee2252174352dce77cd117b09ae118fc539b1b111ce72ed094e7e090",
  "rule_text": "Every `--flag` the command dispatches on appears in its usage text, the usage advertises no flag the command never dispatches on, and the module docstring states the exit convention rather than contradicting it",
  "schema": "purlin-brief/1",
  "state": "Stale",
  "test_hash": "afb587e296278f15d054d93023cf0e47cddf2d547795e8eea0a83d70ec6243ad",
  "test_hash_kind": "file",
  "test_strength": null,
  "tests": [
    {
      "body": "def test_usage_matches_dispatch_chain(self):\n        src = open(_STATIC_CHECKS_PY, encoding='utf-8').read()\n        tree = ast.parse(src)\n\n        main_fn = next(\n            (n for n in tree.body\n             if isinstance(n, ast.FunctionDef) and n.name == 'main'), None)\n        assert main_fn is not None, \"static_checks.py has no main()\"\n\n        # Every string literal tested against sys.argv is a dispatch flag.\n        dispatched = set()\n        for node in ast.walk(main_fn):\n            if isinstance(node, ast.Compare) and isinstance(node.ops[0], ast.In):\n                left, right = node.left, node.comparators[0]\n                is_argv = (\n                    isinstance(right, ast.Attribute) and right.attr == 'argv'\n                ) or (\n                    isinstance(right, ast.Name) and right.id == 'argv'\n                )\n                if is_argv and isinstance(left, ast.Constant) \\\n                        and isinstance(left.value, str) and left.value.startswith('--'):\n                    dispatched.add(left.value)\n        assert dispatched, \"found no --flag dispatches in main(); detector is broken\"\n\n        usage_text = ' '.join(static_checks._USAGE)\n        undocumented = sorted(f for f in dispatched if f not in usage_text)\n        assert not undocumented, \\\n            f\"main() dispatches on flags absent from _USAGE: {undocumented}\"\n\n        # And _USAGE must not advertise a flag that does not exist.\n        advertised = set(re.findall(r'--[a-z][a-z-]+', usage_text))\n        # Sub-options are documented on the line of the form they belong to.\n        sub_options = {'--project-root'}\n        phantom = sorted(f for f in advertised - sub_options if f not in dispatched)\n        assert not phantom, f\"_USAGE advertises flags main() never dispatches on: {phantom}\"\n\n        # RULE-7: the docstring must not promise a non-zero exit for a found defect.\n        docstring = ast.get_docstring(tree) or ''\n        assert 'Exit code 0 = all proofs passed, 1 = at least one failed' not in docstring, \\\n            \"module docstring still contradicts RULE-7's exit convention\"\n        assert 'RULE-7' in docstring, \\\n            \"module docstring should cite the exit convention it follows\"",
      "file": "dev/test_static_checks.py",
      "findings": [
        "tautology"
      ],
      "name": "test_usage_matches_dispatch_chain",
      "proof": "PROOF-55",
      "reasons": [
        "tautological assertion (assert True or equivalent)"
      ]
    }
  ],
  "triple_hash": "5a2f16d104a50092dfddf02f0491c2c8ea108a762464c78e53c3aa9699b82901",
  "verdict": "needs a human"
}
