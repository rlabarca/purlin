{
  "ai_review": "not available",
  "design": null,
  "design_hash": null,
  "feature": "update",
  "generated_at": "2026-09-15T18:59:11Z",
  "layers": [
    "proof text",
    "test body",
    "test strength",
    "model review"
  ],
  "min_strength": 80,
  "origin": "eng",
  "proof_hash": "b94ad6f72be3c42c1b4a1bc266317487b4f428d6ce8bb4086858c9da0d5e497a",
  "proofs": [
    {
      "env": null,
      "findings": [
        "happy_path_only"
      ],
      "id": "PROOF-18",
      "text": "Run the update with `--yes` on each fixture and verify `git log --format=%s` holds exactly two lines, the newer one starting `chore(update): migrate to <VERSION> (` and naming every id applied; verify every remaining line of `git status --porcelain` is an untracked `.bak` file. Decline everything on the v0.9.5 fixture and verify `HEAD` is where it was",
      "tier": "unit"
    }
  ],
  "reasons": [
    "PROOF-18 logic_mirroring",
    "PROOF-18 logic_mirroring",
    "PROOF-18 logic_mirroring",
    "PROOF-18 logic_mirroring",
    "PROOF-18 logic_mirroring"
  ],
  "record": ".purlin/records/update/20260915T185743Z-72c4f1f-ci-windows.json",
  "risk": "high",
  "rule": "RULE-18",
  "rule_hash": "36126c433f3f5779054ff44742cf711b2cdc7e82c3937ab5f7859de361ee6dc8",
  "rule_text": "One commit carries the whole run, its subject naming the release and every migration id applied; a run that applies nothing writes no commit; nothing is left uncommitted afterwards but the backup copies",
  "schema": "purlin-brief/1",
  "state": "Approved",
  "test_hash": "21fcc40d20a6ba44a76c3672b67e2075f44ca2f6bfd29efd56ea0b39f9ad21c1",
  "test_hash_kind": "file",
  "test_strength": null,
  "tests": [
    {
      "body": "def test_one_commit_carries_every_migration_id(tmp_path, layout):\n    root = _project(tmp_path, layout)\n    applied = _ids(root)\n    _apply(root)\n    log = _git(root, 'log', '--format=%s').stdout.splitlines()\n    assert len(log) == 2, log\n    subject = log[0]\n    assert subject.startswith('chore(update): migrate to %s (' % VERSION)\n    for migration_id in applied:\n        assert migration_id in subject",
      "file": "dev/test_init_update.py",
      "findings": [
        "logic_mirroring"
      ],
      "name": "test_a_run_that_applies_nothing_writes_no_commit",
      "proof": "PROOF-18",
      "reasons": [
        "expected value computed by same function as SUT"
      ]
    },
    {
      "body": "def test_one_commit_carries_every_migration_id(tmp_path, layout):\n    root = _project(tmp_path, layout)\n    applied = _ids(root)\n    _apply(root)\n    log = _git(root, 'log', '--format=%s').stdout.splitlines()\n    assert len(log) == 2, log\n    subject = log[0]\n    assert subject.startswith('chore(update): migrate to %s (' % VERSION)\n    for migration_id in applied:\n        assert migration_id in subject",
      "file": "dev/test_init_update.py",
      "findings": [
        "logic_mirroring"
      ],
      "name": "test_nothing_is_left_uncommitted_but_the_backups[upgrade-0.10-dev]",
      "proof": "PROOF-18",
      "reasons": [
        "expected value computed by same function as SUT"
      ]
    },
    {
      "body": "def test_one_commit_carries_every_migration_id(tmp_path, layout):\n    root = _project(tmp_path, layout)\n    applied = _ids(root)\n    _apply(root)\n    log = _git(root, 'log', '--format=%s').stdout.splitlines()\n    assert len(log) == 2, log\n    subject = log[0]\n    assert subject.startswith('chore(update): migrate to %s (' % VERSION)\n    for migration_id in applied:\n        assert migration_id in subject",
      "file": "dev/test_init_update.py",
      "findings": [
        "logic_mirroring"
      ],
      "name": "test_nothing_is_left_uncommitted_but_the_backups[upgrade-0.9.5]",
      "proof": "PROOF-18",
      "reasons": [
        "expected value computed by same function as SUT"
      ]
    },
    {
      "body": "def test_one_commit_carries_every_migration_id(tmp_path, layout):\n    root = _project(tmp_path, layout)\n    applied = _ids(root)\n    _apply(root)\n    log = _git(root, 'log', '--format=%s').stdout.splitlines()\n    assert len(log) == 2, log\n    subject = log[0]\n    assert subject.startswith('chore(update): migrate to %s (' % VERSION)\n    for migration_id in applied:\n        assert migration_id in subject",
      "file": "dev/test_init_update.py",
      "findings": [
        "logic_mirroring"
      ],
      "name": "test_one_commit_carries_every_migration_id[upgrade-0.10-dev]",
      "proof": "PROOF-18",
      "reasons": [
        "expected value computed by same function as SUT"
      ]
    },
    {
      "body": "def test_one_commit_carries_every_migration_id(tmp_path, layout):\n    root = _project(tmp_path, layout)\n    applied = _ids(root)\n    _apply(root)\n    log = _git(root, 'log', '--format=%s').stdout.splitlines()\n    assert len(log) == 2, log\n    subject = log[0]\n    assert subject.startswith('chore(update): migrate to %s (' % VERSION)\n    for migration_id in applied:\n        assert migration_id in subject",
      "file": "dev/test_init_update.py",
      "findings": [
        "logic_mirroring"
      ],
      "name": "test_one_commit_carries_every_migration_id[upgrade-0.9.5]",
      "proof": "PROOF-18",
      "reasons": [
        "expected value computed by same function as SUT"
      ]
    }
  ],
  "triple_hash": "10e7c56507694b4a4abf4f4c1158f5ed50a75b519a5530cd413e3c4f65968d29",
  "verdict": "needs a human"
}
