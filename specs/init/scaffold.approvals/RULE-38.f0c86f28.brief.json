{
  "ai_review": null,
  "design": null,
  "design_hash": null,
  "feature": "scaffold",
  "generated_at": "2026-09-15T19:31:54Z",
  "layers": [
    "proof text",
    "test body",
    "test strength"
  ],
  "min_strength": 80,
  "origin": "eng",
  "proof_hash": "a171dd55d04a2e39c3afe28a661a8e2530e481ccc486d88c50f0b7f94fd2f123",
  "proofs": [
    {
      "env": null,
      "findings": [],
      "id": "PROOF-38",
      "text": "Run init on a pytest project, write `pytest,jest,shell,vitest` into its `test_framework`, run `--gate recorded` and verify the value reads `pytest,shell`, that the output holds `dropped jest from test_framework: nothing in the tree runs it` and the same line for `vitest`, and that neither `jest.config.js` nor `vitest.config.ts` exists. Write a `package.json` into a second pytest project, record `pytest,jest,shell`, run `--gate recorded` and verify the value is unchanged and `dropped` is absent",
      "tier": "unit"
    }
  ],
  "reasons": [],
  "record": ".purlin/records/scaffold/20260915T193117Z-91d34e0-ci-linux.json",
  "risk": "medium",
  "rule": "RULE-38",
  "rule_hash": "f50e351779adfb0da3c8fa59bab04d14c5ca9e81bd45eda0a29a83fd1b2faebd",
  "rule_text": "A recorded `test_framework` name that detection does not find and the tree carries no wiring for is dropped and the drop is printed, so no runner is wired for a language the project does not have; a name the tree does carry is kept",
  "schema": "purlin-brief/1",
  "state": "Reviewed",
  "test_hash": "62fe40aa6afec8ea7d46e54c663e16a31dc6818d49c4cb230088f49fdfee451c",
  "test_hash_kind": "file",
  "test_strength": null,
  "tests": [
    {
      "body": "def test_a_gate_change_drops_a_framework_the_tree_cannot_run(self, project):\n        \"\"\"An older release recorded every plugin it shipped, runnable or not.\"\"\"\n        project.run('--gate', 'tested')\n        recorded = project.config()\n        recorded['test_framework'] = 'pytest,jest,shell,vitest'\n        write(project.path('.purlin/config.json'),\n              json.dumps(recorded, indent=2))\n        output = project.run('--gate', 'recorded')\n        assert project.config()['test_framework'] == 'pytest,shell'\n        for name in ('jest', 'vitest'):\n            assert ('dropped %s from test_framework: nothing in the tree '\n                    'runs it' % name) in output\n        assert not project.has('jest.config.js')\n        assert not project.has('vitest.config.ts')",
      "file": "dev/test_init_scaffold.py",
      "findings": [],
      "name": "test_a_framework_the_tree_carries_survives_a_gate_change",
      "proof": "PROOF-38",
      "reasons": []
    },
    {
      "body": "def test_a_gate_change_drops_a_framework_the_tree_cannot_run(self, project):\n        \"\"\"An older release recorded every plugin it shipped, runnable or not.\"\"\"\n        project.run('--gate', 'tested')\n        recorded = project.config()\n        recorded['test_framework'] = 'pytest,jest,shell,vitest'\n        write(project.path('.purlin/config.json'),\n              json.dumps(recorded, indent=2))\n        output = project.run('--gate', 'recorded')\n        assert project.config()['test_framework'] == 'pytest,shell'\n        for name in ('jest', 'vitest'):\n            assert ('dropped %s from test_framework: nothing in the tree '\n                    'runs it' % name) in output\n        assert not project.has('jest.config.js')\n        assert not project.has('vitest.config.ts')",
      "file": "dev/test_init_scaffold.py",
      "findings": [],
      "name": "test_a_gate_change_drops_a_framework_the_tree_cannot_run",
      "proof": "PROOF-38",
      "reasons": []
    }
  ],
  "triple_hash": "f0c86f2825513054b059741198b36aac2ae9f7e5da9202bb058fe1d8e67e59fb",
  "verdict": "ready"
}
