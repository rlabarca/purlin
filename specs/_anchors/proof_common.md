# Anchor: proof_common

> Type: schema
> Scope: scripts/proof/
> Description: The behavior every Purlin proof plugin implements regardless of language:
>   spec-directory resolution, proof-file naming (agnostic and platform-scoped), fallback,
>   the write-scoped overwrite keyed by (feature, tier, platform, test_file), orphan reaping,
>   the 7 required fields plus the scoped file's `platform`, pass/fail status, the no-marker
>   no-op, glob-based discovery, the fallback stderr warning, purge-on-rerun, the kept entry
>   of a skipped test, the run marker written or merged at the end of a run, forward-slash
>   test paths, and the one-plugin-per-framework rule. Each per-language proof plugin spec
>   (proof_plugins_pytest, proof_plugins_jest, proof_plugins_shell, proof_plugins_c,
>   proof_plugins_php, proof_plugins_sql, proof_plugins_vitest, proof_plugins_xunit)
>   requires this anchor and adds only its framework-specific rules.

## What it does

Defines the cross-cutting contract shared by all proof collection plugins. A plugin spec
that declares `> Requires: proof_common` inherits these rules, so per-language specs only
restate the marker syntax and status mapping unique to their framework. Keeping the shared
behavior in one place means a change to the proof-file contract is made and proved once,
not copied across eight specs.

The merge key is `(feature, tier, platform, test_file)`. Two test files can cover the same
feature at the same tier and be run in any order, in separate processes, without destroying
each other's entries. That is what lets a platform-scoped proof be proven on a remote runner
and a large suite be split across files. The cost of the narrower key is that a run only
reaps what it can see: RULE-11 reaps entries whose test file is gone, RULE-12 states the
bounded case that survives, and RULE-18 holds the entry of a test the run skipped, which is
the one case where a file counts as executed and one of its tests did not.

A proof the spec tags `@on(...)` must be proved on each platform it names. The marker in the
test declares those platforms in the plugin's own syntax, and that declaration alone decides
whether the result is scoped: a scoped entry goes to `<feature>.proofs-<tier>@<platform>.json`
and carries `platform`, an unscoped entry goes to the agnostic file untouched. The id in the
filename is the host's: `PURLIN_PLATFORM` when a runner sets it, the detected OS family
otherwise. Plugins never compare versions, because the registry in `.purlin/config.json` and
`sync_status` own that judgement (RULE-15, RULE-16, RULE-17). Every plugin is the same file
on a developer machine and on a runner, and `test_file` is written with `/` on every OS, so
the merge key and, later, the verification hash are the same for one test wherever it ran.

A run leaves one more record beside the proof files: the run marker
`.purlin/runtime/test_run.json` (RULE-19). A consumer project has no
`dev/run_tests.sh`, so without it every receipt issued there records
`evidence.test_run: null` and says only that some file on disk holds a `pass`.
Each plugin writing the marker as it finishes is what makes the receipt name a
run; the merge rule is what lets several plugins in one project, and several
runs of one plugin, add up to a single record of the commit they all ran at.

A proof file is only evidence while something regenerates it. RULE-14 closes that gap for this
repository's own proofs: a committed entry whose test file no sweep runs would stay green no
matter what the code did, so every proof-named test file is either in `dev/run_tests.sh` or
proved only in platform-scoped files whose ids the registry declares. The scope is the
exemption, which is why no hand-maintained list of exceptions exists to outlive its reason: the
evidence itself names the platform that regenerates it, and an id the registry dropped fails
the same proof.

## Rules

- RULE-1: Each plugin resolves the spec directory by scanning `specs/**/*.md` and matching the feature name to the spec filename stem
- RULE-2: Proof files are written to the spec's directory as `<feature>.proofs-<tier>.json` for an entry whose marker declares no platforms, and as `<feature>.proofs-<tier>@<platform>.json` for an entry whose marker declares platforms, where `<platform>` is the host id of RULE-17. A scoped file carries a top-level `platform` equal to the id in its name, constant per file, so a re-run that changes nothing rewrites identical bytes
- RULE-3: When the spec directory for a feature is not found, the plugin falls back to writing to `specs/`
- RULE-4: Write-scoped overwrite keyed by `(feature, tier, platform, test_file)`. The tier and platform are carried by the file being written, so within that file an existing entry is replaced only if it matches the current feature AND its `test_file` was executed in the current run. Entries belonging to other features, and the current feature's entries from test files this run did not execute, are preserved, so two test files covering the same `(feature, tier, platform)` can run in any order without clobbering each other, and a run that writes a scoped file never touches the agnostic file or another platform's file
- RULE-5: Each proof entry contains all 7 required fields: `feature`, `id`, `rule`, `test_file`, `test_name`, `status`, `tier`. An entry in a scoped file contains exactly one more, `platform`, equal to the file's top-level `platform`; an entry in an agnostic file contains exactly the 7
- RULE-6: `status` is `"pass"` when the test passes and `"fail"` when it fails, no other values
- RULE-7: If no proof markers are collected during a test run, no proof files are written (no-op)
- RULE-8: Custom/community proof plugins installed to `.purlin/plugins/` require no registration: `sync_status` discovers proof files by globbing `specs/**/*.proofs-*.json`, so any plugin that writes files in that pattern works automatically
- RULE-9: When spec directory lookup falls back to specs/ root, the plugin emits a warning to stderr naming the missing spec and suggesting purlin:spec <feature>
- RULE-10: When a test is removed from a test file and that file is re-run, the old proof entry is purged and not carried over from the previous proof file
- RULE-11: Orphan reaping: when writing a tier file (agnostic or scoped), the current feature's entries whose `test_file` no longer resolves to a file in the working tree are dropped, not preserved. A renamed or deleted test file's entries are therefore reaped on the next run of that `(feature, tier, platform)`
- RULE-12: A test file's entries are reaped only by a run that executes that `(feature, tier, platform)`, and within such a run only the entries of the tests it actually executed: an entry belonging to a test the run skipped is kept by RULE-18 even though its file ran. Removing a proof marker from a test file that the run did not execute leaves that entry in place until that file runs again; a run on one platform never reaps another platform's file
- RULE-13: `status` records execution, never availability. A test that did not run on this host emits no proof entry at all: `"fail"` means the test ran and its assertion failed, and `"pass"` means it ran and passed. Writing `"fail"` for a skipped test is forbidden, because a reader and every gate downstream cannot tell a broken build from a missing tool. Emitting nothing is safe because RULE-18 holds the skipped test's committed entry in place: under the RULE-4 merge key alone it would survive only while no other test in its file ran, and a sibling that did run would reap it. With RULE-18, whatever a capable host last proved for those ids stays committed and untouched, in its own scoped file, so the skip neither falsifies nor destroys it
- RULE-14: Every test file named by a committed proof entry is executed by `dev/run_tests.sh`, or every entry naming it lives in a platform-scoped proof file whose id the `platforms` registry declares. There is no hand-maintained exception list: a file the sweep never runs is exempt only while its own evidence says which platform regenerates it, and a scoped file whose id the registry does not declare names a platform the project never defined, so it is an error in both directions. A test file in neither set is a proof nothing regenerates: it stays green no matter what the code does
- RULE-15: `test_file` is written relative to the project root with `/` separators on every operating system; a backslash never reaches a proof file. The same test must produce the same merge key, and later the same verification hash, whether it ran on Windows or on a POSIX host
- RULE-16: One plugin per framework, everywhere. A runner executes the same plugin file the developer runs (this repository's `scripts/proof/*`, a consumer project's `.purlin/plugins/*` copies, byte-identical). Nothing in `scripts/proof/` or `.purlin/plugins/` branches on the host operating system except the family fallback inside one small host-platform helper per plugin (`_host_platform`, `hostPlatform`, `host_platform`, `HostPlatform`); the only per-runner input is `PURLIN_PLATFORM`. This is what makes a simulated Windows path on macOS not a Windows proof without a second code path
- RULE-17: The marker decides, the environment names. A marker that declares no platforms writes the agnostic file whatever `PURLIN_PLATFORM` says. A marker that declares platforms writes the scoped file named by `PURLIN_PLATFORM` when it is set and by the detected OS family (`windows`, `macos`, `linux`; any other system lower-cased) when it is not. A plugin never evaluates version constraints and never reads the platform registry: which host satisfies a declared id is `sync_status`'s judgement, made from the file name
- RULE-18: A skipped test keeps its committed entry: an existing entry whose `(feature, id, test_file)` matches a marked test the run skipped is preserved with its old status; only an executed test replaces its entry and only a deleted file reaps one. Every plugin whose framework reports a skip observes it (pytest the skip report, jest a `skipped`/`pending`/`todo` status, vitest a task with no terminal `pass`/`fail` state, xunit a `Skipped` outcome). Plugins with no skip signal (shell, sql, phpunit, c) are exempt because a script that does not call the marker cannot be told apart from one that skipped
- RULE-19: Every plugin writes or merges the project's run marker `.purlin/runtime/test_run.json` at the moment it writes its proof files, so a receipt issued in a project that has no sweep script still names the run its evidence came from. The marker holds `at` (ISO 8601, UTC), `commit` (`git rev-parse HEAD` in the project root, `null` outside a git work tree), `sweep` (the writer's name: `pytest_purlin`, `jest_purlin`, `vitest_purlin`, `shell_purlin`, `sql_purlin`, `c_purlin`, `phpunit_purlin`, `xunit_purlin`, or `dev/run_tests.sh` for this repository's sweep, so a reader can tell a plugin marker from a sweep marker), `test_files` (the project-relative, forward-slash files the run collected), `passed`, `failed` and `skipped` (the marked results this run observed), `ok` (true only while every merged run had no failure), and `runs` (one `{plugin, at, test_files, passed, failed, skipped}` object per contributing run). A run whose `commit` equals an existing marker's `commit` is merged into it: `test_files` unioned, the three counts summed, the run appended to `runs`, `ok` and-ed, and every other top-level field carried through untouched so a field a later version of this contract adds is never dropped by an older plugin. Any other commit starts a new marker, because a count carried across commits would describe two trees. `dev/run_tests.sh` merges by the same rule but owns the summary: it keeps `runs` and every field it does not write, and replaces `test_files`, the counts and `ok` with the sweep's own, because it watched every suite the plugin runs inside it reported on. Concurrent writers are safe by atomic replace: the marker is written to a temp file in the same directory and renamed over the target, so a reader sees one whole marker or the other, and a read that lands on unparsable JSON is retried before the writer gives up and starts a fresh marker. Nothing is written at all when the project root holds no `.purlin/`: that is not a Purlin project, and a plugin must not create one

## Proof

- PROOF-1 (RULE-1): Create `specs/hooks/gate_hook.md` and run a proof plugin with feature `gate_hook`; verify the proof file is written to `specs/hooks/` @integration
- PROOF-2 (RULE-2): Run the real pytest plugin against `specs/hooks/gate_hook.md` on one test file holding `@pytest.mark.proof("gate_hook", "PROOF-1", "RULE-1")` and `@pytest.mark.proof("gate_hook", "PROOF-2", "RULE-2", platforms=("p1",))` under `PURLIN_PLATFORM=p1`; verify the undeclared entry's file is named exactly `gate_hook.proofs-unit.json` and carries no top-level `platform`, and the declared entry's file is named exactly `gate_hook.proofs-unit@p1.json` with top-level `platform: "p1"`. Re-run the same unchanged test file; verify both files' bytes are identical to the first run's, so a scoped file's constant per-file platform makes an unchanged re-run a no-op diff @integration
- PROOF-3 (RULE-3): Run a proof plugin for feature `nonexistent_feature` (no matching spec); verify proof file is written to `specs/` @integration
- PROOF-4 (RULE-4): Pre-seed `specs/auth/feat_a.proofs-unit.json` with a single `feat_b` entry (`id: "PROOF-1"`, `rule: "RULE-1"`, `test_file: "t.py"`, `status: "pass"`); run the real pytest plugin for feature `feat_a` only from `test_s.py`; verify the merged file holds exactly 2 entries, the `feat_b` entry still present unchanged and exactly one `feat_a` entry naming `test_s.py`, so a run for one feature neither drops nor rewrites another feature's evidence @integration
- PROOF-5 (RULE-5): Run each proof plugin with one marker declaring platforms and one declaring none under `PURLIN_PLATFORM=p1`; verify every entry in the agnostic file's `proofs` array has exactly the 7 fields `feature`, `id`, `rule`, `test_file`, `test_name`, `status`, `tier` (an eighth fails), and every entry in the scoped file has exactly those 7 plus `platform: "p1"` (a missing or different `platform` fails) @integration
- PROOF-6 (RULE-6): Run a passing test and a failing test with proof markers; verify the passing test has `status: "pass"` and the failing test has `status: "fail"` @integration
- PROOF-7 (RULE-7): Run a test suite with no proof markers; verify no `*.proofs-*.json` files are created @integration
- PROOF-8 (RULE-8): Place a `.proofs-unit.json` file in a spec directory written by a non-built-in source; run `sync_status`; verify it reads the proofs @integration
- PROOF-9 (RULE-9): Run the real pytest plugin for feature `unknown_feat_for_warning`, and the shell harness for feature `no_spec_feature_xyz`, in a project whose `specs/` holds no spec for either; verify each run's stderr contains that exact feature name and the literal `purlin:spec`, so the warning names the missing spec and the command that creates it @integration
- PROOF-10 (RULE-4): e2e: Create 2 specs; write proofs for each separately; verify both PASSING and no cross-contamination @e2e
- PROOF-11 (RULE-4): e2e: Overwrite one feature's proofs via shell harness; verify target feature updated, other feature untouched @e2e
- PROOF-12 (RULE-10): e2e: Run a shell test file emitting PROOF-1 and PROOF-2 for one feature, then re-run the same file emitting only PROOF-1; verify coverage drops to 1/2 because the PROOF-2 entry was purged, not carried over @e2e
- PROOF-13 (RULE-10): Write a proof file with 2 proofs, then re-run the same test file with only 1; verify the removed entry is purged and not carried over @integration
- PROOF-14 (RULE-4): Run the real pytest plugin for one feature from test file A, then from test file B, both at tier `unit`; verify the merged file holds both entries. Repeat with the order reversed; verify the merged file again holds both @integration
- PROOF-15 (RULE-11): Run the real pytest plugin for one feature from test files A and B, delete A, then run the same feature from test file C; verify A's entry is absent from the merged file while B's entry, whose file still exists and was not re-run, is still present @integration
- PROOF-16 (RULE-12): Run the real pytest plugin for feature `ledger` at tier `unit` from `test_debit.py` (`PROOF-1`) and `test_credit.py` (`PROOF-2`), then delete the proof marker from `test_debit.py`, leave the file on disk, and re-run `test_credit.py` alone; verify `specs/money/ledger.proofs-unit.json` still holds the entry keyed `("PROOF-1", "test_debit.py")` beside `("PROOF-2", "test_credit.py")`, because only a run that executes a test file reaps that file's entries @integration
- PROOF-17 (RULE-13): Run a suite whose prerequisite is absent so it takes its skip path; verify it writes no proof file and leaves an existing one byte-identical, and that its output names the proof ids it did not execute. Scan every proof-emitting test script in the repo for a call that writes `fail` from inside a skip or unavailable-prerequisite branch; verify there are none @integration
- PROOF-18 (RULE-14): Parse `dev/run_tests.sh` for every `$SCRIPT_DIR/test_*` path it invokes; parse every git-tracked `specs/**/*.proofs-*.json` for `test_file`, recording for each file whether its name carries an `@<id>` scope; build the registry from `.purlin/config.json` through `_platform_registry`. Verify every proof-named test file the sweep does not invoke is named only by scoped files, and that every scoped file's id is in the registry: an unswept file with an agnostic entry fails naming the path and the file, and a scoped file whose id the registry does not declare fails naming the id, so unregistering an id that a scoped file uses fails
- PROOF-19 (RULE-15): For each of the 8 plugins, hand it a backslash-bearing test path through its own API (pytest and shell: a test file literally named `tests\test_x.py` / `dev\t.sh`, collectable on POSIX; jest and vitest: a fake `testFilePath` / `filepath` under a matching root; C: the `__FILE__` argument; phpunit and sql: the argv path; xunit: `CodeFilePath`, which on a POSIX host cannot carry a backslash, so its proof asserts the real run's path and that `MakeRelative` replaces `\` in both branches); verify every written `test_file` equals the `/`-separated relative path and contains no `\` @integration
- PROOF-20 (RULE-16): Grep every file in `scripts/proof/` and `.purlin/plugins/` for `sys.platform`, `platform.system`, `process.platform`, `os.platform`, `PHP_OS`, `RuntimeInformation` and `uname`; verify at least 7 hits exist and every hit sits inside a function whose name, lower-cased with underscores removed, contains `hostplatform`; a hit anywhere else fails naming the file, line and enclosing function. Verify the four `.purlin/plugins/` copies are byte-identical to their `scripts/proof/` originals @integration
- PROOF-21 (RULE-17): For each of the 8 plugins, run its fixture with `PURLIN_PLATFORM=p1` and one marker declaring `on(p1)` (pytest `platforms=("p1",)`, shell `PURLIN_PROOF_PLATFORMS=p1`, C `purlin_proof_on(..., "p1")`) plus one unmarked; verify `<feature>.proofs-unit@p1.json` exists with `platform: "p1"` at the top level and on every entry and holds only the declared marker's id, and that the unmarked entry lands only in `<feature>.proofs-unit.json`; the declared id appearing in the agnostic file, or the unmarked id in the scoped file, fails @integration
- PROOF-22 (RULE-17): For each of the 8 plugins, run the declared-marker fixture with `PURLIN_PLATFORM` absent from the environment; verify the scoped file is named `<feature>.proofs-unit@<family>.json` where `<family>` is computed by the test from `platform.system()` (`macos` on a Darwin host), its top-level `platform` equals that family, and no other `@` file was written @integration
- PROOF-23 (RULE-18): For each plugin that can observe a skip, run a fixture whose one test file holds a passing marked test and a skipped marked test for the same feature and tier, against a proof file that already holds an entry for each of them with `status: "fail"`; verify the skipped test's entry is still in the written file with `status: "fail"` and its original `test_name`, while the passing test's entry now reads `status: "pass"`; a run that drops the skipped entry, or rewrites its status, fails naming the id. pytest is driven as a real subprocess for both of its skip signals (a `@pytest.mark.skip` marker and a `pytest.skip()` inside the body); jest and vitest through their real reporters with a `pending` status and a `skip` task state; the .NET logger has no `dotnet` toolchain on this host, so its arm asserts on `scripts/proof/xunit_purlin.cs` instead, and says so: the `TestOutcome.Skipped` branch records `SkipKey(feature, id, testFile)` before returning, and the kept filter admits an entry whose key is in that set, a source missing either failing @integration
- PROOF-24 (RULE-18): Read RULE-18's text from `specs/_anchors/proof_common.md` and take the plugin names its exemption clause lists; verify that list is exactly `shell`, `sql`, `phpunit`, `c`, and that each of the 4 exempt plugin sources (`shell_purlin.sh`, `sql_purlin.sh`, `phpunit_purlin.php`, `c_purlin_emit.py`) carries no skip-tracking set while each of the 4 capable ones (`pytest_purlin.py`, `jest_purlin.js`, `vitest_purlin.ts`, `xunit_purlin.cs`) does; a rule that drops an exempt name, or a plugin whose source disagrees with the side it was listed on, fails naming the plugin @integration
- PROOF-25 (RULE-19): For each of the 8 plugins, run its two-marker fixture (one marker declaring `on(p1)`, one declaring none, both passing) in a temp project that holds a `.purlin/` directory; verify `.purlin/runtime/test_run.json` reads `sweep` equal to that plugin's name (`pytest_purlin`, `jest_purlin`, `vitest_purlin`, `shell_purlin`, `sql_purlin`, `c_purlin`, `phpunit_purlin`, `xunit_purlin`), `test_files` equal to the single project-relative path that run collected, `passed: 2`, `failed: 0`, `skipped: 0`, `ok: true`, an `at` matching ISO 8601 in UTC, a `commit` that is `null` or 40 hex characters, and a `runs` list of exactly one object whose `plugin` is that plugin and whose counts and `test_files` match. Then age that marker into one an earlier run left, by adding a top-level `skipped_proofs` key and the path `dev/prior_run.py` to its `test_files`, and run the same fixture a second time at the same commit; verify the merge reads `passed: 4`, `failed: 0`, `ok: true`, `runs` holding 2 objects both naming that plugin, `test_files` equal to `dev/prior_run.py` plus the fixture's own path (a merge that replaces instead of unioning loses the earlier run's file and fails here), and `skipped_proofs` byte-for-byte what was seeded, so a field this rule does not name survives. pytest's case runs in a real git repository: verify `commit` equals `git rev-parse HEAD`, then rewrite the marker's `commit` to 40 zeros and run again, verifying the marker starts fresh with `passed: 2`, one object in `runs`, no `skipped_proofs` and no `dev/prior_run.py`. A ninth case covers the other writer: lift `dev/run_tests.sh`'s marker writer out of the script (never running the sweep itself), drive it over a marker a `pytest_purlin` run left at the same commit with a `skipped_proofs` key, and verify the result reads `sweep: dev/run_tests.sh`, `passed: 700`, `skipped: 13` and `test_files: ["dev/test_swept.py"]` from the sweep's own environment while `runs` still holds the one `pytest_purlin` object and `skipped_proofs` is unchanged. A plugin whose marker write is removed fails naming that plugin; a merge that replaces `test_files` instead of unioning them fails the second-run case @integration
