{
  "ai_review": null,
  "design": null,
  "design_hash": null,
  "feature": "upstream",
  "generated_at": "2026-09-16T21:23:31Z",
  "layers": [
    "proof text",
    "test body",
    "test strength"
  ],
  "min_strength": 80,
  "origin": "eng",
  "proof_hash": "e538a9f3bc180cfc5c0f9e98edacbf3a756214549fa25fc9eec5f7d420264f46",
  "proofs": [
    {
      "env": null,
      "findings": [
        "happy_path_only"
      ],
      "id": "PROOF-7",
      "text": "Write a text file holding the single sentence `Every refund is approved by a second person.` and run `add` on it with `--name refunds`; verify the result reads `drafted`, that the copy carries that sentence, the free-text note, a `## Rules` and a `## Proof` heading and no `RULE-` line at all, and that adding the same unchanged file twice returns the same pin. Hand the same absolute path and the repository path to the free-text test and verify it answers true for the file and false for the repository",
      "tier": "integration"
    }
  ],
  "reasons": [
    "PROOF-7 happy_path_only: No proof of this rule names a rejection, an error or a boundary."
  ],
  "record": ".purlin/records/upstream/20260916T212202Z-23127b4-ci-windows.json",
  "risk": "medium",
  "rule": "RULE-7",
  "rule_hash": "88a21ac0decd0e2ad8168acaccc3557862ff8e6822b0ff18554e962dd62e79aa",
  "rule_text": "A source that is a readable text file rather than a repository is copied in whole as a drafted anchor carrying a note and no rules, pinned by the hash of its text, so rewording the source moves the pin",
  "schema": "purlin-brief/1",
  "state": "Tested",
  "test_hash": "a5b26ece457c74cf2a9bb8b2437e06e5f42845a1da4a075c8f185f4cdf6740a7",
  "test_hash_kind": "file",
  "test_strength": null,
  "tests": [
    {
      "body": "def test_add_of_free_text_writes_a_note_and_no_rules(workspace):\n    source = os.path.join(workspace.root, 'policy.txt')\n    _write(source, 'Every refund is approved by a second person.\\n')\n    result = upstream.add(workspace.root, source, name='refunds')\n    assert result['status'] == 'drafted'\n    text = _copy_text(workspace, 'refunds')\n    assert 'Every refund is approved by a second person.' in text\n    assert upstream.FREE_TEXT_NOTE in text\n    assert '## Rules' in text and '## Proof' in text\n    assert 'RULE-' not in text\n    # The pin is the hash of the text, so a reworded source stales the copy.\n    assert result['pinned'] == upstream.add(\n        workspace.root, source, name='refunds')['pinned']",
      "file": "dev/test_upstream.py",
      "findings": [],
      "name": "test_add_of_free_text_writes_a_note_and_no_rules",
      "proof": "PROOF-7",
      "reasons": []
    },
    {
      "body": "def test_free_text_is_not_mistaken_for_a_repository(workspace):\n    \"\"\"An absolute path to a file satisfies the git-url test as well, so the\n    file has to win: a text file is not a repository.\"\"\"\n    source = os.path.join(workspace.root, 'policy.txt')\n    _write(source, 'Nothing is deleted without a record.\\n')\n    is_free, text = upstream._free_text(workspace.root, source)\n    assert is_free is True\n    assert 'Nothing is deleted' in text\n    assert upstream._free_text(workspace.root, workspace.anchor_repo)[0] is False",
      "file": "dev/test_upstream.py",
      "findings": [],
      "name": "test_free_text_is_not_mistaken_for_a_repository",
      "proof": "PROOF-7",
      "reasons": []
    }
  ],
  "triple_hash": "ccf984fd8fe323153e6c8557e28cc5aa719d7a1298718c5534c53bdd1d750b2f",
  "verdict": "add a case"
}
