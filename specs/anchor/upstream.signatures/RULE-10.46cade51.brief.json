{
  "ai_review": null,
  "design": null,
  "design_hash": null,
  "feature": "upstream",
  "generated_at": "2026-09-16T21:21:13Z",
  "layers": [
    "proof text",
    "test body",
    "test strength"
  ],
  "min_strength": 80,
  "origin": "eng",
  "proof_hash": "616d3b6957ccf5393a857cd82021b2d016a2b1e0ee5be4215b6eba1c4607a5bc",
  "proofs": [
    {
      "env": null,
      "findings": [
        "happy_path_only"
      ],
      "id": "PROOF-10",
      "text": "Run `sync --check --json` from the command line on a current pin and verify exit 0 with `behind` 0 in the parsed object; publish a new version and verify exit 1 with `checked` true, `behind` 1 and the first row's `remote_sha` equal to the new sha; run `sync --check` without `--json` and verify exit 1 with `is behind its source` and `purlin:anchor sync no_eval` in the printed lines",
      "tier": "integration"
    }
  ],
  "reasons": [
    "PROOF-10 happy_path_only: No proof of this rule names a rejection, an error or a boundary."
  ],
  "record": ".purlin/records/upstream/20260916T211924Z-4f36835-ci-windows.json",
  "risk": "medium",
  "rule": "RULE-10",
  "rule_hash": "0a4a6983a92f999dad7a031d7b3a5d0b0e4b8a4995c5943e9de4ba548f76e819",
  "rule_text": "`--json` prints the whole answer as one JSON object carrying `checked`, `behind` and a row per anchor; without it the lines name the anchor behind and the `purlin:anchor sync <name>` that fixes it",
  "schema": "purlin-brief/1",
  "state": "Tested",
  "test_hash": "ae08d8415ffa284bd22cb51b7249377e3243abe2578c1c94ff5a175dd62cf165",
  "test_hash_kind": "file",
  "test_strength": null,
  "tests": [
    {
      "body": "def test_check_exit_code_and_json_from_the_command_line(workspace):\n    _add(workspace)\n    code, out = _cli(workspace, ['sync', '--check', '--json'])\n    assert code == 0\n    assert json.loads(out)['behind'] == 0\n\n    new_sha = _advance(workspace)\n    code, out = _cli(workspace, ['sync', '--check', '--json'])\n    assert code == 1\n    payload = json.loads(out)\n    assert payload['checked'] is True\n    assert payload['behind'] == 1\n    assert payload['anchors'][0]['remote_sha'] == new_sha\n\n    code, out = _cli(workspace, ['sync', '--check'])\n    assert code == 1\n    assert 'is behind its source' in out\n    assert 'purlin:anchor sync no_eval' in out",
      "file": "dev/test_upstream.py",
      "findings": [],
      "name": "test_check_exit_code_and_json_from_the_command_line",
      "proof": "PROOF-10",
      "reasons": []
    },
    {
      "body": "def test_check_exit_codes_from_a_child_process(workspace):\n    \"\"\"The exit codes a caller's shell sees, from the script run as a process.\"\"\"\n    _add(workspace)\n    code, out = _cli(workspace, ['sync', '--check', '--json'], child=True)\n    assert code == 0\n    assert json.loads(out)['behind'] == 0\n\n    new_sha = _advance(workspace)\n    code, out = _cli(workspace, ['sync', '--check', '--json'], child=True)\n    assert code == 1\n    payload = json.loads(out)\n    assert payload['behind'] == 1\n    assert payload['anchors'][0]['remote_sha'] == new_sha\n\n    code, out = _cli(workspace, ['sync', '--check'], child=True)\n    assert code == 1\n    assert 'purlin:anchor sync no_eval' in out\n\n    code, out = _cli(workspace, ['sync', 'absent', '--check', '--json'],\n                     child=True)\n    assert code == 2\n    assert json.loads(out)['anchors'][0]['status'] == 'error'",
      "file": "dev/test_upstream.py",
      "findings": [],
      "name": "test_check_exit_codes_from_a_child_process",
      "proof": "PROOF-10",
      "reasons": []
    }
  ],
  "triple_hash": "46cade51228e7b38082974426d36d477f8238e030e3ec45edaf195164afce127",
  "verdict": "add a case"
}
