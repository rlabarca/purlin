# Feature: purlin_report

> Requires: dashboard_visual
> Scope: scripts/report/purlin-report.html
> Stack: html/css/javascript
> Description: Static HTML dashboard that renders Purlin coverage data from `.purlin/report-data.js`. Opens via `file://` in any browser with no server or build step.

## Rules

- RULE-1: HTML loads .purlin/report-data.js via a script tag before rendering
- RULE-2: When PURLIN_DATA is undefined, dashboard shows a no-data message with instructions
- RULE-3: Summary strip shows total features, verified, passing, incomplete (partial + untested combined), failing counts from PURLIN_DATA.summary; the incomplete card shows a subtitle breakdown when both partial and untested are nonzero. When the payload reports `platform_testing`, the Verified and Passing headline numbers stay the all-platform figures and gain a sub-label instead: Verified reads `<verified_here> on host, <n> on <platform>` naming the platform binding the count when `held_by_platform` is above zero and `every declared platform` otherwise, and Passing reads `<held_by_platform> awaiting a platform` when any feature is held. Changing the headline would make a Verified count that fell because a runner has not run yet indistinguishable from a regression, and leaving the drop unlabelled would make it inexplicable
- RULE-4: Feature table renders one row per feature with name, coverage fraction, status badge, design, integrity, and verified columns. Design and Integrity are separate columns because they are separate measurements: one grades the proof description, the other the test behind it. A feature whose payload carries platform records renders a chip strip under its status badge, one chip per declared platform carrying the abbreviation of that platform's registry `os` and a mark for its record status, with the full record in the chip's tooltip. No new column: seven columns of table already compete for the width, and the chips belong to the status the badge is already stating
- RULE-5: Clicking a feature row expands it to show per-rule detail
- RULE-6: Expanded detail shows each rule with id, description, source (blank for own rules, "required" or "global" for others), status, and proof column; when a rule has multiple proofs, all proofs are stacked vertically in a single cell separated by a thin divider, not split across rowspan rows
- RULE-7: Toggling dark/light mode persists the preference to localStorage
- RULE-8: Staleness indicator shows amber warning when data is older than 1 hour
- RULE-9: Staleness indicator shows red warning when data is older than 24 hours
- RULE-10: Anchor features display a type pill (anchor or global) next to their name
- RULE-11: Anchors with source_url display an external link icon with the URL as tooltip
- RULE-12: Feature table columns are sortable by clicking column headers
- RULE-13: Footer docs link uses docs_url from PURLIN_DATA, not a hardcoded URL
- RULE-14: Summary strip shows integrity percentage from audit_summary with color coding, or dash with "run purlin:audit" when null. When `platform_testing` is true the card also opens the per-platform Integrity modal, because a single project figure cannot say which platform was measured and which was not
- RULE-15: The header shows one freshness label per quality gauge, each naming its own gauge and carrying its own relative time and staleness class, sourced from `audit_summary.stale` and `design_summary.stale` independently. A single combined `audit` label is forbidden: the two caches have independent timestamps, so one label reported the Integrity cache's age as though it were the project's, and a repo graded for Design an hour ago read as 78 days stale
- RULE-16: Status column is centered in both the feature table and the expanded rules sub-table at all viewport widths
- RULE-17: Dashboard uses full available width up to 2400px and is responsive down to 1100px
- RULE-18: Coverage bar fill width matches the proved/total fraction — a feature with 2/6 coverage has a bar fill at ~33%, not 100%
- RULE-19: Features are grouped by category with collapsible section headers showing rolled-up summaries (total count, coverage fraction, and status breakdown); category coverage bar is amber when any feature is untested or coverage is incomplete, red when any feature is failing, green only when all features are passing or verified; this grouping applies independently within both the Specs and Anchors sections
- RULE-20: Category sections are expanded by default; a category renders collapsed only when localStorage explicitly records it as collapsed
- RULE-21: Category and feature open/closed state is persisted to localStorage and restored on reload
- RULE-22: Proof audit tags (STRONG/WEAK/HOLLOW) only appear when audit_summary has data; when audit_summary is null (integrity card shows dash), no audit tags render on any proof
- RULE-23: Expanded detail view shows a status-colored action banner above the rules table: PARTIAL shows how many rules need proofs, FAILING shows how many tests are failing, PASSING shows "run purlin:verify", UNTESTED shows "write tests to begin coverage", VERIFIED shows no banner of its own. When the payload's `migrations` list is non-empty the same banner area also shows a pending-migrations banner naming the count, the ids and `purlin:init --update`, in every status including VERIFIED: pending migrations are project state rather than feature state, and a feature verified against a legacy alias is verified against a reading the next release drops. It carries the same directive `sync_status` prints, so the dashboard and the CLI cannot send a reader to different commands
- RULE-24: Rules with NONE status display with an amber left border, visually distinguishing uncovered rules from proved ones
- RULE-25: Rules with FAIL status display with a red left border, visually distinguishing failing rules from passing ones
- RULE-26: Expanded detail view for anchors with source_url shows an "External Reference" info block above the rules table, displaying Source URL (as clickable link), Path (if present, in code font), and Pinned version (SHA truncated to 7 chars for git, full timestamp for Figma); unpinned anchors show amber "Unpinned" text
- RULE-27: External link icon tooltip includes Source URL, Path (if present), and Pinned value (truncated)
- RULE-28: Anchors with `ext_status` of "stale" or "unpinned" display an amber outline "STALE" badge next to the external link icon, with tooltip "Run purlin:anchor sync to update"; anchors with "current" or null ext_status show no badge
- RULE-29: When `uncommitted` array in PURLIN_DATA is non-empty, an expandable "uncommitted work" section appears between the summary strip and the feature table, showing the file count when collapsed and the file list when expanded; when the array is empty or absent, no section appears
- RULE-30: Expanded detail view shows the spec's `> Description:` content as a description block (`.desc-block`) using the full width of the detail container, positioned between the external reference block (or action banner if no external reference) and the "Rules & Proofs" header; the block is only rendered when the feature has a non-null, non-empty description
- RULE-31: Proofs whose description matches structural patterns (grep, file exists, section present, verify contains) display a green "Structural" tag (solid green background, white text) next to the proof ID; structural tags render independently of audit data and are not counted in the integrity score
- RULE-32: Anchor features render in a separate "Anchors" section below the Specs section, with its own section label and table; the Specs section contains only non-anchor features
- RULE-33: Planned proofs (proof entries with status "planned") render in the rule's proof cell greyed with a "not run" indicator, visually distinct from executed proofs and from the bare em dash shown when a rule has no proofs at all; planned proofs never show an audit tag
- RULE-34: The summary strip renders a Proof Design card beside Proof Integrity, reading `design_summary.design` with the same 80/50 colour bands and CSS classes as the integrity card, and a sub-label of per-level counts. When `design_summary` is null it shows `—`. The integrity card's null sub-label distinguishes a project where every feature is UNTESTED (`no tests yet`) from one that simply has not been audited (`run purlin:audit`)
- RULE-35: Every quality cell in the feature table renders a value, never an em dash: a percentage when the gauge is `measured`, `not audited` when it is `unmeasured`, and when it is `excluded` the word from that gauge's own vocabulary: `structural` for Proof Design, whose unscorable level is STRUCTURAL, and `excluded` for Proof Integrity, whose unscorable level is EXCLUDED. The two vocabularies never mix, so a Design cell must never read `excluded`. Each cell carries a `title` tooltip naming the reason and stating the feature's measurement coverage, and is coloured by its meaning rather than greyed out. Tokens are whole words, not abbreviations
- RULE-36: Each gauge card headlines the coverage-weighted figure rather than the assessed score, so a high score over a small measured subset cannot read as a green project-wide result, and the card's colour band follows that headline with no override. Beneath it a sub-label reads `<measured> of <total> measured`, coloured by the measured/total fraction through the same colour-band helper as the headline, so how much was measured is legible at a glance rather than dim grey text under a large number. The card's tooltip carries the per-level counts and the unweighted assessed score, because a low headline caused by thin coverage and one caused by bad proofs need different fixes. `gaugeCard` takes a fifth `attrs` argument carrying the modal attributes of RULE-42, empty for a project declaring no platform, so one helper still builds both cards and neither grows a second definition to drift from
- RULE-37: A stale or unmeasured gauge's refresh directive names the narrowest command that refreshes that gauge: `purlin:audit --design` when only Design needs it, `purlin:audit --integrity` when only Integrity does, and bare `purlin:audit` only when both do. Design grading is deterministic and needs no tests, while Integrity grading needs test code and costs LLM calls, so directing someone to a full audit to refresh Design alone spends budget for nothing
- RULE-38: At every breakpoint of 900px or wider, the summary strip tiles its cards into completely filled rows. A column count that leaves empty cells in the last row reads as a rendering failure rather than a layout, so the narrower desktop breakpoint spans the five status cards and the two gauge cards into two exactly-filled rows instead of wrapping seven cards into four columns. Below 900px a trailing gap is ordinary responsive wrapping and is allowed. The modal overlay is `position: fixed` outside the strip's grid, so opening it adds no grid item and the card count stays seven at every breakpoint: a dialog that tiled as an eighth card would break the exactly-filled rows this rule exists to guarantee
- RULE-39: A feature whose payload carries platform records renders a Platforms block in its expanded detail, one line per declared platform with the host first and badged, each naming the proved fraction, the failing and awaiting proof ids when there are any, and when the platform was last proved and by which runner. A feature whose records are empty but whose `awaiting_runner` list is not falls back to the tier-grouped Awaiting Runner block, which is what a payload written before platform records existed carries. Either block carries no status badge change and no red: the coverage fraction on that row already excludes those rules, so the block exists to explain an exclusion the row would otherwise make silently. A feature with neither renders no block at all
- RULE-40: The header renders the declared remote-verification mode whenever the payload's `remote_verification` is not `"off"`, as a labelled chip beside the two gauge-freshness labels, and its tooltip carries the declaration/enforcement split: the config field declares the mode and branch protection enforces it. An `"off"` project renders no chip, so the header gains nothing for a project that never opted in. The mode is read from the payload and never recomputed here, for the same reason the gauges are: two surfaces computing one figure is how they come to disagree
- RULE-41: The dashboard has exactly one modal helper, `openModal(title, bodyHtml, footHtml)` with `closeModal()` and `onModalKey`, and every dialog on the page goes through it. The overlay is appended to `document.body` and never to `#app`, because `render()` rewrites `#app` wholesale and a dialog mounted inside it would be destroyed mid-interaction by any sort, expand or theme toggle; `render()` itself begins with `closeModal()`, so no dialog outlives the content it described. The dialog carries `role="dialog"` and `aria-modal="true"` and is labelled by its own heading. Escape, a click on the backdrop and the close button each close it; Tab is trapped inside it; focus moves to the close button on open and returns to the element that opened it on close; and a second open replaces the first rather than stacking two dialogs
- RULE-42: When and only when the payload reports `platform_testing`, the Verified, Passing and Proof Integrity cards, and the Failing card when some platform has a failing feature, carry `role="button"`, `tabindex="0"`, `aria-haspopup="dialog"` and a `data-modal` naming which modal they open, and open it on click and on Enter or Space. The three modals are the counts table (Platform, Features, Verified, Passing, Awaiting, Failing, Last proved), the Integrity table (Platform, Executed, Measured, Integrity, Assessed, S / W / H) and the failing table (Platform, Failing features, Proofs), each with the host row first and badged and each closed by a footer stating what the numbers above it do and do not claim. With `platform_testing` false no card carries any of those attributes and every card is inert exactly as it was before platforms existed, because a card that opens an empty table is worse than one that does not open
- RULE-43: Platform colour is colour by meaning, taken from the RULE-3 status colours of `dashboard_visual`: green for proved, amber for awaiting a runner, red for failing. The chip strip never alters the feature's status badge and carries no click handler of its own, so the row click that expands the detail keeps working; a PASSING badge held by an awaiting platform gains a tooltip naming that platform and nothing else. A badge that changed colour because a platform had not run yet would be reporting a failure that has not happened

## Proof

All visual proofs use Playwright to load the dashboard HTML with synthetic data, interact with UI elements, take screenshots, and verify DOM state.

- PROOF-1 (RULE-1): Use Playwright to read page source; verify script tag with src=".purlin/report-data.js" is present @e2e
- PROOF-2 (RULE-2): Use Playwright to load purlin-report.html without report-data.js; take screenshot; verify page contains text "No dashboard data" and "purlin:status" @e2e
- PROOF-3 (RULE-3): Write report-data.js with summary {total_features:10, verified:4, passing:2, partial:2, failing:1, untested:1}; load page in Playwright; verify summary cards show: Features=10, Verified=4, Passing=2, Incomplete=3 (with subtitle "2 partial, 1 untested"), Failing=1; take screenshot @e2e
- PROOF-4 (RULE-4): Write report-data.js with 8 features in mixed states; load in Playwright; count table rows with class "fr"; verify count is 8; take screenshot @e2e
- PROOF-5 (RULE-5): Load page with features; click first feature row; verify a detail row with class "dr" becomes visible; verify it contains a rules table; take screenshot of expanded state @e2e
- PROOF-6 (RULE-6): Expand a feature with own + global rules, one rule having 2 proofs; verify own rules have empty Source column; verify global rules show "global" in Source column; verify the multi-proof rule has both proofs stacked in a single td with a .rprf-sep divider between them; take screenshot @e2e
- PROOF-7 (RULE-7): Load page; click theme toggle; verify data-theme attribute changes; reload page; verify theme persisted from localStorage @e2e
- PROOF-8 (RULE-8): Write report-data.js with timestamp set to 2 hours ago; load in Playwright; verify staleness text contains "ago" and has CSS class "warning"; take screenshot @e2e
- PROOF-9 (RULE-9): Write report-data.js with timestamp set to 2 days ago; load in Playwright; verify staleness text has CSS class "stale"; take screenshot @e2e
- PROOF-10 (RULE-10): Write report-data.js with anchor features (one global, one local); load in Playwright; verify elements with class "tp-global" and "tp-anchor" exist; take screenshot @e2e
- PROOF-11 (RULE-11): Write report-data.js with anchor having source_url; load in Playwright; verify element with class "ext-icon" exists and has title attribute containing the URL @e2e
- PROOF-12 (RULE-12): Load page; click "Coverage" column header; verify table row order changes (first row name differs from default sort); take screenshot @e2e
- PROOF-13 (RULE-13): Write report-data.js with docs_url set to "https://example.com/docs"; load in Playwright; verify footer link href equals "https://example.com/docs" @e2e
- PROOF-14 (RULE-14): Write report-data.js with audit_summary.integrity=85; load in Playwright; verify a summary card contains "85%"; write another with audit_summary=null; reload; verify dash shown instead; take screenshots of both @e2e
- PROOF-15 (RULE-15): Write report-data.js with `audit_summary.stale` true and `design_summary.stale` false, their timestamps 78 days and 1 hour old; load in Playwright; verify two distinct header freshness labels render, that the one naming Design shows the 1-hour time with no stale class, that the one naming Integrity shows the 78-day time with the stale class, and that no single label reports one gauge's age for both; take screenshot @e2e
- PROOF-16 (RULE-16): Load page in Playwright at 1920px and 1280px widths; expand a feature; verify the status badge td in the feature table has text-align:center; verify the status td in the rules sub-table has text-align:center; take screenshots at both widths @e2e
- PROOF-17 (RULE-17): Load page in Playwright at viewport width 2400px; verify dashboard container max-width is 2400px; resize to 1100px; verify summary strip reflows to 3 columns; take screenshots at both widths @e2e
- PROOF-18 (RULE-18): Create features with known coverage fractions (2/6 and 5/5); load in Playwright; measure the cov-fill element width as a percentage of the cov-bar; verify 2/6 bar is ~33% and 5/5 bar is 100% @e2e
- PROOF-19 (RULE-19): Write report-data.js with features in 3 categories: "skills" (2 PASSING), "mcp" (1 PASSING + 1 UNTESTED), "_anchors" (1 VERIFIED anchor); load in Playwright; verify the Specs section has 2 category headers ("skills", "mcp"); verify the Anchors section has 1 category header ("_anchors"); verify "mcp" category has amber coverage bar; take screenshot @e2e
- PROOF-20 (RULE-20): Load page with categorized features and empty localStorage; verify feature rows (.fr) are visible without any interaction; click a category header; verify its features become hidden; take screenshot @e2e
- PROOF-21 (RULE-21): Load page (categories expanded by default); collapse a category; reload page; verify the same category is still collapsed and other categories remain expanded; expand it; reload; verify it is expanded @e2e
- PROOF-22 (RULE-22): Load page with audit_summary having integrity=85 and features with proof-level audit tags; expand a feature; verify .atag elements appear on proofs; take screenshot. Then load with audit_summary=null but same per-feature audit data; expand the same feature; verify zero .atag elements render; take screenshot @e2e
- PROOF-23 (RULE-23): Write report-data.js with features in each status (PARTIAL with 2 NONE rules, FAILING with 1 FAIL rule, PASSING, UNTESTED, VERIFIED) and an empty `migrations` list; expand each; verify PARTIAL has .ab-partial banner containing "2 rules need proofs"; FAILING has .ab-failing banner containing "1 test failing"; PASSING has .ab-passing banner containing "purlin:verify"; UNTESTED has .ab-untested banner containing "write tests"; VERIFIED has no .ab element; take screenshots. Then reload the same data with two entries in `migrations` and verify every expanded feature, VERIFIED included, now carries exactly one .ab-migrations banner reading "2 pending migrations" with both ids and `purlin:init --update`, and that each feature's own banner is still present beside it @e2e
- PROOF-24 (RULE-24): Expand a PARTIAL feature with NONE rules; verify rule rows with NONE status have class "rule-np"; verify the first td of those rows has a computed border-left-color matching amber; take screenshot @e2e
- PROOF-25 (RULE-25): Expand a FAILING feature with FAIL rules; verify rule rows with FAIL status have class "rule-fail"; verify the first td of those rows has a computed border-left-color matching red; take screenshot @e2e
- PROOF-26 (RULE-26): Write report-data.js with an anchor feature having source_url, pinned (40-char SHA), and source_path; expand the anchor; verify .ext-ref-block exists containing a Source link, Path in code element, and Pinned truncated to 7 chars; write another anchor with no pinned; expand it; verify amber "Unpinned" text; take screenshots @e2e
- PROOF-27 (RULE-27): Write report-data.js with anchor having source_url, pinned, and source_path; verify .ext-icon title attribute contains all three values @e2e
- PROOF-28 (RULE-28): Write report-data.js with one anchor having ext_status="stale" and another with ext_status=null; load dashboard; verify stale anchor has .ext-stale badge with text "STALE" and tooltip containing "sync"; verify non-stale anchor has no .ext-stale badge @e2e
- PROOF-29 (RULE-29): Write report-data.js with uncommitted=["M file1.py","?? file2.js"]; load dashboard; verify .uw-section exists with count "2"; click the header; verify .uw-files becomes visible with file list; reload with uncommitted=[]; verify no .uw-section exists @e2e
- PROOF-30 (RULE-30): Write report-data.js with one feature having description "Handles user login" and another with description null; expand each; verify the first shows a .desc-block containing "Handles user login" positioned before the rules table; verify the second has no .desc-block element @e2e
- PROOF-31 (RULE-31): Write report-data.js with audit_summary having integrity data and a feature with two proofs: one with a structural description ("Grep scripts/ for eval(); verify zero matches") and one behavioral ("Returns 401 when token is expired"); expand the feature; verify the structural proof has an .atag-st element with text "Structural" and computed background-color matching green (#22c55e); verify the behavioral proof does not have an .atag-st element; verify the structural proof's tag has white text color @e2e
- PROOF-32 (RULE-32): Write report-data.js with 2 regular features (category "auth") and 2 anchor features (category "_anchors"); load in Playwright; verify two distinct .section-label elements exist with text "Specs" and "Anchors"; verify anchor feature names appear only inside the Anchors section table; verify no anchor features appear in the Specs section table; take screenshot @e2e
- PROOF-33 (RULE-33): Write report-data.js with audit_summary having integrity data and one rule whose proofs array contains one entry with status "planned" (empty test_file/test_name) and one executed entry, plus a second rule with an empty proofs array; expand the feature; verify the planned proof renders inside an element with class .rprf-planned containing "not run" and has no .atag or .atag-st element; verify the executed proof renders normally; verify the empty-proofs rule still shows an em dash; take screenshot @e2e
- PROOF-35 (RULE-34): Parse `scripts/report/purlin-report.html`; verify it binds `design_summary` from PURLIN_DATA, renders a card labelled `Proof Design` reading `.design` with the same `int-mid`/`int-lo` classes and 80/50 thresholds as the integrity card, emits an em dash when the gauge is null, and that the integrity card's null sub-label branches on whether every feature is UNTESTED @integration
- PROOF-34 (RULE-34): Load the dashboard in Playwright with `design_summary` at 90%, 60% and 30%; verify the Proof Design card shows each percentage with the green, amber and red classes respectively. Load with `design_summary` null; verify the card renders `—`. Load with every feature UNTESTED and no audit_summary; verify the Proof Integrity card sub-label reads `no tests yet`, and with a tested-but-unaudited project verify it reads `run purlin:audit` @e2e
- PROOF-36 (RULE-4): Parse `scripts/report/purlin-report.html`; verify the shared column list holds exactly `name`, `coverage`, `status`, `design`, `integrity`, `verified` in that order with `design` before `integrity`, that the category header row spans 4 columns and the expanded detail row spans 6, and that both gauge cells route through the same colour-band helper @integration
- PROOF-37 (RULE-35): Load the dashboard in Playwright with three features: one at `measured` 75%, one `excluded`, one `unmeasured`; verify the Design column reads `75%`, `structural` and `not audited` while the Integrity column reads `75%`, `excluded` and `not audited`, proving each gauge uses its own vocabulary; verify each cell carries a non-empty `title`, that the unscorable cell carries the teal class and the unmeasured cell the amber class, and that no cell in either column contains an em dash or the gray `int-na` class @e2e
- PROOF-38 (RULE-36): Load the dashboard with a Proof Design gauge whose assessed score is 100% but whose `weighted` figure is 2% over `{measured: 11, total: 560, complete: false}`; verify the card headlines `2%` and not `100%`, carries the red band class rather than amber, shows the sub-label `11 of 560 measured` in the red band, and names the assessed score in the sub-label's tooltip. Reload at 60% coverage and verify the sub-label moves to the amber band. Reload with `{measured: 560, total: 560, complete: true}` and a weighted figure equal to the assessed 100%; verify the card headlines `100%` with the green band and the sub-label reads `560 of 560 measured` in green @e2e
- PROOF-39 (RULE-37): Load the dashboard with only `design_summary.stale` true; verify the header directive reads `purlin:audit --design` and names neither `--integrity` nor a bare `purlin:audit`. Reload with only `audit_summary.stale` true; verify it reads `purlin:audit --integrity`. Reload with both stale; verify it reads bare `purlin:audit` with no flag @e2e
- PROOF-40 (RULE-38): Load the dashboard in Playwright at 1600px and at 1280px, the widths either side of the desktop breakpoint; at each, read every summary card's grid column span and the strip's column count and verify the spans sum to an exact multiple of the column count, so no row has an empty cell. Verify no card's computed bounding box leaves a card-sized horizontal gap before the strip's right edge @e2e
- PROOF-41 (RULE-39): Load the dashboard in Playwright with one feature carrying `awaiting_runner: [{id: PROOF-2, tier: windows}]` and another carrying an empty list; expand both rows and verify the first shows an Awaiting Runner block naming `@windows` and `PROOF-2` while the second has no such block, and that the first feature's status badge is unchanged from what its status alone dictates @e2e
- PROOF-42 (RULE-40): Render the dashboard with `remote_verification: "required"` and verify the header shows a chip naming the mode whose tooltip names both the config declaration and branch protection as the enforcement. Re-render with `"optional"` and verify the chip reads that mode. Re-render with `"off"` and verify no such chip is present anywhere in the header @integration
- PROOF-43 (RULE-41): Grep `scripts/report/purlin-report.html` and assert it defines `openModal(title, bodyHtml, footHtml)`, `closeModal()` and `onModalKey` exactly once each, that the overlay is appended with `document.body.appendChild` and never with an `app.appendChild` or an `#app` insertion, that the dialog markup carries `role="dialog"`, `aria-modal="true"` and `aria-labelledby`, that the body of `render()` begins with a `closeModal()` call before any other statement, that `--bg-overlay` and `--shadow` are each defined in both the dark and the light theme block, and that the `.modal-overlay` and `.modal` rules contain no hex colour literal, referencing the custom properties instead @integration
- PROOF-44 (RULE-42): Load the dashboard in Playwright with `platform_testing: true` and two platform summary rows, one of them the host: click the Verified card and assert a `#modal` appears with `role="dialog"`, `aria-modal="true"`, one table row per platform, the host row first carrying a `.host-badge` or `.awr-host` element, and a footer containing "Verified counts a feature only when every declared platform is proved and receipted"; press Escape and assert the overlay is gone and focus returned to the Verified card. Open it again with Enter on the focused card, click the backdrop and assert it closes; open it again and press Tab through more elements than the dialog contains and assert `document.activeElement` never leaves `#modal`. Open the Proof Integrity card and assert the table header reads Platform, Executed, Measured, Integrity, Assessed and `S / W / H`; capture `proof44_platform_modal.png`. Reload with `platform_testing: false` and assert no summary card carries `data-modal`, that clicking Verified opens no `#modal`, and that the Failing card carries no `data-modal` when no platform row reports a failing feature @e2e
- PROOF-45 (RULE-3): Load the dashboard in Playwright with `summary.verified` 4, `summary.verified_here` 6 and `summary.held_by_platform` 2 over two platform rows: assert the Verified card's number reads `4` and not `6`, that its sub-label reads `6 on host, ` followed by the verified count and id of the platform with the fewest verified features, and that the Passing card's sub-label reads `2 awaiting a platform`. Reload with `held_by_platform` 0 and assert the Verified sub-label reads `every declared platform` and the Passing card carries no sub-label. Reload with `platform_testing: false` and assert neither card carries a sub-label at all @e2e
- PROOF-46 (RULE-43): Load the dashboard in Playwright with one feature whose platform records are PASSING on a `macos` registry entry, AWAITING on a `windows` one and FAILING on a `linux` one: assert its status cell carries three `.pchip` elements reading `mac`, `win` and `linux` with the marks for proved, awaiting and failing, that their computed colours through `rgb_to_hex` are exactly `#22c55e`, `#f59e0b` and `#ef4444`, and that each chip's tooltip names its platform and record status. Assert the feature's status badge class is the one its status alone dictates and is unchanged from the same feature rendered with no platform records, that no chip carries a click handler by asserting a click on a chip still toggles the row's expanded state, and that a PASSING feature with an awaiting platform has a badge `title` naming that platform @e2e
- PROOF-47 (RULE-39): Load the dashboard in Playwright with one feature carrying platform records for a host platform proved 2 of 2 with provenance and a second platform awaiting one proof: expand it and assert the detail shows a `Platforms` section heading, that the host line comes first and carries the host badge, that it reads `2/2 proved` with the provenance runner and relative time, and that the second line names the awaiting proof id. Assert a feature carrying `platforms: {}` but a non-empty `awaiting_runner` still renders the tier-grouped `Awaiting Runner` block with its `.awr-tier` element, and that a feature with neither renders no block of either kind. Assert the status badge of the feature with an awaiting platform is unchanged from what its status alone dictates @e2e
