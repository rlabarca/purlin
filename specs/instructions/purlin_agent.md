# Feature: purlin_agent

> Requires: schema_spec_format
> Scope: agents/purlin.md
> Stack: markdown (agent definition)
> Description: Defines the Purlin Agent persona and behavior for Claude Code — the primary instruction document that shapes how Claude operates in spec-driven mode. Covers the core development loop, proof marker syntax, coverage gates, and routing between skills.

## Rules

- RULE-1: The agent definition's YAML frontmatter carries `name` and `description` and **no** `model` field. A shipped agent that pins a model overrides the choice of every host that installs the plugin, goes stale the moment the named model is superseded, and, on the auditor, quietly reframes independence as a model difference when it is a context difference. The host chooses; this repository's own choice, if it makes one, belongs in `.claude/settings.json`
- RULE-2: The `## Core Loop` section names four moves, `Do the work`, `Call \`sync_status\``, `Follow → directives` and `Ship`, and numbers none of them: no line in the section begins with a digit and a full stop. The statement that there is no fixed order stands above the first of the four. The section ended in a numbered `4. Ship` immediately above a subsection headed "There is no fixed order", which is a fixed order stated twice and denied once, and an agent reading top to bottom meets the order first. Numbering carries the claim whatever the prose underneath says, so the numbers go rather than the denial moving again
- RULE-3: The `## Specs` section includes the 2-section format template with `> Description:`, `## Rules`, `## Proof`
- RULE-4: The `## Proof Markers` section documents marker syntax for all 3 frameworks: pytest, Jest, and shell
- RULE-5: The `## Hard Gates` section defines exactly 1 gate: proof coverage
- RULE-6: The `## Implicit Routing` section maps user intents to actions including role-based priority handling (test/build/fix → read spec + build, status → sync_status, drift → purlin:drift, spec → purlin:spec, verify → purlin:verify with independent audit, engineer items → drift + build/test, QA items → drift + verify)
- RULE-7: Every row of the `## Skills` table names a skill that exists on disk as `skills/<name>/SKILL.md`, and every shipped skill has a row, each with a non-empty purpose. The table is proved by that correspondence and not by a row count: a count passes while naming a skill that was deleted, and the previous count of 12 passed only because a thirteenth row, `purlin:init --update`, was a flag the row regex could not see. Flags belong in the row of the skill they belong to
- RULE-8: Every row of the `## References` table that names a repository path names one that exists on disk, and every row carries a non-empty topic. The table is a reading list, not an inventory, so it is proved by every row leading somewhere rather than by a count: a count freezes the table at a number, says nothing about whether a reader following a row arrives anywhere, and has to be edited by hand every time the list changes
- RULE-9: The agent instructions present the three questions (provable / proven / passes now) mapped to Proof Design, Proof Integrity and `purlin:verify`, and state that `purlin:verify` alone owns pass/fail
- RULE-10: The Core Loop states there is no fixed order, enumerates the observable states with the next step for each, and names all three supported pathways — specs-and-proofs-first, specs-then-code-and-tests, and specs-then-code-then-tests-then-verify. It contains no instruction to build code first when a spec exists without code
- RULE-11: Implicit routing includes design-intent triggers ("are my proofs any good", "before we build") routed to `purlin:audit --design`, and an integrity-target trigger that requires answering the ceiling arithmetic before starting work. The agent routes to that arithmetic by name, in `references/audit_criteria.md` (“Reaching a target Integrity score”), and carries no copy of the formula: this file is read on every turn, so a formula here costs tokens every turn and is the copy that goes stale when the denominator's definition changes
- RULE-12: The agent definition links the two shared sections rather than restating them: `references/spec_quality_guide.md#mutation-check` in the core loop, so the practice that catches a proof passing against broken code is one click away when a proof is written, and `references/purlin_commands.md#pending-migrations`, so a pending migration is handled before anything else. The reference table's `spec_quality_guide.md` row names the mutation check among what that file covers
- RULE-13: The `--no-verify` entry of the NEVER list says what the pre-push hook actually is: Layer 1 of the four enforcement layers, with `references/hard_gates.md` named as where the layers are recorded, and a mode the project chooses and may set to `off`. The entry carries none of `safety gate`, `no legitimate reason` and `defeats proof enforcement`. `hard_gates.md` states that Layer 1 blocks nothing in `off`, that the developer picks the mode with `purlin:init --pre-push` and can bypass any mode with `--no-verify`, and this repository's own config runs `pre_push: off`; an agent told the hook is a safety gate that no one has a legitimate reason to skip has been told the opposite of the reference it is pointed at, and has not been told the thing that actually holds: the mode is the project's decision and a push is not where an agent overturns it
- RULE-14: The `## Skills` heading names both audiences, `optional for the user, mandatory for you`, and the sentence below the table says the same thing once: the user may write specs, code and tests by hand, and the agent invokes the skill because the NEVER list above is its contract and `references/hard_gates.md` records that no hook enforces it. `references/hard_gates.md` carries the qualifier in the same sentence as `optional`, naming the human and the agent, so the two files cannot be read against each other. The heading said `optional tools` and the closing line said `Skills are tools, not gatekeepers`, both true of a person and neither true of the agent, whose own NEVER list forbids writing a spec, running a test or issuing a receipt outside a skill. An unqualified `optional` read by the agent is permission to ignore the contract two screens above it

## Proof

- PROOF-1 (RULE-1): Read the YAML frontmatter of `agents/purlin.md` between its `---` delimiters; verify a `name:` key and a `description:` key are present and that no line of the block declares a `model:` key. Adding `model: claude-sonnet-4-6` back to the frontmatter fails the proof naming that key
- PROOF-2 (RULE-2): Take the body of `agents/purlin.md` from `## Core Loop` to the next `## ` heading; verify it carries all four labels `Do the work`, `sync_status`, `Follow` and `Ship`, that no line of the body matches `^\d+\.`, so nothing in the section is numbered, and that the character offset of the first occurrence of `There is no fixed order` is smaller than the offset of the first of the four labels. Restoring the `1.` to `4.` list fails the numbering assertion naming the lines; moving the no-fixed-order sentence back below the four fails the offset assertion
- PROOF-3 (RULE-3): Grep `agents/purlin.md` for `## Specs`; verify it contains `> Description:`, `## Rules`, `## Proof` within the template
- PROOF-4 (RULE-4): Grep `agents/purlin.md` for `## Proof Markers`; verify it contains `**pytest:`, `**Jest:`, and `**Shell:` framework subsections
- PROOF-5 (RULE-5): Grep `agents/purlin.md` for `## Hard Gates`; verify it lists "Proof coverage" and contains exactly 1 gate
- PROOF-6 (RULE-6): Grep `agents/purlin.md` for `## Implicit Routing`; verify it contains mappings for "test", "status", "drift", "spec", "verify", "engineer", and "QA"
- PROOF-7 (RULE-7): Take the `purlin:<name>` token from every row of the `## Skills` table of `agents/purlin.md` and the directory name of every `skills/*/SKILL.md` on disk; verify the two sets are equal in both directions, that at least ten rows were read so the comparison cannot pass on an empty table, and that every row's purpose cell is non-empty. Verify additionally that no row of the table names a skill in a form the comparison cannot read, so a row whose first cell carries a flag rather than a bare skill name is rejected rather than passing unseen. Deleting a row fails naming the skill whose file has no row; adding a row for `purlin:ghost` fails naming it as a skill with no file; splitting `--update` back into a row of its own fails the unreadable-row assertion
- PROOF-8 (RULE-8): Take the backticked token from the first cell of every row of the `## References` table of `agents/purlin.md`; for each token that looks like a repository path (it carries a `/` and ends `.md`), verify the file exists under the project root. Verify at least eight such paths were read, so the scan cannot pass on an empty table, and that every row's topic cell is longer than five characters. Repointing a row at `references/gone.md` fails naming that path; emptying a topic cell fails naming that row
- PROOF-9 (RULE-9): Grep `agents/purlin.md` for the three-question table; verify it names Proof Design, Proof Integrity and `purlin:verify`, and states that verify owns pass/fail
- PROOF-10 (RULE-10): Grep `agents/purlin.md` for the no-fixed-order statement and the state table; verify all three pathways are named and that the string "build the code first" does not appear anywhere in the file
- PROOF-11 (RULE-11): Grep `agents/purlin.md` implicit routing and verify it names `purlin:audit --design`, carries a design-intent trigger phrase, routes an integrity-target request to `references/audit_criteria.md` by name, and carries no reachability formula of its own (no `(1 - T)` in either dash spelling). Then read that reference's “Reaching a target Integrity score” section and verify it states the ceiling, the reachability condition for a target `T` and the count of proofs that must be rewritten. Restoring the formula to the agent fails the skill half; deleting the reachability line from the reference fails the other
- PROOF-12 (RULE-12): Grep `agents/purlin.md` and verify both literal anchors `spec_quality_guide.md#mutation-check` and `purlin_commands.md#pending-migrations` appear, that the first sits inside the Core Loop section rather than only in the reference table, and that the reference table row for `spec_quality_guide.md` names the mutation check. Removing either anchor fails the proof
- PROOF-13 (RULE-13): Take the line of `agents/purlin.md` whose NEVER entry names `--no-verify`; verify exactly one such line exists, that it carries the literal `references/hard_gates.md` and the words `Layer 1`, and that it carries none of `safety gate`, `no legitimate reason` and `defeats proof enforcement`, naming any phrase it still carries. Then read `references/hard_gates.md` and verify its enforcement-layer table carries a `Layer 1` row naming both `--no-verify` and the `off` mode, so the agent's sentence and the reference it cites say the same thing rather than opposite things. Restoring `The pre-push hook is a safety gate.` fails the proof naming that phrase
- PROOF-14 (RULE-14): Read `agents/purlin.md`; verify its `## Skills` heading is exactly `## Skills (optional for the user, mandatory for you)`, and take the section body to the next `## ` heading. Verify exactly one paragraph of that body outside the table says `optional`, and that it carries all of `optional`, `mandatory`, `user` and `hand`, names `references/hard_gates.md`, and names the NEVER list; verify the retired sentence `Skills are tools, not gatekeepers` appears nowhere in the file. Then read `references/hard_gates.md` and verify exactly one line carries `Skills are optional`, that `mandatory` sits in that line's first sentence rather than a later one, and that the line names `agents/purlin.md`, and that the unqualified `Skills are optional tools, not gatekeepers.` is gone. Restoring either unqualified sentence fails the proof naming the file that carries it
