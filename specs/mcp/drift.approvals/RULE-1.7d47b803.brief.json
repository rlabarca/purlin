{
  "ai_review": "not available",
  "design": null,
  "design_hash": null,
  "feature": "drift",
  "generated_at": "2026-09-15T18:58:09Z",
  "layers": [
    "proof text",
    "test body",
    "test strength",
    "model review"
  ],
  "min_strength": 80,
  "origin": "eng",
  "proof_hash": "b8308f0c51a9138d2377bfdd25dc5751ac68974cb610050933054b86778a8342",
  "proofs": [
    {
      "env": null,
      "findings": [],
      "id": "PROOF-1",
      "text": "Call drift with `since` set to `--output=/tmp/x` while `subprocess.run` is wrapped by a recording spy; verify the returned JSON carries `error` `rejected since`, that the reason names both a commit count and a `YYYY-MM-DD` date, and that the spy recorded zero calls. As a control, call it with `since` `2` and verify a `commits` list comes back and the spy did record calls",
      "tier": "integration"
    },
    {
      "env": null,
      "findings": [],
      "id": "PROOF-2",
      "text": "Call drift over a temporary project with `since` set to `--output=/tmp/x`; verify the parsed report's `error` reads exactly `rejected since`",
      "tier": "integration"
    }
  ],
  "reasons": [],
  "record": ".purlin/records/drift/20260915T185739Z-72c4f1f-ci-windows.json",
  "risk": "high",
  "rule": "RULE-1",
  "rule_hash": "651118fc6c70a8419d090af964b884a6f1bb4f36cc7f7b3f32042987c5a9c2a5",
  "rule_text": "A `since` argument is accepted only as a commit count of digits or a `YYYY-MM-DD` date; any other value is refused with the error `rejected since` and a reason naming both accepted forms, and no subprocess starts",
  "schema": "purlin-brief/1",
  "state": "Approved",
  "test_hash": "64f25bbdb04f5a2f095bfff24981b1ea6597ac02fab50429c4210f874522c115",
  "test_hash_kind": "file",
  "test_strength": null,
  "tests": [
    {
      "body": "def test_hostile_since_is_refused_before_any_subprocess(self, tmp_path):\n        root = str(tmp_path / 'proj')\n        os.makedirs(root)\n        self._repo(root)\n\n        calls = []\n        real_run = subprocess.run\n\n        def spy(args, *rest, **kwargs):\n            calls.append(list(args) if isinstance(args, (list, tuple)) else [args])\n            return real_run(args, *rest, **kwargs)\n\n        purlin_drift.subprocess.run = spy\n        try:\n            refused = json.loads(purlin_drift.drift(root, since='--output=/tmp/x'))\n            refused_calls = list(calls)\n            calls.clear()\n            allowed = json.loads(purlin_drift.drift(root, since='2'))\n            allowed_calls = list(calls)\n        finally:\n            purlin_drift.subprocess.run = real_run\n\n        assert refused.get('error') == 'rejected since', refused\n        reason = refused.get('reason', '')\n        assert 'digits only' in reason and 'YYYY-MM-DD' in reason, reason\n        assert refused_calls == [], (\n            f\"a refused since still reached a subprocess: {refused_calls}\")\n\n        # Control: a valid since does resolve, and does run git.\n        assert 'commits' in allowed, allowed\n        assert allowed_calls, \"the control call ran no subprocess at all\"",
      "file": "dev/test_drift.py",
      "findings": [],
      "name": "test_hostile_since_is_refused_before_any_subprocess",
      "proof": "PROOF-1",
      "reasons": []
    },
    {
      "body": "def test_a_hostile_since_never_reaches_git(self, project):\n        report = json.loads(purlin_drift.drift(project.root,\n                                               since='--output=/tmp/x'))\n        assert report['error'] == 'rejected since'",
      "file": "dev/test_mcp_server.py",
      "findings": [],
      "name": "test_a_hostile_since_never_reaches_git",
      "proof": "PROOF-2",
      "reasons": []
    }
  ],
  "triple_hash": "7d47b803a2f67419bdf751a6572ca7bfd25faa4ce62020227de0b680c48a1ee1",
  "verdict": "ready"
}
