# Feature: sync_status

> Requires: schema_spec_format, schema_proof_format, security_no_dangerous_patterns
> Scope: scripts/mcp/purlin_server.py
> Stack: python/stdlib, json, glob, re, hashlib
> Description: Rule coverage reporting tool. Scans specs and proof files, computes per-feature coverage including own, required, and global anchor rules. Reports actionable directives and computes verification hashes.

## Rules

- RULE-1: Scans `specs/**/*.md` for RULE-N lines, reads `*.proofs-*.json`, and reports per-feature coverage including own rules, required rules (from `> Requires:`), and global anchor rules, with actionable directives
- RULE-2: Status is determined by proof coverage: VERIFIED (all rules proved + passing + non-stale receipt exists), PASSING (all rules proved + passing + no current receipt), PARTIAL (some but not all rules proved, none failing), FAILING (any proof has status FAIL), UNTESTED (zero proofs). Partial coverage never earns PASSING — every rule must have a passing proof
- RULE-3: Warns about unnumbered lines under `## Rules` and missing `## Rules` sections
- RULE-4: Required rules from `> Requires:` specs count toward a feature's coverage total (X/total) with `(required)` label; proofs are looked up under the source spec's feature name
- RULE-5: Detects manual proof staleness by checking if scope files have commits newer than the stamp's commit SHA
- RULE-6: `vhash` is `sha256` over `\x00`-joined segments truncated to 8 hex, and the first segment is the literal version tag `purlin-vhash/2`. After it come `["R", rule_key, sha256(" ".join(rule_text.split()))[:16]]` for every active rule key in sorted order, where a required or global key is `<source>/RULE-N` and its text comes from the source feature's spec; then `["P", feature, id, rule, status, tier, platform or "", test_file, test_name]` for every relevant proof entry, sorted by `(feature, id, rule, tier, platform or "", test_file, test_name)`; then `["M", feature, proof_id, rule, email, date, sha]` for every counted manual stamp. Each clause exists for a failure it prevents: hashing the rule text and not only the key means a reworded rule stales the receipt, and normalising whitespace first means a reflow of the same words does not; binding `test_file` and `test_name` means a rewritten or renamed test stales it, where before a test could be gutted without moving the hash; binding `feature` on every proof segment ends the collision where the same `PROOF-3` under a feature and under a required anchor hashed identically; binding `tier` and `platform` means a result that moves between them is a different hash, while an undeclared platform result (RULE-53) never enters the input at all. The separator is `\x00`, which no field can contain, so no value can forge a field boundary the way a `:` or `,` join allowed. The version tag is first so a version 1 and a version 2 hash of the same state can never collide. The hash is a change detector, not a token: it is not a signature, and it binds nothing about the source code under test
- RULE-7: All proofs count equally toward coverage regardless of proof type (grep-based, behavioral, etc.). Proof quality (STRONG/WEAK/HOLLOW) is assessed by the auditor, not the coverage system
- RULE-8: Detects `> Global: true` metadata on anchor specs and sets the `is_global` flag
- RULE-9: Global anchor rules auto-apply to all non-anchor feature specs without needing `> Requires:`; they appear in coverage with `(global)` label
- RULE-10: Displays rule labels: `(own)` for the feature's own rules, `(required)` for `> Requires:` rules, `(global)` for global anchor rules
- RULE-11: Shows a scope overlap advisory when an anchor's `> Scope:` overlaps a feature's scope but is not in `> Requires:`
- RULE-12: Warns when a `> Requires:` target does not match any known spec name
- RULE-13: Warns when a spec has manual proofs but no `> Scope:` metadata
- RULE-14: Prefers proof files adjacent to their spec over proof files in the specs/ root directory
- RULE-15: Explains receipt staleness cause: distinguishes own rule changes from required/global anchor rule changes
- RULE-16: Warns when `specs/` contains uncommitted changes to `.md` or `.proofs-*.json` files, listing the affected files and recommending a commit before drift or verify
- RULE-17: All proofs are displayed uniformly — grep-based and behavioral proofs both show PASS/FAIL status with no visual distinction in coverage reporting
- RULE-18: sync_status output begins with a summary table showing feature name, coverage fraction, status (VERIFIED/PASSING/FAILING/PARTIAL/UNTESTED), Proof Design and Proof Integrity for all features, sorted by status priority (FAILING, PARTIAL, PASSING, VERIFIED, UNTESTED). Each gauge cell shows a percentage, `not audited` when the feature has no assessments or only a subset, or the unscorable word from that gauge's own vocabulary when every proof is assessed and none is gradeable: `structural` for Design and `excluded` for Integrity. Tokens match the dashboard's exactly so the two surfaces cannot disagree
- RULE-19: sync_status appends an integrity summary after the features VERIFIED line showing the integrity percentage and relative time since last purlin:audit, sourced from the audit cache; when no cache exists, shows a prompt to run purlin:audit
- RULE-20: Reports UNTESTED when a feature has zero behavioral proofs — this replaces the em-dash display for features with no proofs
- RULE-21: Reports PARTIAL (not PASSING) when a feature has some rules proved and passing but not all rules are covered — partial coverage never earns PASSING status
- RULE-22: Anchor detail shows Source URL, Path (if present), and Pinned value (SHA truncated to 7 chars) for anchors with `> Source:` metadata
- RULE-23: Shows unpinned warning for anchors with `> Source:` but no `> Pinned:`
- RULE-24: report-data.js feature entries include `pinned` and `source_path` fields when the anchor spec has `> Pinned:` and `> Path:` metadata
- RULE-25: A gauge is stale when the newest assessment in its own cache is older than 24 hours, computed per gauge and never shared between them. A stale gauge is marked in the summary line; what the mark says is RULE-45's concern
- RULE-26: report-data.js includes audit_summary with integrity, assessment counts, last_audit, last_audit_relative, and stale fields when cache exists; audit_summary is null when no cache exists
- RULE-27: report-data.js per-feature audit data is populated from cache entries matching the feature name
- RULE-28: When the audit cache already contains entries without a `feature` field — written before the writer-side check existed, or hand-edited — the reader tolerates them: they are excluded from per-feature audit data but still counted in the project-wide summary. Rejecting such entries is the writer's job (`static_checks` RULE-33), not the reader's
- RULE-29: Per-feature integrity = (STRONG + MANUAL) / (STRONG + WEAK + HOLLOW + MANUAL) — measures proof quality only; NONE rules do not affect the integrity denominator
- RULE-30: Per-feature integrity counts only proofs cached under that feature name — required/global anchor proofs are counted under the anchor's own feature, not the consuming feature
- RULE-31: Global integrity (project-wide) = (STRONG + MANUAL) / (STRONG + WEAK + HOLLOW + MANUAL) across all features — NONE rules do not affect the denominator. That is the assessed score, over the proofs the audit cache actually holds; RULE-46 defines the coverage-weighted figure the surfaces report from it
- RULE-32: sync_status CLI integrity percentage matches the audit_summary.integrity value in report-data.js
- RULE-33: The integrity formula `(STRONG + MANUAL) / (STRONG + WEAK + HOLLOW + MANUAL)` is consistent across `references/audit_criteria.md`, `skills/audit/SKILL.md`, and this spec; none of these files include NONE in the integrity formula denominator
- RULE-34: Integrity is computed by exactly one function (`_compute_integrity`) — both `_read_audit_summary` and `_build_feature_audit` delegate to it; no other function in purlin_server.py contains the integrity formula
- RULE-35: Feature status is determined by exactly one function (`_determine_status`) — all call sites in purlin_server.py delegate to it; no other function contains the status determination logic
- RULE-36: `_scan_specs` parses `> Stack:` metadata from spec files and includes it in feature info; report-data.js includes `stack` field when present
- RULE-37: Does NOT warn about rule count — rule count scales with feature complexity per `references/spec_quality_guide.md` ("Coverage dimensions")
- RULE-38: When the project's `.mcp.json` defines a `purlin` server whose command or args path points into the Claude plugin cache (`.claude/plugins/cache/`), sync_status prepends a preamble advisory warning that the entry shadows the plugin-bundled MCP server and is pinned to an old plugin version, with a `→ Run: purlin:init --mcp` directive; no advisory when `.mcp.json` is absent, has no `purlin` entry, or the `purlin` entry points elsewhere (e.g., a dev checkout)
- RULE-39: sync_status reports both quality gauges. When a design cache exists it prints `Proof Design: N%`, where N is the coverage-weighted figure of RULE-46 computed from the assessed score PROVABLE / (PROVABLE + LOOSE + UNPROVABLE), excluding STRUCTURAL from both numerator and denominator; when none exists it prints `Proof Design: not measured` rather than omitting the gauge, so a project that has never graded its descriptions is distinguishable from one where the reporter forgot. When no audit cache exists it distinguishes two states: a project where no proof has executed reads `Proof Integrity: no tests yet`, while one with executed proofs reads `No audit data` — reporting them identically made a deliberate spec-first project look neglected. Each printed percentage is followed by a parenthetical carrying its measurement coverage as `N of M measured`, the unweighted assessed score as `P% of those assessed` whenever coverage is incomplete, and that gauge's own relative age, because a score over a thin slice of the project is not a project-wide score, a weighted figure is unreadable without the assessed one it came from, and the two caches age independently
- RULE-40: The coverage table renders every status without breaking its borders — the status column is sized from the longest status word, so `UNTESTED` and `VERIFIED` (8 characters) do not overflow. Every rendered line has identical width
- RULE-41: A rule with no executed proof reports the PROOF ids and descriptions the spec already declares for it, and the suggested `@pytest.mark.proof` marker names the real declared PROOF id rather than the literal placeholder `PROOF-N`
- RULE-42: The next-step directive is chosen from observable state: a rule with no proof whose feature has no existing `> Scope:` files routes to `purlin:build <name>`, while one whose scope files exist routes to `purlin:test`. A feature emits exactly one directive per uncovered rule, never a duplicate
- RULE-43: The design formula `PROVABLE / (PROVABLE + LOOSE + UNPROVABLE)` is consistent across `references/audit_criteria.md`, `skills/audit/SKILL.md`, and this spec, the same three files RULE-33 pins for the integrity formula; none of them include STRUCTURAL in the design denominator
- RULE-44: A fully covered feature whose quality gauges show findings emits a gauge-driven directive rather than `No action needed.` Design findings route to `purlin:spec <name>` because the proof description is the artifact at fault, Integrity WEAK or HOLLOW findings route to `purlin:build <name>` because only test code moves them, and a gauge in state `unmeasured` routes to `purlin:audit`. Design is reported before Integrity, since a high Integrity score over LOOSE descriptions measures an unfalsifiable spec rather than good tests. These directives are additional to the per-rule coverage directives and never replace or duplicate them
- RULE-45: Each gauge's freshness is reported from its own cache timestamp, never from the other's. A stale gauge's directive names the narrowest refresh command (`purlin:audit --design`, `purlin:audit --integrity`, or bare `purlin:audit` when both are stale), because Design grading is deterministic and free while Integrity grading needs test code and costs LLM calls
- RULE-46: Every project-wide gauge figure a surface reports is weighted by measurement coverage as `passing / (gradeable + unmeasured)`, where `unmeasured` is that gauge's population minus what its cache has assessed. An unassessed item is unknown, not passing, so it counts in the denominator; an item excluded from scoring (STRUCTURAL for Design, EXCLUDED for Integrity) counts in neither, being a correct terminal state rather than an unknown. At full coverage the weighted figure equals the assessed score of RULE-29/31/43 exactly, so the gauge does not change meaning as coverage fills in. The assessed score remains available and is reported alongside, because a weighted 2% caused by thin coverage and a 2% caused by bad proofs need different fixes. Per-feature gauges report the assessed score directly, since RULE-18 makes a partially assessed feature read `not audited` rather than a number
- RULE-47: A proof declaring `@on(<platforms>)` that has no result satisfying one of those platforms reports `AWAITING RUNNER`, naming the proof id and the platform. A result satisfies a declared platform D when it comes from a proof file scoped to R (`<feature>.proofs-<tier>@<R>.json`, or the legacy `proofs-windows.json` read as `unit@windows`) and either R equals D, or D is a family id (`windows`, `macos`, `linux`) and the registry entry for R has `os` equal to D; nothing else satisfies, so a family result never satisfies a specific id, an unregistered id satisfies nothing but itself, and an agnostic result satisfies no platform. When several files satisfy the same platform a failing result wins. This is distinct from `NO PROOF`, which means nobody wrote it. It warns and never blocks: the awaiting proofs do not count against coverage, and a rule leaves the coverage denominator, the way a DEFERRED rule does, only when every declared proof for it is awaiting on every platform it declares; a rule with any satisfying result stays in and counts what that result proves, so a proof declared on two platforms and proved on one keeps its rule in the denominator with the other platform reported as awaiting. A required anchor rule is never excluded this way, because it is proved under the anchor's own feature name. The directive names `purlin:test` as the way to close the gap, because that skill owns the remote path that dispatches a runner and pulls back the proofs it commits; it must not describe the work as something the reader has to arrange by hand
- RULE-48: When a declared platform has a satisfying result, sync_status reports when it was last proved and by which runner, read per scoped proof file from `git log -1` on that file and the commit's `Purlin-Runner:` trailer, with the commit's `Purlin-Platform:` trailer cross-checked against the platform id in the filename: a trailer naming a different id is reported as `trailer says <id>` after the runner rather than trusted or hidden. Proof entries carry no timestamp and gain none, because that absence is what makes a CI commit-back idempotent; a missing trailer reports the date with the runner unrecorded rather than omitting the line. One `git log` per file per report run, memoized, so a feature proved on several platforms does not re-read the same commit
- RULE-49: sync_status reports the project's declared `remote_verification` mode (`required`, `optional`, `off`) on its own line after the summary line, and states in the same breath that the field is a declaration and the enforcement is branch protection. The field lives in a file the agent can edit, so presenting it as a gate would misreport where the trust boundary is. The line is printed whenever the mode is not `off` or some proof is actually awaiting a runner, and is silent only for an `off` project with nothing waiting, because a project that never opted in has nothing to report. The `off` branch keys on awaiting rather than on declared: a project whose runner-gated proofs were already proved elsewhere has nothing stuck, so telling it those proofs will stay `AWAITING RUNNER` would be false. When something is waiting it is told how many and pointed at `purlin:test`. A value outside the three is reported as unrecognized and named, never silently treated as `off`: a typo in the mode would otherwise disable the declaration invisibly
- RULE-50: The platform ids a spec's `@on(...)` tags name resolve against a registry built from three built-in family entries (`windows`, `macos`, `linux`, each carrying only its `os`) overlaid by the optional `platforms` object in `.purlin/config.json`, where a config entry of the same id replaces the built-in so a project can attach a runner to a family id. Each config entry is validated: the id matches `[a-z0-9][a-z0-9-]*`; `os` is required and one of `windows`, `macos`, `linux`, with `ios` and `android` rejected as "not yet supported" so the extension point is named; `version` is optional and either `N[.N...]` or `>=N[.N...]`; `distro` is optional and valid only when `os` is `linux`; `arch` is optional and normalised to `x86_64` or `arm64` (accepting `AMD64`, `amd64`, `x64`, `aarch64`); `runner` is an optional object whose `provider` is required and whose only other keys are `runs_on` and `workflow`; `label` is optional; any other key is an error, so a typo cannot vanish into an entry that matches every host of its family. A malformed entry is dropped and reported in the sync_status preamble naming the id and the problem, never silently, and a built-in it would have replaced stays. A proof naming an id that is neither registered nor a family id gets a per-feature advisory naming the proof, the id and the fix (register it, or use a family id); advisories change no verdict
- RULE-51: The host is described by `_detect_host_platform` as `{os, version, distro, arch, id}`: `os` maps `platform.system()` to the family (`Darwin` to `macos`, `Windows` to `windows`, `Linux` to `linux`, anything else lower-cased); `version` is `mac_ver()[0]` on macOS, `win32_ver()[1]` on Windows and the os-release `VERSION_ID` on Linux; `distro` is the os-release `ID` on Linux and empty elsewhere or when os-release is unreadable; `arch` is `machine()` through the same normaliser as the registry; `id` is `PURLIN_PLATFORM` when set, else None. A registry entry is satisfied by the host when `PURLIN_PLATFORM` equals the entry's id, and otherwise only when `os` matches, `distro` matches whenever the entry names one, `arch` matches whenever the entry names one, and the version constraint holds: no constraint always holds, any constraint fails against an unknown host version, `N[.N...]` matches when the host's leading components equal it (`14` matches `14.7.1`, `14.7` does not match `14.8`), and `>=N[.N...]` compares component by component with the shorter side padded with zeros. A family entry carrying only `os` is therefore satisfied by any host of that family
- RULE-52: When any proof in the project declares a platform, sync_status prints a `Platforms:` block after the remote-verification line and before the feature detail: a host line (`Platforms: host <os> <version> <arch>`, plus the `PURLIN_PLATFORM` id when set), then one `local:` line per declared id this host satisfies (RULE-51) naming the proof count and `run with PURLIN_PLATFORM=<id>`, and one `runner:` line per declared id it does not, naming the proof count and how a runner reaches it: `github workflow <name>` from the registry entry's runner block, `no runner configured` when the entry has none, `runner provider <x> is not one purlin:test can dispatch` for any other provider, and `unregistered` for an id that is neither registered nor a family id. Every declared id lands on exactly one line and registry errors keep printing in the preamble (RULE-50), so a platform the project names is never silently dropped. A project with no `@on` prints no `Platforms:` line at all
- RULE-53: A scoped proof file whose platform satisfies none of the platforms its proof declares is an undeclared result: sync_status reports it as `Undeclared platform result`, naming the file and the proof id, and it counts toward nothing. It satisfies no declared platform, so the proof stays `AWAITING RUNNER` there; it moves no rule to PASS or FAIL; it enters no vhash; and the payload lists it under `undeclared` beside `awaiting_runner` rather than mixing the two. A proof that declares no platform never produces an undeclared result, and an agnostic result is never undeclared, because it is not a platform result at all. Silence here would make a file that was ignored look like a file that was never produced
- RULE-54: Every surface that states a feature's verdict reads it from exactly one function, `_feature_verdict(name, info, all_features, all_proofs, global_anchors, project_root, registry)`, which returns the rule entries, the active entries, the proof lookup, the relevant proofs, the active rules' text, the counted manual stamps, `proved`, `has_fail`, the vhash, the awaiting and undeclared lists, the per-platform results, the receipt on disk and whether that receipt is current. The detail report, the summary table, the dashboard payload, the drift snapshot and `dev/issue_receipts.py` all call it and none recomputes any part of it, so `_compute_vhash` and `_active_rule_entries` are each called from exactly one place in `scripts/mcp/purlin_server.py` and that place is this function. Duplication here is not a tidiness question: the issuer built its own active rule set and skipped as unproved every feature whose only uncovered rule was awaiting a runner, while sync_status read the same feature PASSING, and the summary table rebuilt the rule entries a second time inside the loop that had just printed them. A receipt that disagrees with the report that sent you to write it is worse than no receipt

## Proof

- PROOF-1 (RULE-1): Create a spec with rules plus a required anchor; run sync_status; verify coverage includes own and required rules with directives @integration
- PROOF-2 (RULE-2): Create a spec with all behavioral rules proved but no receipt; verify "PASSING" and vhash in output @integration
- PROOF-3 (RULE-3): Create a spec with unnumbered rule; verify WARNING in output @integration
- PROOF-4 (RULE-4): Create spec A with RULE-1 and spec B requiring A; verify B's total includes A's rule with (required) label @integration
- PROOF-5 (RULE-5): Create a spec with manual stamp at old SHA and modified scope file; verify MANUAL PROOF STALE @integration
- PROOF-6 (RULE-6): Call `_compute_vhash` on a fixed rule/proof input and verify it returns the pinned 8 hex literal, so any change to the segment order, the field list or the separator fails here. From that same input verify each binding independently: rewording one rule's text changes the hash; reflowing that text with different whitespace only does not; swapping one proof's `test_name` changes it; the same `PROOF-3` under a feature and under a required anchor hash differently. Then construct two inputs that differ only in where a `:` falls inside two adjacent proof fields, so a `:`-joined payload would be byte-identical for both, and verify the two hashes differ: a value must not be able to forge a field boundary. Verify the hash is 8 lowercase hex characters
- PROOF-7 (RULE-7): Create a spec with all grep-based proofs; verify structural checks are reported separately and feature is not VERIFIED @integration
- PROOF-8 (RULE-8): Create an anchor with `> Global: true`; verify is_global flag in scan result @integration
- PROOF-9 (RULE-9): Create a global anchor and a feature with no Requires; verify global rule appears with (global) label @integration
- PROOF-10 (RULE-10): Create a feature requiring an anchor; verify (own) and (required) labels @integration
- PROOF-11 (RULE-11): Create an anchor with overlapping scope but no Requires; verify scope overlap advisory @integration
- PROOF-12 (RULE-12): Create a feature spec with `> Requires: does_not_exist`; run sync_status; verify warning about unresolved requires target @integration
- PROOF-13 (RULE-13): Create a spec with manual proof but no `> Scope:`; verify warning about staleness detection; create another with `> Scope:` and verify no warning @integration
- PROOF-14 (RULE-14): Create a proof file at specs/ root and a matching proof in a subdirectory next to its spec; run sync_status; verify the subdirectory proof is used @integration
- PROOF-15 (RULE-15): Create a feature requiring an anchor; verify and write receipt; add a rule to the anchor; run sync_status; verify output explains staleness is from the anchor change @integration
- PROOF-16 (RULE-16): Create a temp git repo with a committed spec; modify the spec without committing; run sync_status; verify warning appears with filename; commit the change; run sync_status again; verify no warning @integration
- PROOF-17 (RULE-17): Create a spec with structural-only proofs; verify output shows structural checks with "not counted" label and summary line @integration
- PROOF-18 (RULE-18): Create 3 features (one VERIFIED, one PARTIAL, one with no proofs); run sync_status; verify output starts with summary table, correct ready count, and detail follows @integration
- PROOF-19 (RULE-19): Create a temp project with an audit cache containing STRONG and WEAK entries with timestamps; run sync_status; verify output contains "Integrity: NN%" and "last purlin:audit:" with relative time; delete the cache; run sync_status; verify output contains "No audit data" @integration
- PROOF-20 (RULE-20): Create a spec with 2 behavioral rules but zero proof entries; run sync_status; verify status is "UNTESTED" in the summary table @integration
- PROOF-21 (RULE-21): Create a spec with 3 behavioral rules; write proof file with 2 passing proofs covering RULE-1 and RULE-2 only; run sync_status; verify status is "PARTIAL" not "PASSING" despite both proofs passing @integration
- PROOF-22 (RULE-4): e2e: Create anchor (2 rules), global anchor (1 rule), feature (2 own + Requires); run sync_status; verify 0/5 total @e2e
- PROOF-23 (RULE-10): e2e: Run sync_status on feature with required and global rules; verify (own), (required), (global) labels on rule lines @e2e
- PROOF-24 (RULE-21): e2e: Add proofs for feature's 2 own rules only; run sync_status; verify 2/5 and not VERIFIED @e2e
- PROOF-25 (RULE-2): e2e: Add proofs for all 5 rules (own + required + global); run sync_status; verify 5/5 and PASSING @e2e
- PROOF-26 (RULE-5): e2e: Create spec with @manual stamp at current HEAD; verify PASS with verified date @e2e
- PROOF-27 (RULE-5): e2e: Edit scope file and commit; verify MANUAL PROOF STALE with re-verify directive @e2e
- PROOF-28 (RULE-5): e2e: Re-stamp with new HEAD SHA; verify PASS again @e2e
- PROOF-29 (RULE-6): e2e: Compute vhash and write receipt; verify receipt has correct vhash, commit, rules, proofs @e2e
- PROOF-30 (RULE-6): e2e: Recompute vhash with no changes; verify matches receipt on disk @e2e
- PROOF-31 (RULE-15): e2e: Add rule to spec; recompute vhash; verify mismatch with stale receipt @e2e
- PROOF-32 (RULE-6): e2e: Add proof for new rule; write new receipt; verify different vhash and audit matches @e2e
- PROOF-33 (RULE-2): e2e: Create specs with proofs; verify both PASSING and check_spec_coverage returns correct counts @e2e
- PROOF-34 (RULE-1): e2e: Create anchor with Source/Pinned/Path metadata; run _scan_specs; verify pinned and source_path extracted @e2e
- PROOF-35 (RULE-22): e2e: Create external anchor; run sync_status; verify Source, Path, and Pinned lines in output @e2e
- PROOF-36 (RULE-23): e2e: Create anchor with Source but no Pinned; run sync_status; verify unpinned warning @e2e
- PROOF-37 (RULE-24): e2e: Create external anchor with report=true; verify report-data.js has pinned and source_path fields @e2e
- PROOF-38 (RULE-4): e2e: Create 1 external anchor (2 rules) + feature (1 own); verify coverage 0/3 @e2e
- PROOF-39 (RULE-4): e2e: Create 2 external anchors + feature; verify coverage 0/5 @e2e
- PROOF-40 (RULE-4): e2e: Create 1 external + 1 local anchor + feature; verify coverage 0/4 @e2e
- PROOF-41 (RULE-9): e2e: Create global external anchor; verify auto-applies to all features @e2e
- PROOF-42 (RULE-2): e2e: Coverage progression UNTESTED to PARTIAL to PASSING with external anchor rules @e2e
- PROOF-43 (RULE-19): e2e: Write audit cache with STRONG/WEAK entries; run sync_status; verify Integrity percentage and relative time @e2e
- PROOF-44 (RULE-19): e2e: No audit cache; run sync_status; verify "No audit data" message @e2e
- PROOF-45 (RULE-25): e2e: Write an audit cache timestamped 3 days ago and a design cache timestamped 1 hour ago; verify the summary line marks Integrity stale with a refresh command and leaves Design unmarked, proving the 24-hour threshold is evaluated per gauge against its own newest assessment @e2e
- PROOF-46 (RULE-26): e2e: Write cache with report=true; parse report-data.js; verify audit_summary fields @e2e
- PROOF-47 (RULE-26): e2e: No cache with report=true; verify report-data.js audit_summary is null @e2e
- PROOF-48 (RULE-27): e2e: Write cache for feature login; verify per-feature audit data with correct integrity and findings @e2e
- PROOF-49 (RULE-28): e2e: Seed an audit cache on disk containing one entry with a `feature` field and one without; verify per-feature grouping excludes the no-feature entry while the project-wide summary counts both. Verify write_audit_cache refuses the same entry, so the tolerance is read-side only @e2e
- PROOF-50 (RULE-19): e2e: Write cache then delete; verify sync_status reverts to no-audit state @e2e
- PROOF-51 (RULE-29): e2e: Feature with 5 rules, 3 proved (2 STRONG, 1 WEAK); verify integrity = 2/3 = 67% (quality only, NONE excluded) @e2e
- PROOF-52 (RULE-30): e2e: Feature with 2 own STRONG rules requiring anchor with 3 rules; verify integrity = 2/2 = 100% (anchor proofs counted under anchor) @e2e
- PROOF-53 (RULE-31): e2e: Two features each with 1 NONE rule; write 2 STRONG each; verify global integrity = 4/4 = 100% (NONE excluded) @e2e
- PROOF-54 (RULE-29): e2e: Feature with 4 rules, 2 STRONG proofs + 2 NONE; verify integrity = 100% not 50% @e2e
- PROOF-55 (RULE-32): e2e: Write cache; run sync_status with report=true; verify CLI integrity percentage matches report-data.js audit_summary.integrity @e2e
- PROOF-56 (RULE-33): Grep `references/audit_criteria.md`, `skills/audit/SKILL.md`, and `specs/mcp/sync_status.md` for the integrity formula; verify all three contain `(STRONG + MANUAL) / (STRONG + WEAK + HOLLOW + MANUAL)` and none include NONE in the integrity denominator
- PROOF-57 (RULE-32): e2e: Create isolated project with 5-rule feature, 3 proved (1 STRONG, 1 WEAK, 1 HOLLOW) + 2 NONE; run sync_status with report=true; verify CLI, dashboard, and computed integrity all equal 33% (NONE excluded from denominator) @e2e
- PROOF-58 (RULE-34): Grep purlin_server.py for the integrity computation pattern; verify `_compute_integrity` is the only function containing the formula and that both `_read_audit_summary` and `_build_feature_audit` call it
- PROOF-59 (RULE-35): Grep purlin_server.py for the status determination pattern; verify `_determine_status` is the only function containing the if/elif chain and that all three call sites delegate to it
- PROOF-60 (RULE-34): e2e: Create isolated project with 2 features, each with different audit mixes; run sync_status with report=true; verify per-feature and global integrity in CLI output match report-data.js per-feature audit.integrity and audit_summary.integrity @e2e
- PROOF-61 (RULE-35): e2e: Create isolated project with features in every status (VERIFIED, PASSING, PARTIAL, FAILING, UNTESTED); verify CLI summary table status matches report-data.js per-feature status for all five @e2e
- PROOF-62 (RULE-36): Create spec with `> Stack: python/stdlib, json`; run `_scan_specs`; verify features dict has `stack == "python/stdlib, json"`. Create spec without Stack; verify `stack is None` @integration
- PROOF-63 (RULE-37): Create feature with 3 rules; verify NO rule-count warning. Create feature with 12 rules; verify NO rule-count warning. Create anchor with 2 rules; verify NO warning. Create instruction spec with 3 rules; verify NO warning @integration
- PROOF-68 (RULE-38): Create a temp project with `.mcp.json` defining `mcpServers.purlin` with an args path containing `.claude/plugins/cache/purlin/`; run sync_status; verify the preamble contains the legacy-entry advisory and `→ Run: purlin:init --mcp`. Rewrite the entry with a non-cache path (dev checkout); verify no advisory. Delete `.mcp.json`; verify no advisory @integration
- PROOF-69 (RULE-39): Build a project with specs and no proof files; call sync_status and verify the summary reads `Proof Integrity: no tests yet` rather than `No audit data`. Seed a design cache with 1 PROVABLE, 1 UNPROVABLE and 1 STRUCTURAL entry; call sync_status and verify it reports `Proof Design: 50%`, proving STRUCTURAL is excluded from the denominator. Add a passing proof file and verify the no-audit-data wording returns
- PROOF-70 (RULE-40): Build summary rows covering FAILING, PARTIAL, PASSING, VERIFIED and UNTESTED; render the table and verify every border and data line has the same character width, and that the widest status word fits inside its column
- PROOF-71 (RULE-41): Create a spec declaring PROOF-7 for RULE-1 with no executed proof; call sync_status and verify the output contains the declared proof id and its description, and that the suggested marker names PROOF-7 rather than PROOF-N
- PROOF-72 (RULE-42): Create a spec whose `> Scope:` names a file that does not exist and which has no proofs; call sync_status and verify the directive is `purlin:build <name>` and appears exactly once. Create the scope file and verify the directive becomes `purlin:test`
- PROOF-73 (RULE-18): Build a project with three features, one at 50% design and 100% integrity, one whose assessments are all STRUCTURAL and EXCLUDED, and one with no cache entries; render the summary table and verify the header carries `Design` and `Integrity` columns, that the three rows read `50%`/`100%`, `structural`/`excluded` and `not audited`/`not audited`, and that every rendered line has identical character width
- PROOF-74 (RULE-39): Build a project with a design cache covering 2 of 3 declared descriptions; call sync_status and verify the summary line contains `Proof Design: 100% (2 of 3 measured` followed by that cache's own relative age. Delete the design cache and verify it reads `Proof Design: not measured` rather than omitting the gauge entirely
- PROOF-75 (RULE-43): Grep `references/audit_criteria.md`, `skills/audit/SKILL.md` and `specs/mcp/sync_status.md` for the design formula; verify all three contain `PROVABLE / (PROVABLE + LOOSE + UNPROVABLE)` and none includes STRUCTURAL in the design denominator
- PROOF-76 (RULE-44): Build a fully covered VERIFIED feature and seed a design cache with one LOOSE entry and an audit cache with one WEAK entry for it; call sync_status and verify the feature's directives name `purlin:spec` before `purlin:build` and do not include `No action needed.`; clear both caches and verify the directive becomes `purlin:audit`; seed both caches with only PROVABLE and STRONG entries and verify `No action needed.` returns
- PROOF-77 (RULE-45): Seed a design cache timestamped 1 hour ago and an audit cache timestamped 78 days ago; call sync_status and verify the summary line reports `1 hour ago` against Proof Design and `78 days ago` against Proof Integrity, that only Integrity carries the re-audit prompt, and that the prompt names `purlin:audit --integrity` rather than a bare full audit. Age both caches past 24 hours and verify the prompt becomes bare `purlin:audit`
- PROOF-78 (RULE-46): Seed a design cache assessing 2 of a project's 8 declared descriptions, both PROVABLE, so the assessed score is 100%; run sync_status and verify the printed Proof Design percentage is 25%, not 100%, and that the parenthetical carries `2 of 8 measured` and `100% of those assessed`. Seed the remaining 6 as PROVABLE and verify the printed figure becomes 100% with no `of those assessed` clause, proving the weighted figure collapses onto the assessed score at full coverage @integration
- PROOF-79 (RULE-47): Build a project registering `windows-2022` and `macos-14` whose feature declares PROOF-1 @unit and PROOF-2 `@unit @on(windows-2022)` for two different rules, with only the unit proof executed; run sync_status and verify the output contains `AWAITING RUNNER` naming PROOF-2 and `@on(windows-2022)`, that it does NOT contain `NO PROOF` for the platform-only rule, that the coverage fraction counts 1 of 1 rather than 1 of 2, and that the feature reads PASSING rather than PARTIAL. Add `proofs-unit@windows-2022.json` with PROOF-2 passing and verify the `AWAITING RUNNER` line is gone and coverage reads 2 of 2. Redeclare PROOF-2 as `@on(windows)` and verify the same windows-2022 file still satisfies it through the family rule; replace that file with a `proofs-unit@macos-14.json` result and verify `AWAITING RUNNER` on windows returns with 1 of 1. Call `_result_satisfies` directly and verify `windows-2022` satisfies `windows-2022` and `windows`, that `macos-14` and an agnostic result do not satisfy `windows`, that a `windows` result does not satisfy `windows-2022`, and that an unregistered `ubuntu-24` does not satisfy `linux` @integration
- PROOF-80 (RULE-48): In a temp git repo, commit a `proofs-unit@windows.json` file for a proof declared `@on(windows)` with both a `Purlin-Runner: github-actions/windows-latest` and a `Purlin-Platform: windows` trailer; run sync_status and verify it reports `@on(windows) proved remotely` naming that runner with no `trailer says` clause, and that `_platform_provenance` returns the HEAD commit, the runner and the trailer platform. Assert the on-disk entry's key set is exactly the eight scoped-entry fields, so no timestamp was added to achieve it. Amend the commit with `Purlin-Platform: other` and verify the line reads `trailer says other`; amend with no trailers and verify the line still appears with `runner not recorded` and no `trailer says` @integration
- PROOF-81 (RULE-49): Build a project with one runner-gated proof and run sync_status four times, changing only `remote_verification`: verify `required` and `optional` each print the mode with the declaration/enforcement split naming branch protection, that `off` names the count of proofs awaiting a runner and points at `purlin:test`, and that a garbage value is reported as unrecognized and names the three valid modes rather than being treated as `off`. Then, still under `off`, commit a result at that runner-gated tier and verify the line disappears: a project with nothing stuck must not be told anything will stay stuck. Finally remove the runner-gated proof entirely and verify the line is still absent @integration
- PROOF-82 (RULE-50): Call `_platform_registry` on a config whose `platforms` carries `win-2022` (`os: windows`, `version: ">=10.0.20348"`, `arch: "AMD64"`, a github runner), `windows` (`os: windows` with a runner naming a workflow), `mac-typo` (`os: macos` plus the unknown key `vresion`), `iphone` (`os: ios`), `mac-tilde` (`version: "~14"`) and `Bad_Id` (`os: linux`); verify the registry holds exactly `linux`, `macos`, `windows` and `win-2022`, that `windows` carries the config runner's workflow, that `win-2022` reads `arch: x86_64` with `_id: win-2022`, and that exactly four errors are returned, naming `mac-typo` with `vresion`, `iphone` with `not yet supported`, `mac-tilde` with `~14`, and `Bad_Id`. Write the same config into a project and run sync_status; verify the preamble, before any feature block, prints `Platform registry: 4 entries ignored` followed by each of the four ids and the `edit "platforms" in .purlin/config.json` fix, and that the same project with no `platforms` key prints no `Platform registry` line @integration
- PROOF-83 (RULE-50): Build a project whose proof lines are `PROOF-1 (RULE-1): fcntl path locks @unit` and `PROOF-2 (RULE-2): locks on the declared platform @unit @on(foo)`, with a passing unit result for PROOF-1, no result on foo, and no `platforms` config; run sync_status and verify the output contains `WARNING: PROOF-2 names platform "foo"`, `not a family id (windows, macos, linux)` and the `add it under platforms, or use a family id` fix, and that the feature still reads PASSING. Register `foo` as `{os: linux}` in config and verify the warning is gone; rewrite the tag to `@on(macos)` with no config and verify no `names platform` warning prints @integration
- PROOF-84 (RULE-51): Monkeypatch `platform.system` to `Darwin`, `mac_ver` to `14.7.1` and `machine` to `aarch64` with `PURLIN_PLATFORM` unset; verify `_detect_host_platform` returns `{os: macos, version: 14.7.1, distro: "", arch: arm64, id: None}`, that `_platform_satisfied_by_host` is True for `{os: macos}` and for versions `14`, `14.7`, `14.7.1`, `>=13`, `>=14.7`, `>=14.7.1` and `arch: arm64`, and False for `{os: linux}`, versions `14.8`, `15`, `13`, `>=14.8`, `>=15`, `>=14.7.2` and `arch: x86_64`; verify `_version_satisfies(">=14.0", "14")` is True and `(">=14.0.1", "14")` is False. Patch to `Linux` with os-release `ID: ubuntu`, `VERSION_ID: 24.04`; verify `distro: ubuntu`, `version: 24` and `>=22.04` satisfy while `distro: debian` and `>=24.10` do not, and that an os-release call raising OSError yields empty `distro` and `version` so `version: 24` and `distro: ubuntu` fail while bare `{os: linux}` holds. Patch to `Windows` with `win32_ver` build `10.0.20348` and `machine: AMD64`; verify `arch: x86_64`, that `>=10.0.20348` and prefix `10.0` hold and `>=10.0.20349` does not. Set `PURLIN_PLATFORM=win-2022` on the macOS host with `win-2022` registered as `os: windows`; verify the entry is satisfied and `_host_platform_ids` returns `["macos", "win-2022"]`, then unset it and verify the entry is not satisfied and the ids are `["macos"]` @integration
- PROOF-85 (RULE-52): Monkeypatch the host to macOS 14.7.1 arm64 with `PURLIN_PLATFORM` unset; register `macos-14` (`os: macos`, `version: 14`) and `windows-2022` (`os: windows` with a github runner naming workflow `purlin-windows-proofs.yml`); declare PROOF-1 `@unit @on(macos-14)` and PROOF-2 `@unit @on(windows-2022)`; run sync_status and verify the `Platforms:` block reads `host macos 14.7.1 arm64`, lists `macos-14 (1 proof); run with PURLIN_PLATFORM=macos-14` under `local:` and `windows-2022 (1 proof; github workflow purlin-windows-proofs.yml)` under `runner:`, and sits after the `Remote verification:` line and before the first feature block. Remove the runner block and verify `no runner configured`; register `provider: ado` and verify `runner provider ado is not one purlin:test can dispatch`; declare `@on(foo)` with `foo` unregistered and verify `foo (1 proof; unregistered)` under `runner:`; remove every `@on` and verify no `Platforms:` line prints @integration
- PROOF-86 (RULE-53): Declare PROOF-2 `@unit @on(macos, windows)` for RULE-2 with a passing `proofs-unit@macos.json` result, so the rule stays in the denominator and reads 2 of 2 PASSING, and record the vhash; then write a `proofs-unit@ubuntu-24.json` holding a FAILING PROOF-2 entry; run sync_status and verify the output contains `Undeclared platform result` naming `locking.proofs-unit@ubuntu-24.json` and PROOF-2, that `AWAITING RUNNER` on `@on(windows)` is still reported, that coverage still reads 2 of 2 PASSING with no FAIL and no FAILING, and that the vhash is unchanged; verify the payload's `undeclared` is exactly `[{id: PROOF-2, tier: unit, platform: ubuntu-24}]`, its `awaiting_runner` still holds the windows triple, `platforms` carries no `ubuntu-24` record, and RULE-2 reads PASS with no failing proof object under it. Delete the file and verify the advisory is gone and `undeclared` is empty @integration
- PROOF-88 (RULE-54): Parse `scripts/mcp/purlin_server.py` with `ast` and collect every call site of `_compute_vhash` and of `_active_rule_entries`; verify each is called exactly once in the file and that the enclosing function of both call sites is `_feature_verdict`, and that `_report_feature`, `sync_status`, `_build_report_data` and `_compute_drift` each contain a call to `_feature_verdict`. Then build a temp git project with three features: one ordinary, one declaring `@on(windows-2022)` with no result there so its rule is awaiting-only, and one with a failing proof; write a run marker naming the executed test files and issue receipts through `issue_receipts.main(root, quiet=True)`; verify the ordinary and awaiting-only features are both issued (the awaiting-only one must not be skipped as unproved) and the failing one is not, and that every issued receipt's `vhash` equals the `vhash` `read_report_payload` reports for that same feature @integration
- PROOF-87 (RULE-47): Declare a single rule whose only proof is `@unit @on(macos, windows)` and write a passing `proofs-unit@macos.json` result for it; run sync_status and verify the feature reads PASSING with 1 of 1 rules proved, not PARTIAL and not 0 of 0, that `AWAITING RUNNER: 1 proof declared @on(windows)` is reported, and that no `left the coverage denominator` line prints; verify the payload reports `proved` 1 of `total` 1, `awaiting_runner` holding exactly the windows triple, and per-platform status `PROVED` for macos and `AWAITING` for windows. Add RULE-2 with PROOF-2 `@unit @on(windows)` unproved and PROOF-3 `@unit` passing agnostically; verify the feature reads 2 of 2 PASSING with no denominator line, so a rule leaves the denominator only when every one of its proofs is awaiting @integration
