{
  "ai_review": null,
  "design": null,
  "design_hash": null,
  "feature": "run_script",
  "generated_at": "2026-09-15T18:58:32Z",
  "layers": [
    "proof text",
    "test body",
    "test strength"
  ],
  "min_strength": 80,
  "origin": "eng",
  "proof_hash": "74f41fe5cd4eab369dc3fa987a255b01d3c5025729018179cc1b6e9c2c299861",
  "proofs": [
    {
      "env": null,
      "findings": [],
      "id": "PROOF-58",
      "text": "Run `--all --quick` over a marked pytest test asserting `1 == 2`; verify the output holds `--- pytest output (last 60 lines) ---`, that pytest's own `1 failed` line is under it, and that the heading comes before `Evidence is missing`. Run `--all --quick` over a passing test; verify no such heading is printed",
      "tier": "unit"
    },
    {
      "env": null,
      "findings": [],
      "id": "PROOF-59",
      "text": "Run `--all --record --ci` with no runner temporary directory in the environment, over a project whose pytest test fails; verify `.purlin/runtime/report/logs/pytest.log` exists and holds the arm's own output including the command line the arm ran",
      "tier": "unit"
    }
  ],
  "reasons": [
    "PROOF-58 logic_mirroring",
    "PROOF-58 logic_mirroring"
  ],
  "record": ".purlin/records/run_script/20260915T185741Z-72c4f1f-ci-windows.json",
  "risk": "medium",
  "rule": "RULE-40",
  "rule_hash": "b237e804f963f612f9e903bbb1fb17d5d241c843712983f22e0801f589ecf2f9",
  "rule_text": "An arm that exits non-zero or is killed has the last 60 lines of its own output printed to stdout under `--- <arm> output (last 60 lines) ---` before the missing-evidence lines, and under `--ci` every arm's whole output is published as `logs/<arm>.log` in the dashboard directory",
  "schema": "purlin-brief/1",
  "state": "Approved",
  "test_hash": "f5bbf376b5bfbd1dfb99ffcd4d33df1818a24834126bcb062e0e263575b8cb40",
  "test_hash_kind": "file",
  "test_strength": null,
  "tests": [
    {
      "body": "def test_a_failing_arm_prints_its_tail_before_the_missing_evidence(\n            self, tmp_path):\n        root = _pytest_project(tmp_path, body=(\n            'import pytest\\n\\n'\n            '@pytest.mark.proof(\"feat\", \"PROOF-1\", \"RULE-1\")\\n'\n            'def test_bad():\\n'\n            '    assert 1 == 2\\n'))\n        _spec(root, 'feat')\n        code, output = _run(root, '--all', '--quick')\n        assert code == 1, output\n        heading = '--- pytest output (last 60 lines) ---'\n        assert heading in output, output\n        assert '1 failed' in output, output\n        assert output.index(heading) < output.index('Evidence is missing'), \\\n            output",
      "file": "dev/test_run_script.py",
      "findings": [
        "logic_mirroring"
      ],
      "name": "test_a_failing_arm_prints_its_tail_before_the_missing_evidence",
      "proof": "PROOF-58",
      "reasons": [
        "expected value computed by same function as SUT"
      ]
    },
    {
      "body": "def test_a_failing_arm_prints_its_tail_before_the_missing_evidence(\n            self, tmp_path):\n        root = _pytest_project(tmp_path, body=(\n            'import pytest\\n\\n'\n            '@pytest.mark.proof(\"feat\", \"PROOF-1\", \"RULE-1\")\\n'\n            'def test_bad():\\n'\n            '    assert 1 == 2\\n'))\n        _spec(root, 'feat')\n        code, output = _run(root, '--all', '--quick')\n        assert code == 1, output\n        heading = '--- pytest output (last 60 lines) ---'\n        assert heading in output, output\n        assert '1 failed' in output, output\n        assert output.index(heading) < output.index('Evidence is missing'), \\\n            output",
      "file": "dev/test_run_script.py",
      "findings": [
        "logic_mirroring"
      ],
      "name": "test_an_arm_that_passed_prints_no_tail",
      "proof": "PROOF-58",
      "reasons": [
        "expected value computed by same function as SUT"
      ]
    },
    {
      "body": "def test_ci_publishes_each_arms_whole_output(\n            self, tmp_path, record_run, monkeypatch, capsys):\n        # Off a runner the dashboard goes into the project, which is where the\n        # test reads it back. On a runner both variables are set, and the\n        # published directory would be the runner's own.\n        for variable in ('RUNNER_TEMP', 'AGENT_TEMPDIRECTORY'):\n            monkeypatch.delenv(variable, raising=False)\n        root = _pytest_project(tmp_path, body=(\n            'import pytest\\n\\n'\n            '@pytest.mark.proof(\"feat\", \"PROOF-1\", \"RULE-1\")\\n'\n            'def test_bad():\\n'\n            '    assert 1 == 2\\n'))\n        _spec(root, 'feat')\n        record_run(root, '--all', '--ci')\n        capsys.readouterr()\n        published = root / '.purlin' / 'runtime' / 'report' / 'logs'\n        text = (published / 'pytest.log').read_text(encoding='utf-8')\n        assert '-m pytest' in text, text\n        assert '1 failed' in text, text",
      "file": "dev/test_run_script.py",
      "findings": [],
      "name": "test_ci_publishes_each_arms_whole_output",
      "proof": "PROOF-59",
      "reasons": []
    }
  ],
  "triple_hash": "c4139f032f62f1c6382302cf8dc87d7e9db1314400fb9c8e77f44d4f3bf1be5b",
  "verdict": "needs a human"
}
