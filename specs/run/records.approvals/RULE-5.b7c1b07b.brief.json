{
  "ai_review": "not available",
  "design": null,
  "design_hash": null,
  "feature": "records",
  "generated_at": "2026-09-15T16:21:02Z",
  "layers": [
    "proof text",
    "test body",
    "test strength",
    "model review"
  ],
  "min_strength": 80,
  "origin": "eng",
  "proof_hash": "a8b95ab3044549aded4fd812c4c138d6058a84c5544a5bfd6d0077182c37f340",
  "proofs": [
    {
      "env": null,
      "findings": [
        "happy_path_only"
      ],
      "id": "PROOF-5",
      "text": "With `GITHUB_REPOSITORY` and `GITHUB_TOKEN` set and `urlopen` standing in for the API, call `commit_records(identity=\"ci\")`; verify the calls are GET ref, GET commit, POST trees, POST commits, PATCH refs in that order with no POST to the blob endpoint at all, that the commit body carries neither `author` nor `committer`, and that the tree entry carries the record path, the file permission `100644` and the record's own text rather than a blob id. Call it again with a record path, a `.ci.json` approval and a `.brief.json` brief beside a spec; verify the tree lists all three in the order they were handed over, that the two outside `.purlin/records/` are exactly the approval and the brief, that each entry carries its own text, and that still no blob request was made. Call it once more with a file holding the bytes `89 50 4e 47 0d 0a 1a 0a ff fe`; verify exactly one blob request, that its `encoding` is `base64` and its content decodes to those bytes, and that its tree entry carries that blob's id and no text",
      "tier": "unit"
    }
  ],
  "reasons": [
    "PROOF-5 happy_path_only: No proof of this rule names a rejection, an error or a boundary."
  ],
  "record": ".purlin/records/records/20260915T162020Z-ed20c8b-ci-windows.json",
  "risk": "high",
  "rule": "RULE-5",
  "rule_hash": "753bd6c8db8b6c82697b40cf7828bfd0e64ca2030c788af85a09b19f347e86fd",
  "rule_text": "A CI commit on GitHub is made through the git host's REST API as one tree request, then commit, then ref update, where the single tree carries the text of every path the run handed over including the approvals and the briefs that sit beside a spec, only a path whose bytes are not UTF-8 costs a blob request of its own, and no `author` and no `committer` field is sent, so the git host signs the commit with its own key",
  "schema": "purlin-brief/1",
  "state": "Reviewed",
  "test_hash": "771992699504d61546cf730d2719cb7d19c365741acf131766837a445697fbc1",
  "test_hash_kind": "file",
  "test_strength": null,
  "tests": [
    {
      "body": "def test_the_ci_commit_is_one_tree_then_commit_then_ref(project, github_env,\n                                                        monkeypatch):\n    \"\"\"The file's text travels in the tree request, so it costs no request.\n\n    A run that writes a record, its auto-approvals and several hundred briefs\n    would otherwise make one content-creating request per file, which is what\n    the git host's limit on those counts.\n    \"\"\"\n    host = FakeHost()\n    monkeypatch.setattr(urllib.request, 'urlopen', host)\n    path = records_module.write_record(project, record(), 'ci', 'linux')\n\n    sha = records_module.commit_records(project, [path], 'ci',\n                                        'purlin: record for 4f1c2ab')\n\n    assert sha == 'c' * 40\n    order = [url.rsplit('/git/', 1)[-1].split('?')[0] for url in host.urls()]\n    assert order == ['ref/heads/main', 'commits/' + '1' * 40,\n                     'trees', 'commits', 'refs/heads/main']\n    assert [url for url in host.urls() if url.endswith('/blobs')] == []",
      "file": "dev/test_records.py",
      "findings": [],
      "name": "test_a_file_that_is_not_text_gets_a_blob_of_its_own",
      "proof": "PROOF-5",
      "reasons": []
    },
    {
      "body": "def test_the_ci_commit_is_one_tree_then_commit_then_ref(project, github_env,\n                                                        monkeypatch):\n    \"\"\"The file's text travels in the tree request, so it costs no request.\n\n    A run that writes a record, its auto-approvals and several hundred briefs\n    would otherwise make one content-creating request per file, which is what\n    the git host's limit on those counts.\n    \"\"\"\n    host = FakeHost()\n    monkeypatch.setattr(urllib.request, 'urlopen', host)\n    path = records_module.write_record(project, record(), 'ci', 'linux')\n\n    sha = records_module.commit_records(project, [path], 'ci',\n                                        'purlin: record for 4f1c2ab')\n\n    assert sha == 'c' * 40\n    order = [url.rsplit('/git/', 1)[-1].split('?')[0] for url in host.urls()]\n    assert order == ['ref/heads/main', 'commits/' + '1' * 40,\n                     'trees', 'commits', 'refs/heads/main']\n    assert [url for url in host.urls() if url.endswith('/blobs')] == []",
      "file": "dev/test_records.py",
      "findings": [],
      "name": "test_the_ci_commit_carries_a_path_outside_the_records_directory",
      "proof": "PROOF-5",
      "reasons": []
    },
    {
      "body": "def test_the_ci_commit_is_one_tree_then_commit_then_ref(project, github_env,\n                                                        monkeypatch):\n    \"\"\"The file's text travels in the tree request, so it costs no request.\n\n    A run that writes a record, its auto-approvals and several hundred briefs\n    would otherwise make one content-creating request per file, which is what\n    the git host's limit on those counts.\n    \"\"\"\n    host = FakeHost()\n    monkeypatch.setattr(urllib.request, 'urlopen', host)\n    path = records_module.write_record(project, record(), 'ci', 'linux')\n\n    sha = records_module.commit_records(project, [path], 'ci',\n                                        'purlin: record for 4f1c2ab')\n\n    assert sha == 'c' * 40\n    order = [url.rsplit('/git/', 1)[-1].split('?')[0] for url in host.urls()]\n    assert order == ['ref/heads/main', 'commits/' + '1' * 40,\n                     'trees', 'commits', 'refs/heads/main']\n    assert [url for url in host.urls() if url.endswith('/blobs')] == []",
      "file": "dev/test_records.py",
      "findings": [],
      "name": "test_the_ci_commit_is_one_tree_then_commit_then_ref",
      "proof": "PROOF-5",
      "reasons": []
    },
    {
      "body": "def test_the_ci_commit_is_one_tree_then_commit_then_ref(project, github_env,\n                                                        monkeypatch):\n    \"\"\"The file's text travels in the tree request, so it costs no request.\n\n    A run that writes a record, its auto-approvals and several hundred briefs\n    would otherwise make one content-creating request per file, which is what\n    the git host's limit on those counts.\n    \"\"\"\n    host = FakeHost()\n    monkeypatch.setattr(urllib.request, 'urlopen', host)\n    path = records_module.write_record(project, record(), 'ci', 'linux')\n\n    sha = records_module.commit_records(project, [path], 'ci',\n                                        'purlin: record for 4f1c2ab')\n\n    assert sha == 'c' * 40\n    order = [url.rsplit('/git/', 1)[-1].split('?')[0] for url in host.urls()]\n    assert order == ['ref/heads/main', 'commits/' + '1' * 40,\n                     'trees', 'commits', 'refs/heads/main']\n    assert [url for url in host.urls() if url.endswith('/blobs')] == []",
      "file": "dev/test_records.py",
      "findings": [],
      "name": "test_the_ci_commit_sends_no_author_and_no_committer",
      "proof": "PROOF-5",
      "reasons": []
    },
    {
      "body": "def test_the_ci_commit_is_one_tree_then_commit_then_ref(project, github_env,\n                                                        monkeypatch):\n    \"\"\"The file's text travels in the tree request, so it costs no request.\n\n    A run that writes a record, its auto-approvals and several hundred briefs\n    would otherwise make one content-creating request per file, which is what\n    the git host's limit on those counts.\n    \"\"\"\n    host = FakeHost()\n    monkeypatch.setattr(urllib.request, 'urlopen', host)\n    path = records_module.write_record(project, record(), 'ci', 'linux')\n\n    sha = records_module.commit_records(project, [path], 'ci',\n                                        'purlin: record for 4f1c2ab')\n\n    assert sha == 'c' * 40\n    order = [url.rsplit('/git/', 1)[-1].split('?')[0] for url in host.urls()]\n    assert order == ['ref/heads/main', 'commits/' + '1' * 40,\n                     'trees', 'commits', 'refs/heads/main']\n    assert [url for url in host.urls() if url.endswith('/blobs')] == []",
      "file": "dev/test_records.py",
      "findings": [],
      "name": "test_the_tree_entry_carries_the_file_and_its_permission",
      "proof": "PROOF-5",
      "reasons": []
    }
  ],
  "triple_hash": "b7c1b07be059e9805373737a0dee41da6d8bb2a39aa769c7546e1a81e3db7473",
  "verdict": "add a case"
}
