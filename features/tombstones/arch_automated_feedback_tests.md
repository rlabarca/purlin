# TOMBSTONE: arch_automated_feedback_tests

**Retired:** 2026-03-18
**Reason:** Replaced by `features/arch_testing.md`. The AFT type taxonomy (Web, Agent, API, LLM, iOS, Android) has been removed. Only the core testing pattern, execution tiers, assertion quality invariant, and behavioral constraints survive in the successor.

## Files to Delete

- `features/arch_automated_feedback_tests.md` -- entire file (already deleted by Architect)

## Dependencies to Check

- `tools/critic/critic.py` -- may reference "Implementation Family" table parsing or AFT-specific type detection from this anchor
- `tools/critic/test_critic.py` -- test data may reference `arch_automated_feedback_tests.md`
- `dev/setup_fixture_repo.sh` -- fixture tags may reference the old anchor name
- `.purlin/cache/dependency_graph.json` -- will be regenerated by the Critic automatically

## Context

This anchor node defined the Automated Feedback Test (AFT) pattern with a six-type taxonomy (Web, Agent, API, LLM, iOS, Android). Only Web and Agent were implemented; the other four were speculative. The refactoring collapses the taxonomy into a simpler model:

- The core testing pattern, execution tiers, assertion quality invariant, assertion modification discipline, and behavioral constraints are preserved in `features/arch_testing.md`.
- The "AFT Spot" tier is renamed to "Spot Check" and references `/pl-web-test` instead of `/pl-aft-web`.
- The Implementation Family table and naming convention table are removed entirely.
- Feature metadata changes: `> AFT Web:` -> `> Web Test:`, `> AFT Start:` -> `> Web Start:`, `> AFT Agent:` removed.

**Successor:** `features/arch_testing.md`
