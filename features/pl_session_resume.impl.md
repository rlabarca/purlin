# Implementation Notes: /pl-resume Session Checkpoint and Recovery

## Active Deviations

| Spec says | Implementation does | Tag | PM status |
|-----------|-------------------|-----|-----------|
| (see prose) | Step 7's final context guard counter reset was counterproductive. The restore flow consumes ~25-30 turns of real context during state gathering. Resetting the counter at the end made the guard lie about remaining budget. The initial reset in Step 0 is correct (clears stale state from the previous session). **Acknowledged — spec updated to remove the final counter reset from Step 7.** | DISCOVERY | RESOLVED |
| (see prose) | Spec audit (e55c871) restored Step 0 heading to "Context Guard Counter Reset" with instruction to reset the turn counter. The context guard mechanism was previously removed from the codebase (no counter files, no hooks directory, no context_guard.sh). The instruction is effectively a no-op since there is no counter to reset, but the command file now matches the spec exactly. If a new context guard mechanism is introduced, Step 0 will be ready. (Severity: LOW) **Acknowledged -- no-op is acceptable; Step 0 is a placeholder for future context guard reintroduction.** | DISCOVERY | RESOLVED |

**[CLARIFICATION]** The skill is implemented as a Claude command file (`.claude/commands/pl-resume.md`) following the same pattern as other shared skills (`/pl-status`, `/pl-find`, `/pl-whats-different`). The command file contains step-by-step instructions for the agent to follow — no Python tool is needed since all operations (file I/O, git commands, reading instruction files) use the agent's existing tool set. (Severity: INFO)

**[CLARIFICATION]** The checkpoint format uses Markdown with `**Label:**` syntax for field parsing. The test suite validates field extraction using regex matching on this pattern, consistent with how agents will parse the file during restore. (Severity: INFO)

**[CLARIFICATION]** Tests verify the underlying behaviors (file I/O, format validation, argument parsing, file existence) rather than end-to-end agent execution, matching the test pattern established by `tests/pl_whats_different/test_command.py`. Agent command files are instruction documents executed by the LLM — the automated tests verify the preconditions and infrastructure those instructions depend on. (Severity: INFO)

**[DISCOVERY]** Step 7's final context guard counter reset was counterproductive. The restore flow consumes ~25-30 turns of real context during state gathering. Resetting the counter at the end made the guard lie about remaining budget. The initial reset in Step 0 is correct (clears stale state from the previous session). **Acknowledged — spec updated to remove the final counter reset from Step 7.** (Severity: HIGH)

**[DISCOVERY]** Spec audit (e55c871) restored Step 0 heading to "Context Guard Counter Reset" with instruction to reset the turn counter. The context guard mechanism was previously removed from the codebase (no counter files, no hooks directory, no context_guard.sh). The instruction is effectively a no-op since there is no counter to reset, but the command file now matches the spec exactly. If a new context guard mechanism is introduced, Step 0 will be ready. (Severity: LOW) **Acknowledged -- no-op is acceptable; Step 0 is a placeholder for future context guard reintroduction.**

*   **Startup Briefing Integration (2026-03-18):** Spec Section 2.3.5 updated to use `tools/cdd/scan.sh --startup <role>` instead of multi-step state gathering. The command file (`.claude/commands/pl-resume.md`) was updated by PM mode to reference the startup briefing mechanism. Tests updated: 4 command file content checks fixed for new wording (`config.local.json` → briefing config, `verification_effort` → `testing features`, `anchor node preload` → `anchor constraints`). 5 new integration tests added verifying `--startup` flag wiring in scan.sh, role-specific extension references in command file, and `delivery_plan_state`/`phasing_recommended` field references.

**[CLARIFICATION]** BUG fix (2026-03-24): Removed QA-only extra fields (`Regression Authoring`, `Last Authored Feature`) from the skill file's QA Context checkpoint template -- these fields were not specified in spec Section 2.2.3. Expanded Engineer Delivery Plan template to include "completed: <features>" sub-field to match spec Section 2.2.2's "features completed this phase" requirement. Also fixed two tests broken by the `submodule_command_path_resolution` feature (commit 4d407392) which changed `tools/` paths to `${TOOLS_ROOT}/` in command files: `test_critic_report_path_convention` (updated to verify startup briefing reference instead of /pl-status file existence) and `test_command_file_references_startup_flag` (updated assertion to match `${TOOLS_ROOT}` prefix). (Severity: INFO)

