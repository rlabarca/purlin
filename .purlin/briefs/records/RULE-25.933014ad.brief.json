{
  "ai_review": null,
  "design": null,
  "design_hash": null,
  "feature": "records",
  "generated_at": "2026-09-16T22:43:07Z",
  "layers": [
    "proof text",
    "test body",
    "test strength"
  ],
  "min_strength": 80,
  "observations": [],
  "origin": "eng",
  "proof_hash": "19818ddb434261e01f6917fa32b11d2f96f62a408c27f6b2aab9e1541d5739ab",
  "proofs": [
    {
      "env": null,
      "findings": [],
      "id": "PROOF-28",
      "text": "Render the review list of a payload listing `billing RULE-2` low unsigned, `login RULE-10` high needing a person, `login RULE-3` high stale and `auth RULE-1` medium unsigned; verify the header reads `Review list: 4 rules need a person.` and the lines follow in the order `login RULE-3`, `login RULE-10`, `auth RULE-1`, `billing RULE-2`, each carrying its risk, its blocking cell and its `why` tokens. Render a payload with an empty list and verify it reads `Review list: no rule needs a person.`",
      "tier": "unit"
    },
    {
      "env": null,
      "findings": [],
      "id": "PROOF-29",
      "text": "Scan a local repository at the gate `strong` whose `RULE-1` is tagged `[risk: high]` and whose only proof is `@manual`, so no machine can settle it; verify the output carries a `Review list:` line after the rollup and a line naming `high`, `greeting RULE-1`, the cell `strong` and the token `manual`",
      "tier": "integration"
    }
  ],
  "record": ".purlin/records/records/20260916T224242Z-e8bd239-ci-windows.json",
  "risk": "medium",
  "rule": "RULE-25",
  "rule_hash": "a631fdd6d351837846ef2dc48232558ca19a17a6c38b1b31a892b3740355f006",
  "rule_text": "`scan.py` prints the review list after the rollup, one line per rule naming its risk, its feature and rule id, the cell that blocks it and the `why` tokens that put it there, high risk first and a stale or held rule first within a risk, and says so in one line when no rule needs a person",
  "schema": "purlin-brief/2",
  "settled": null,
  "test_hash": "e7d0dc941fa7bdb51493bb81b379b93fd00f48ae03f0798fbe3c25c7731e81c8",
  "test_hash_kind": "file",
  "test_strength": null,
  "tests": [
    {
      "body": "def test_one_rule_reads_in_the_singular():\n    text = scan_module.review_list_text(\n        _payload([('login', 'RULE-3', 'high', 'signed', ['held'])]))\n    assert text.splitlines()[0] == 'Review list: 1 rule needs a person.'",
      "file": "dev/test_scan_review_list.py",
      "findings": [],
      "name": "test_one_rule_reads_in_the_singular",
      "proof": "PROOF-28",
      "reasons": []
    },
    {
      "body": "def test_the_list_is_one_line_per_rule_in_review_order():\n    payload = _payload(\n        [('billing', 'RULE-2', 'low', 'signed', ['unsigned']),\n         ('login', 'RULE-10', 'high', 'strong', ['needs a person']),\n         ('login', 'RULE-3', 'high', 'signed', ['stale']),\n         ('auth', 'RULE-1', 'medium', 'signed', ['unsigned'])])\n    lines = scan_module.review_list_text(payload).splitlines()\n\n    assert lines[0] == 'Review list: 4 rules need a person.'\n    order = [line.split()[1] + ' ' + line.split()[2] for line in lines[1:]]\n    assert order == ['login RULE-3', 'login RULE-10', 'auth RULE-1',\n                     'billing RULE-2'], lines\n    assert lines[1].split()[:4] == ['high', 'login', 'RULE-3', 'signed']\n    assert lines[1].endswith('stale'), lines[1]\n    assert lines[2].split()[:4] == ['high', 'login', 'RULE-10', 'strong']\n    assert lines[2].endswith('needs a person'), lines[2]\n    assert lines[3].split()[:4] == ['medium', 'auth', 'RULE-1', 'signed']\n    assert lines[4].endswith('unsigned'), lines[4]\n    assert scan_module.review_list_text(_payload([])) == \\\n        'Review list: no rule needs a person.'",
      "file": "dev/test_scan_review_list.py",
      "findings": [],
      "name": "test_the_list_is_one_line_per_rule_in_review_order",
      "proof": "PROOF-28",
      "reasons": []
    },
    {
      "body": "def test_the_scan_prints_the_list_after_the_rollup(tmp_path):\n    \"\"\"A `@manual` proof is the one thing no machine can settle.\n\n    The gate is `strong`, so the strong cell exists, and the rule lands on\n    the review list reading `needs a person` with the token `manual`.\n    \"\"\"\n    source = str(tmp_path / 'source')\n    make_project(source)\n    spec = os.path.join(source, 'specs', 'core', 'greeting.md')\n    with open(spec, 'w', encoding='utf-8') as handle:\n        handle.write(SPEC.replace('returns `Hello, <name>!`',\n                                  'returns `Hello, <name>!` [risk: high]')\n                         .replace('verify `Hello, Ada!` @unit',\n                                  'verify `Hello, Ada!` @manual'))\n    git(source, 'commit', '--quiet', '-am', 'a high-risk rule proved by hand')\n    bare = str(tmp_path / 'origin.git')\n    git(tmp_path, 'init', '--bare', '--quiet', '-b', 'main', bare)\n    git(source, 'push', '--quiet', bare, 'main')\n\n    text = scan_module.scan(bare, 'main')\n\n    assert 'Review list:' in text\n    assert text.index('Review list:') > text.index('rules.')\n    rows = [line.split() for line in text.splitlines()\n            if 'greeting RULE-1' in line]\n    assert rows and rows[0][:3] == ['high', 'greeting', 'RULE-1'], text\n    assert rows[0][3] == 'strong', text\n    assert rows[0][4] == 'manual', text",
      "file": "dev/test_scan_review_list.py",
      "findings": [
        "logic_mirroring"
      ],
      "name": "test_the_scan_prints_the_list_after_the_rollup",
      "proof": "PROOF-29",
      "reasons": [
        "expected value computed by same function as SUT"
      ]
    }
  ],
  "triple_hash": "933014ad260040c86f6cca2cfaafdad2dcd8502ddffd67f5d45ca788eec2fc87"
}
