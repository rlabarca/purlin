{
  "ai_review": "not available",
  "design": null,
  "design_hash": null,
  "feature": "update",
  "generated_at": "2026-09-16T22:37:03Z",
  "layers": [
    "proof text",
    "test body",
    "test strength",
    "model review"
  ],
  "min_strength": 80,
  "observations": [],
  "origin": "eng",
  "proof_hash": "96277e7fe80a205fe8fa2844fb388c373d89a7a9e7a94421ba14498975c9823e",
  "proofs": [
    {
      "env": null,
      "findings": [
        "happy_path_only"
      ],
      "id": "PROOF-18",
      "text": "Run the update with `--yes` on the v0.9.5 fixture and verify `git log --format=%s` holds exactly two lines, the newer one starting `chore(update): migrate to <VERSION> (` and naming every id applied; verify every remaining line of `git status --porcelain` is an untracked `.bak` file. Decline everything on a fresh copy and verify `HEAD` is where it was",
      "tier": "unit"
    }
  ],
  "record": ".purlin/records/update/20260916T223615Z-3aa5d77-ci-linux.json",
  "risk": "high",
  "rule": "RULE-18",
  "rule_hash": "36126c433f3f5779054ff44742cf711b2cdc7e82c3937ab5f7859de361ee6dc8",
  "rule_text": "One commit carries the whole run, its subject naming the release and every migration id applied; a run that applies nothing writes no commit; nothing is left uncommitted afterwards but the backup copies",
  "schema": "purlin-brief/2",
  "settled": null,
  "test_hash": "410d000b60c4af56aad036564caca1fc6238426efcc98078688338d052c4e775",
  "test_hash_kind": "file",
  "test_strength": null,
  "tests": [
    {
      "body": "def test_a_run_that_applies_nothing_writes_no_commit(tmp_path, capsys,\n                                                     monkeypatch):\n    root = _project(tmp_path, V095)\n    head = _git(root, 'rev-parse', 'HEAD').stdout.strip()\n    _answers(monkeypatch, default='n')\n    _apply(root, argv=())\n    capsys.readouterr()\n    assert _git(root, 'rev-parse', 'HEAD').stdout.strip() == head",
      "file": "dev/test_init_update.py",
      "findings": [
        "logic_mirroring"
      ],
      "name": "test_a_run_that_applies_nothing_writes_no_commit",
      "proof": "PROOF-18",
      "reasons": [
        "expected value computed by same function as SUT"
      ]
    },
    {
      "body": "def test_nothing_is_left_uncommitted_but_the_backups(tmp_path, layout):\n    root = _project(tmp_path, layout)\n    _apply(root)\n    left = _git(root, 'status', '--porcelain').stdout.splitlines()\n    assert left, 'the backups should still be sitting there'\n    for line in left:\n        assert line.startswith('?? '), line\n        assert line.endswith('.bak'), line",
      "file": "dev/test_init_update.py",
      "findings": [],
      "name": "test_nothing_is_left_uncommitted_but_the_backups[upgrade-0.9.5]",
      "proof": "PROOF-18",
      "reasons": []
    },
    {
      "body": "def test_one_commit_carries_every_migration_id(tmp_path, layout):\n    root = _project(tmp_path, layout)\n    applied = _ids(root)\n    _apply(root)\n    log = _git(root, 'log', '--format=%s').stdout.splitlines()\n    assert len(log) == 2, log\n    subject = log[0]\n    assert subject.startswith('chore(update): migrate to %s (' % VERSION)\n    for migration_id in applied:\n        assert migration_id in subject",
      "file": "dev/test_init_update.py",
      "findings": [],
      "name": "test_one_commit_carries_every_migration_id[upgrade-0.9.5]",
      "proof": "PROOF-18",
      "reasons": []
    },
    {
      "body": "def test_nothing_is_left_uncommitted_but_the_backups(tmp_path, layout):\n    root = _project(tmp_path, layout)\n    _apply(root)\n    left = _git(root, 'status', '--porcelain').stdout.splitlines()\n    assert left, 'the backups should still be sitting there'\n    for line in left:\n        assert line.startswith('?? '), line\n        assert line.endswith('.bak'), line",
      "file": "dev/test_init_update.py",
      "findings": [],
      "name": "test_nothing_is_left_uncommitted_but_the_backups[upgrade-0.10-dev]",
      "proof": "PROOF-18",
      "reasons": []
    },
    {
      "body": "def test_one_commit_carries_every_migration_id(tmp_path, layout):\n    root = _project(tmp_path, layout)\n    applied = _ids(root)\n    _apply(root)\n    log = _git(root, 'log', '--format=%s').stdout.splitlines()\n    assert len(log) == 2, log\n    subject = log[0]\n    assert subject.startswith('chore(update): migrate to %s (' % VERSION)\n    for migration_id in applied:\n        assert migration_id in subject",
      "file": "dev/test_init_update.py",
      "findings": [],
      "name": "test_one_commit_carries_every_migration_id[upgrade-0.10-dev]",
      "proof": "PROOF-18",
      "reasons": []
    }
  ],
  "triple_hash": "1478135665f794809d9787f8f9016009199b2b3e6a6830f018f57a7385474e92"
}
