{
  "ai_review": null,
  "design": null,
  "design_hash": null,
  "feature": "scaffold",
  "generated_at": "2026-09-16T22:50:53Z",
  "layers": [
    "proof text",
    "test body",
    "test strength"
  ],
  "min_strength": 80,
  "observations": [],
  "origin": "eng",
  "proof_hash": "aa60e5704acf76cc752a109e814c6e7945a3590ab2979cad09d977e697dd652a",
  "proofs": [
    {
      "env": null,
      "findings": [],
      "id": "PROOF-38",
      "text": "Run init on a pytest project, write `pytest,jest,shell,vitest` into its `test_framework`, run `--gate strong` and verify the value reads `pytest,shell`, that the output holds `dropped jest from test_framework: nothing in the tree runs it` and the same line for `vitest`, and that neither `jest.config.js` nor `vitest.config.ts` exists. Write a `package.json` into a second pytest project, write `pytest,jest,shell` into its `test_framework`, run `--gate strong` and verify the value is unchanged and `dropped` is absent",
      "tier": "unit"
    }
  ],
  "record": ".purlin/records/scaffold/20260916T225002Z-3aa5d77-ci-windows.json",
  "risk": "medium",
  "rule": "RULE-38",
  "rule_hash": "ce9e78de550ff47aa1580b9fff9bc5c4ff1f839fbbd0bf729ef8b4a2d313db92",
  "rule_text": "A `test_framework` name the config carries that detection does not find and the tree carries no wiring for is dropped and the drop is printed, so no runner is wired for a language the project does not have; a name the tree does carry is kept",
  "schema": "purlin-brief/2",
  "settled": null,
  "test_hash": "1b0abc1b12d6541857dd21254f523295b0f11273fd62d78b453fd5ffb31dc3f3",
  "test_hash_kind": "file",
  "test_strength": null,
  "tests": [
    {
      "body": "def test_a_framework_the_tree_carries_survives_a_gate_change(self, project):\n        \"\"\"Wiring the tree carries outranks detection, the stricter test.\"\"\"\n        write(project.path('package.json'), '{\"name\": \"app\"}\\n')\n        project.run('--gate', 'passed')\n        written = project.config()\n        written['test_framework'] = 'pytest,jest,shell'\n        write(project.path('.purlin/config.json'),\n              json.dumps(written, indent=2))\n        output = project.run('--gate', 'strong')\n        assert project.config()['test_framework'] == 'pytest,jest,shell'\n        assert 'dropped' not in output",
      "file": "dev/test_init_scaffold.py",
      "findings": [],
      "name": "test_a_framework_the_tree_carries_survives_a_gate_change",
      "proof": "PROOF-38",
      "reasons": []
    },
    {
      "body": "def test_a_gate_change_drops_a_framework_the_tree_cannot_run(self, project):\n        \"\"\"An older release wrote down every plugin it shipped, runnable or not.\"\"\"\n        project.run('--gate', 'passed')\n        written = project.config()\n        written['test_framework'] = 'pytest,jest,shell,vitest'\n        write(project.path('.purlin/config.json'),\n              json.dumps(written, indent=2))\n        output = project.run('--gate', 'strong')\n        assert project.config()['test_framework'] == 'pytest,shell'\n        for name in ('jest', 'vitest'):\n            assert ('dropped %s from test_framework: nothing in the tree '\n                    'runs it' % name) in output\n        assert not project.has('jest.config.js')\n        assert not project.has('vitest.config.ts')",
      "file": "dev/test_init_scaffold.py",
      "findings": [],
      "name": "test_a_gate_change_drops_a_framework_the_tree_cannot_run",
      "proof": "PROOF-38",
      "reasons": []
    }
  ],
  "triple_hash": "26d907ad98e340000d0cbb35252dc9420bdad313e1ce275f8f9978fa285cc9ef"
}
