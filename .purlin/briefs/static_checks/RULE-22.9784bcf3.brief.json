{
  "ai_review": null,
  "design": null,
  "design_hash": null,
  "feature": "static_checks",
  "generated_at": "2026-09-16T22:51:18Z",
  "layers": [
    "proof text",
    "test body",
    "test strength"
  ],
  "min_strength": 80,
  "observations": [],
  "origin": "eng",
  "proof_hash": "7d32e52cf5e1878a01abafdf245dda91c8942316f184f80c1b7280fcc6a4c207",
  "proofs": [
    {
      "env": null,
      "findings": [
        "happy_path_only"
      ],
      "id": "PROOF-64",
      "text": "Parse the module's own source; verify each brace-language wrapper is one return of a driver call, that the driver is defined exactly 1 time, and that running each wrapper over its always-true fixture returns the reason string that language's own proofs assert",
      "tier": "integration"
    }
  ],
  "record": ".purlin/records/static_checks/20260916T225004Z-3aa5d77-ci-windows.json",
  "risk": "medium",
  "rule": "RULE-22",
  "rule_hash": "8d61541b898719a288ff9aefdc03399c0a74874a852ae486b20125d724e09392",
  "rule_text": "The brace-body checkers are one-line wrappers over one driver, and each keeps the reason its own language's proofs assert",
  "schema": "purlin-brief/2",
  "settled": null,
  "test_hash": "d4f4d3943dfbc8665d90732bbc7778bc0e402cee9abf48dc075a776e8c36b631",
  "test_hash_kind": "file",
  "test_strength": null,
  "tests": [
    {
      "body": "def test_every_wrapper_delegates_and_keeps_its_reason_string(self):\n        path = STATIC_CHECKS_PY\n        with open(path, encoding='utf-8') as f:\n            tree = ast.parse(f.read())\n        functions = [n for n in tree.body\n                     if isinstance(n, (ast.FunctionDef, ast.AsyncFunctionDef))]\n        by_name = {n.name: n for n in functions}\n\n        assert [n.name for n in functions].count('_run_body_checks') == 1, \\\n            \"the driver must be defined exactly once\"\n\n        for name in self._WRAPPERS:\n            node = by_name[name]\n            body = node.body\n            if (body and isinstance(body[0], ast.Expr)\n                    and isinstance(body[0].value, ast.Constant)\n                    and isinstance(body[0].value.value, str)):\n                body = body[1:]          # the docstring is not an inlined check\n            assert len(body) == 1 and isinstance(body[0], ast.Return), \\\n                f\"{name} has {len(body)} statements, so its body is inlined again\"\n            call = body[0].value\n            assert isinstance(call, ast.Call) and isinstance(call.func, ast.Name) \\\n                and call.func.id == '_run_body_checks', \\\n                f\"{name} returns something other than a _run_body_checks(...) call\"\n\n        langs = {n.args[-1].value for n in\n                 [by_name[w].body[-1].value for w in self._WRAPPERS]}\n        assert set(static_checks._BODY_CHECKS) == langs == {'js', 'csharp', 'sql'}, \\\n            \"the table and the wrappers name different languages\"\n\n        with tempfile.TemporaryDirectory() as tmpdir:\n            for wrapper, filename, source, reason in self._FIXTURES:\n                path = os.path.join(tmpdir, filename)\n                with open(path, 'w', encoding='utf-8') as f:\n                    f.write(source)\n                results = getattr(static_checks, wrapper)(path, 'feat')\n                assert len(results) == 1, f\"{wrapper} returned {results}\"\n                got = results[0]\n                assert (got['proof_id'], got['rule_id']) == ('PROOF-1', 'RULE-1')\n                assert got['status'] == 'fail', got\n                assert got['check'] == 'tautology', got\n                assert got['reason'] == reason, (\n                    f\"{wrapper} reason moved: {got['reason']!r} != {reason!r}\")",
      "file": "dev/test_static_checks.py",
      "findings": [],
      "name": "test_every_wrapper_delegates_and_keeps_its_reason_string",
      "proof": "PROOF-64",
      "reasons": []
    }
  ],
  "triple_hash": "9784bcf3ee03b33a24ca9f54ea062330ac9b5005401d85452b07a2720e51cff7"
}
