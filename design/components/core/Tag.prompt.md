Use `Tag` when the string came from the machine rather than a person — `claude-cli`, `gemini-cli`, `gate: strong`, `7c41d0a`.

```jsx
<Tag>claude-cli …</Tag>
<Tag tone="pass">gemini-cli …</Tag>
```

Always monospace, always bordered, never filled. Human-authored labels use `SectionLabel` instead.
