/** 1px horizontal rule. The deck's only separator. */
export interface DividerProps {
  tone?: 'hairline' | 'strong' | 'accent';
  style?: React.CSSProperties;
}
export declare function Divider(props: DividerProps): JSX.Element;
