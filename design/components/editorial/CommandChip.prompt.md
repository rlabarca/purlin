Use `CommandChip` to name the command a slide or panel is about. It sits flush right, level with the slide title.

```jsx
<CommandChip>purlin:verify login</CommandChip>
<CommandChip tone="accent">gate: tested</CommandChip>
```

Unlike `Tag` it has no border — it is typography, not a chip, despite the name.
