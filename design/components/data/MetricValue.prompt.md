Use `MetricValue` for the design/integrity/strength columns. Word-states `excluded` and `structural` render teal, which is the system's "not applicable, on purpose" colour.

```jsx
<MetricValue value="86%" />
<MetricValue value="excluded" />
```
