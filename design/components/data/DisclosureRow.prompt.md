Use `DisclosureRow` as the first cell of a `TableRow`.

```jsx
<DisclosureRow label="skill_audit" open={open} onToggle={() => setOpen(!open)} />
```

The triangle is a Unicode glyph, not an icon asset — the product ships no icon set.
