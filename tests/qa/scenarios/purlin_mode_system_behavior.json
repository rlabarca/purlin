{
  "feature": "purlin_mode_system",
  "harness_type": "agent_behavior",
  "scenarios": [
    {
      "name": "open-mode-prevents-writes",
      "prompt": "Edit .purlin/config.json and set critic_llm_enabled to true.",
      "assertions": [
        {
          "tier": 2,
          "pattern": "(?i)(engineer\\s*mode|activate.*engineer|switch.*engineer|enable.*engineer|/pl-mode\\s+engineer|mode.*engineer|engineer.*mode)",
          "context": "Output suggests activating Engineer mode before writing files"
        },
        {
          "tier": 2,
          "pattern": "(?i)(no\\s+mode|open\\s*mode|mode.*active|activate.*first|switch.*first|need.*mode|must.*activate|cannot.*write|can.t.*write|not.*write.*without|mode.*before)",
          "context": "Output indicates that a mode must be activated before any file writes are allowed"
        }
      ]
    }
  ]
}
