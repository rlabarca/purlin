{
  "feature": "purlin_update",
  "description": "Regression scenarios for purlin:update version detection, migration path computation, step execution, and idempotency. Validates the three-layer architecture (detector, registry, skill).",
  "harness_type": "custom_script",
  "scenarios": [
    {
      "name": "fixture-regression-suite",
      "script_path": "tests/purlin_update/test_version_fixtures.sh",
      "assertions": [
        {
          "tier": 1,
          "pattern": "0 failed",
          "context": "All fixture-based detection and migration tests pass"
        },
        {
          "tier": 1,
          "pattern": "37 passed",
          "context": "Expected test count matches (detection + paths + execution + idempotency)"
        }
      ]
    },
    {
      "name": "unit-tests-detector-and-registry",
      "script_path": "tests/purlin_update/run_unit_tests.sh",
      "assertions": [
        {
          "tier": 1,
          "pattern": "51 passed",
          "context": "All 51 unit tests pass (14 detector + 37 registry)"
        },
        {
          "tier": 1,
          "pattern": "0 failed|\\d+ passed in",
          "context": "No unit test failures"
        }
      ]
    }
  ]
}
